---
phase: 04-inventory-management
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/app/(auth)/inventory/page.tsx
  - src/app/(auth)/inventory/actions.ts
  - src/app/(auth)/inventory/inventory-table.tsx
  - src/app/(auth)/inventory/adjust-stock-dialog.tsx
  - src/app/(auth)/inventory/history-dialog.tsx
autonomous: false
requirements: [INV-01, INV-04]

must_haves:
  truths:
    - "Admin can view all inventory items with SKU, product name, total/reserved/available stock"
    - "Admin can set initial stock for a new SKU from the inventory page"
    - "Admin can manually adjust stock with a reason dropdown and optional note"
    - "Admin can view the audit history for any inventory item"
  artifacts:
    - path: "src/app/(auth)/inventory/page.tsx"
      provides: "Inventory management page (server component)"
      min_lines: 20
    - path: "src/app/(auth)/inventory/inventory-table.tsx"
      provides: "Client-side TanStack Table for inventory list"
      min_lines: 50
    - path: "src/app/(auth)/inventory/adjust-stock-dialog.tsx"
      provides: "Dialog for manual stock adjustment with reason"
      min_lines: 40
    - path: "src/app/(auth)/inventory/actions.ts"
      provides: "Server actions for inventory CRUD"
      exports: ["setStockAction", "adjustStockAction"]
  key_links:
    - from: "src/app/(auth)/inventory/actions.ts"
      to: "src/lib/inventory/actions.ts"
      via: "import setStock/adjustStock"
      pattern: "setStock|adjustStock"
    - from: "src/app/(auth)/inventory/page.tsx"
      to: "src/lib/inventory/queries.ts"
      via: "import getInventoryList"
      pattern: "getInventoryList"
---

<objective>
Build the inventory management UI: a data table showing all inventory items, dialogs for setting/adjusting stock with reason tracking, and an audit history viewer.

Purpose: Gives the admin a central view to manage stock levels (INV-01) and perform manual adjustments with recorded reasons (INV-04). Completes the user-facing inventory feature.

Output: /inventory page with TanStack Table, stock adjustment dialog, history dialog.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-inventory-management/04-CONTEXT.md
@.planning/phases/04-inventory-management/04-01-SUMMARY.md

<interfaces>
<!-- Types and functions created in Plan 01 that this plan consumes -->

From src/lib/inventory/types.ts (created in 04-01):
```typescript
export type AdjustmentReason = 'incoming' | 'defective' | 'physical_count' | 'return' | 'order_ship' | 'order_cancel' | 'other'
export interface InventoryRecord { id, userId, sku, productName, totalStock, reservedStock, availableStock, createdAt, updatedAt }
export interface InventoryHistoryRecord { id, inventoryId, userId, adjustmentReason, delta, previousTotal, newTotal, note, orderId, createdAt }
export interface InventoryFilters { page?, pageSize?, search?, sort?, order? }
export const ADJUSTMENT_REASON_LABELS: Record<AdjustmentReason, string>
```

From src/lib/inventory/queries.ts (created in 04-01):
```typescript
export function getInventoryList(userId: string, filters: InventoryFilters): Promise<{ items: InventoryRecord[], total: number }>
export function getInventoryBySku(userId: string, sku: string): Promise<InventoryRecord | null>
export function getInventoryHistory(inventoryId: string, page?: number, pageSize?: number): Promise<{ items: InventoryHistoryRecord[], total: number }>
```

From src/lib/inventory/actions.ts (created in 04-01):
```typescript
export function adjustStock(userId, sku, delta, reason, opts?): Promise<ActionResult>
export function setStock(userId, sku, productName, totalStock): Promise<ActionResult>
```

From src/app/(auth)/orders/page.tsx (existing pattern):
- Server component fetches data, passes to client table component
- Uses nuqs for URL state (search, pagination, sorting)
- TanStack Table with column definitions in the client component
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory page, server actions, and data table</name>
  <files>src/app/(auth)/inventory/page.tsx, src/app/(auth)/inventory/actions.ts, src/app/(auth)/inventory/inventory-table.tsx</files>
  <read_first>src/app/(auth)/orders/page.tsx, src/lib/inventory/types.ts, src/lib/inventory/queries.ts, src/lib/inventory/actions.ts</read_first>
  <action>
1. Create `src/app/(auth)/inventory/actions.ts` (server actions with 'use server'):
   - `setStockAction(formData: FormData)`: extracts sku, productName, totalStock from formData, gets userId from Supabase auth, calls setStock(). Returns ActionResult. Revalidates /inventory path.
   - `adjustStockAction(formData: FormData)`: extracts sku, delta, reason (AdjustmentReason), note from formData, gets userId, calls adjustStock(). Returns ActionResult. Revalidates /inventory path.

2. Create `src/app/(auth)/inventory/page.tsx` (server component):
   - Gets authenticated user via Supabase
   - Reads search params for filters (page, pageSize, search, sort, order) -- use the same URL param pattern as orders page
   - Calls getInventoryList(userId, filters)
   - Renders page header "재고관리" with an "재고 등록" (Add Stock) button
   - Passes items + total + filters to InventoryTable client component
   - The "재고 등록" button opens the AdjustStockDialog in "set" mode

3. Create `src/app/(auth)/inventory/inventory-table.tsx` ('use client'):
   - TanStack Table v8 with columns: SKU, 상품명 (productName), 총재고 (totalStock), 예약 (reservedStock), 가용 (availableStock), 최종수정 (updatedAt), 작업 (actions)
   - Actions column: "조정" (adjust) button and "이력" (history) button per row
   - "조정" opens AdjustStockDialog in "adjust" mode for that SKU
   - "이력" opens HistoryDialog for that inventory item
   - Search input filters by SKU or product name (updates URL param via nuqs)
   - Pagination controls matching orders page pattern
   - Sortable column headers
   - Color coding: availableStock <= 0 shows red text, <= 10 shows yellow (low stock indicator, Claude's discretion per CONTEXT.md)
  </action>
  <verify>
    <automated>cd /Users/ian/Desktop/funtastic-saas/.claude/worktrees/infallible-pike && npx tsc --noEmit src/app/\(auth\)/inventory/page.tsx src/app/\(auth\)/inventory/inventory-table.tsx src/app/\(auth\)/inventory/actions.ts 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "재고관리" src/app/\(auth\)/inventory/page.tsx
    - grep -q "getInventoryList" src/app/\(auth\)/inventory/page.tsx
    - grep -q "setStockAction" src/app/\(auth\)/inventory/actions.ts
    - grep -q "adjustStockAction" src/app/\(auth\)/inventory/actions.ts
    - grep -q "useReactTable" src/app/\(auth\)/inventory/inventory-table.tsx
    - grep -q "totalStock" src/app/\(auth\)/inventory/inventory-table.tsx
  </acceptance_criteria>
  <done>Inventory page renders a TanStack Table with all inventory columns, search/sort/pagination work, server actions wire to business logic</done>
</task>

<task type="auto">
  <name>Task 2: Stock adjustment dialog and history dialog</name>
  <files>src/app/(auth)/inventory/adjust-stock-dialog.tsx, src/app/(auth)/inventory/history-dialog.tsx</files>
  <read_first>src/app/(auth)/inventory/actions.ts, src/lib/inventory/types.ts, src/app/(auth)/inventory/inventory-table.tsx</read_first>
  <action>
1. Create `src/app/(auth)/inventory/adjust-stock-dialog.tsx` ('use client'):
   - Two modes: "set" (new inventory item) and "adjust" (change existing)
   - **Set mode**: fields for SKU (text input), productName (text input), totalStock (number input). Calls setStockAction.
   - **Adjust mode**: shows SKU (read-only), current stock (read-only), delta (number input, can be negative), reason (native HTML select per Phase 1 pattern, options from ADJUSTMENT_REASON_LABELS -- only show manual reasons: incoming, defective, physical_count, return, other), note (optional textarea). Calls adjustStockAction.
   - Uses a dialog/modal pattern -- can use HTML dialog element or a simple overlay div with Tailwind
   - Shows success/error toast via sonner after action completes
   - Closes dialog on success

2. Create `src/app/(auth)/inventory/history-dialog.tsx` ('use client'):
   - Shows audit history for a specific inventory item
   - Fetches history via a server action or direct API call to getInventoryHistory
   - Add `getHistoryAction(inventoryId: string, page?: number)` server action to actions.ts if needed
   - Table columns: 일시 (createdAt), 사유 (adjustmentReason label), 변동 (delta with +/- prefix), 이전 (previousTotal), 이후 (newTotal), 메모 (note), 주문 (orderId link if present)
   - Delta column: green text for positive, red for negative
   - Pagination if history is long (pageSize 20)
   - Simple dialog/modal overlay matching adjust dialog style
  </action>
  <verify>
    <automated>cd /Users/ian/Desktop/funtastic-saas/.claude/worktrees/infallible-pike && npx tsc --noEmit src/app/\(auth\)/inventory/adjust-stock-dialog.tsx src/app/\(auth\)/inventory/history-dialog.tsx 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "setStockAction\|adjustStockAction" src/app/\(auth\)/inventory/adjust-stock-dialog.tsx
    - grep -q "ADJUSTMENT_REASON_LABELS" src/app/\(auth\)/inventory/adjust-stock-dialog.tsx
    - grep -q "inventoryHistory\|getHistoryAction\|getInventoryHistory" src/app/\(auth\)/inventory/history-dialog.tsx
    - grep -q "delta" src/app/\(auth\)/inventory/history-dialog.tsx
  </acceptance_criteria>
  <done>Adjust dialog supports set/adjust modes with reason enum and note, history dialog shows paginated audit log with color-coded deltas</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify inventory management end-to-end</name>
  <files>none</files>
  <action>Human verifies the complete inventory management system works correctly end-to-end.</action>
  <what-built>Complete inventory management system: schema with audit log, automatic stock deduction on ship/cancel, inventory page with TanStack Table, manual adjustment dialog with reason tracking, and history viewer</what-built>
  <how-to-verify>
    1. Navigate to /inventory in the app
    2. Click "재고 등록" to add a test inventory item (enter SKU, product name, quantity)
    3. Verify the item appears in the table with correct stock numbers
    4. Click "조정" on the item, enter a negative delta with reason "불량", confirm
    5. Verify totalStock decreased and availableStock updated
    6. Click "이력" on the item and verify both the initial set and the adjustment appear in the audit log
    7. Go to /orders, ship an order that has a matching SKU -- verify inventory decremented
    8. Cancel that order -- verify inventory restored
  </how-to-verify>
  <verify>Human confirms all 8 verification steps pass</verify>
  <done>All inventory features verified working by human</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- /inventory page loads with inventory table
- Stock set creates new inventory record with audit entry
- Stock adjust modifies record and creates audit entry with reason
- History dialog shows complete audit trail
- Order shipped -> inventory deducted (check via history dialog)
- Order cancelled (from shipped) -> inventory restored
</verification>

<success_criteria>
- Admin can view inventory items in a sortable, searchable table
- Admin can add new inventory items with initial stock
- Admin can adjust stock with reason selection and optional note
- Audit history shows complete change trail per item
- Low stock items visually highlighted
</success_criteria>

<output>
After completion, create `.planning/phases/04-inventory-management/04-02-SUMMARY.md`
</output>
