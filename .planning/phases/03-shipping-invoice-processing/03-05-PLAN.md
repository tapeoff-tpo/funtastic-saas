---
phase: 03-shipping-invoice-processing
plan: 05
type: execute
wave: 3
depends_on: ["03-02", "03-03", "03-04"]
files_modified:
  - src/app/(auth)/orders/shipping-actions.tsx
  - src/app/(auth)/orders/invoice-upload-dialog.tsx
  - src/app/(auth)/orders/excel-import-dialog.tsx
  - src/app/(auth)/orders/page.tsx
  - src/app/(auth)/orders/columns.tsx
  - src/app/(auth)/shipping/combined/page.tsx
  - src/app/(auth)/shipping/templates/page.tsx
  - src/app/(auth)/shipping/print/page.tsx
  - src/app/(auth)/orders/actions.ts
  - src/app/api/shipping/export/route.ts
  - src/app/api/shipping/import/route.ts
autonomous: false
requirements: [SHIP-01, SHIP-02, SHIP-03, SHIP-04, SHIP-05, SHIP-06, SHIP-07, SHIP-08, DATA-01]

must_haves:
  truths:
    - "Admin can select orders and upload invoices via API with tracking number and carrier selection"
    - "Admin can upload an Excel file to bulk-assign tracking numbers to orders"
    - "Admin can see combined shipping suggestions and confirm/reject groups"
    - "Admin can export orders to carrier-specific Excel with template selection"
    - "Admin can export a custom order list to Excel with selected columns"
    - "Admin can print shipping labels for selected orders"
    - "Admin can manage carrier template column layouts"
    - "Admin can split an order into multiple shipments"
  artifacts:
    - path: "src/app/(auth)/orders/shipping-actions.tsx"
      provides: "Bulk action buttons: Upload Invoice, Excel Import, Export, Print, Combined Shipping"
      contains: "shipping-actions"
    - path: "src/app/(auth)/orders/invoice-upload-dialog.tsx"
      provides: "Dialog for entering tracking number and selecting carrier"
      contains: "trackingNumber"
    - path: "src/app/(auth)/shipping/combined/page.tsx"
      provides: "Combined shipping review UI with confirm/reject per group"
      contains: "ShipmentGroup"
    - path: "src/app/(auth)/shipping/templates/page.tsx"
      provides: "Carrier template management page"
      contains: "CarrierTemplate"
    - path: "src/app/(auth)/shipping/print/page.tsx"
      provides: "Batch shipping label print page with CSS @media print"
      contains: "@media print"
  key_links:
    - from: "src/app/(auth)/orders/shipping-actions.tsx"
      to: "src/lib/shipping/actions.ts"
      via: "Server action calls for invoice upload and bulk operations"
      pattern: "queueInvoiceUpload|bulkQueueInvoiceUpload"
    - from: "src/app/api/shipping/export/route.ts"
      to: "src/lib/shipping/excel/export.ts"
      via: "API route calls exportToCarrierExcel"
      pattern: "exportToCarrierExcel"
    - from: "src/app/(auth)/shipping/combined/page.tsx"
      to: "src/lib/shipping/combined-queries.ts"
      via: "Reads groups, confirms/rejects via server actions"
      pattern: "getShipmentGroups|confirmShipmentGroup"
---

<objective>
Wire all Phase 3 backend logic into the UI: shipping action buttons on the order dashboard, invoice upload dialog, Excel import/export API routes, combined shipping review page, carrier template management, and batch label printing.

Purpose: Make all shipping features accessible to the admin through the existing dashboard and new shipping pages, completing the 사방넷 replacement workflow.
Output: Shipping UI components, API routes for Excel download/upload, combined shipping page, template management page, print page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-shipping-invoice-processing/03-RESEARCH.md
@.planning/phases/03-shipping-invoice-processing/03-02-SUMMARY.md
@.planning/phases/03-shipping-invoice-processing/03-03-SUMMARY.md
@.planning/phases/03-shipping-invoice-processing/03-04-SUMMARY.md

@src/app/(auth)/orders/page.tsx
@src/app/(auth)/orders/columns.tsx
@src/app/(auth)/orders/data-table.tsx
@src/app/(auth)/orders/status-actions.tsx
@src/app/(auth)/orders/actions.ts

<interfaces>
From src/lib/shipping/actions.ts (Plan 02):
```typescript
export async function queueInvoiceUpload(orderId: string, trackingNumber: string, carrierId: string): Promise<{ success: boolean; error?: string }>
export async function bulkQueueInvoiceUpload(orders: Array<{ orderId: string, trackingNumber: string, carrierId: string }>): Promise<{ queued: number, errors: Array<{ orderId: string, error: string }> }>
```

From src/lib/shipping/combined-shipping.ts (Plan 03):
```typescript
export function findMergeCandidates(orders: OrderWithItems[], maxPackQuantity?: number): ShipmentGroup[]
```

From src/lib/shipping/combined-queries.ts (Plan 03):
```typescript
export function getShipmentGroups(userId: string, status?: string): Promise<ShipmentGroupWithOrders[]>
export function confirmShipmentGroup(groupId: string): Promise<void>
export function rejectShipmentGroup(groupId: string): Promise<void>
```

From src/lib/shipping/excel/export.ts (Plan 04):
```typescript
export function exportToCarrierExcel(orders: OrderWithShipment[], template: CarrierTemplate): Promise<Buffer>
```

From src/lib/shipping/excel/import.ts (Plan 04):
```typescript
export function parseInvoiceExcel(buffer: Buffer, columnMapping?): Promise<{ valid, invalid }>
export function matchInvoicesToOrders(parsedRows, userId): Promise<{ matched, unmatched }>
```

From src/lib/shipping/excel/order-export.ts (Plan 04):
```typescript
export function exportOrdersToExcel(orders, columns): Promise<Buffer>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Order dashboard shipping actions + invoice upload + Excel import/export API routes</name>
  <files>src/app/(auth)/orders/shipping-actions.tsx, src/app/(auth)/orders/invoice-upload-dialog.tsx, src/app/(auth)/orders/excel-import-dialog.tsx, src/app/(auth)/orders/page.tsx, src/app/(auth)/orders/columns.tsx, src/app/(auth)/orders/actions.ts, src/app/api/shipping/export/route.ts, src/app/api/shipping/import/route.ts</files>
  <read_first>src/app/(auth)/orders/page.tsx, src/app/(auth)/orders/columns.tsx, src/app/(auth)/orders/data-table.tsx, src/app/(auth)/orders/status-actions.tsx, src/app/(auth)/orders/actions.ts, src/lib/shipping/actions.ts, src/lib/shipping/carrier-codes.ts</read_first>
  <action>
    1. **Create `src/app/(auth)/orders/shipping-actions.tsx`** (client component):
       - Renders a toolbar row of shipping action buttons below the order table filters
       - Buttons: "송장업로드" (Invoice Upload), "엑셀 송장등록" (Excel Import), "엑셀 내보내기" (Excel Export), "합포장" (Combined Shipping link), "라벨인쇄" (Print Labels)
       - Receives `selectedOrderIds: string[]` prop from TanStack Table row selection
       - "송장업로드" opens InvoiceUploadDialog for selected orders
       - "엑셀 송장등록" opens ExcelImportDialog
       - "엑셀 내보내기" triggers download via /api/shipping/export with selected order IDs
       - "합포장" links to /shipping/combined
       - "라벨인쇄" opens /shipping/print in new window with selected order IDs as URL params
       - Disabled states when no orders selected (except Excel Import and Combined Shipping which work globally)
       - Use shadcn/ui Button components

    2. **Create `src/app/(auth)/orders/invoice-upload-dialog.tsx`** (client component):
       - shadcn/ui Dialog with form fields:
         - Carrier dropdown (native HTML select per Phase 1 pattern) populated from CARRIERS
         - Tracking number input (text)
         - If multiple orders selected, show count: "N건 주문에 동일 송장 적용" with option to use same tracking number for all (합포장 case)
       - On submit: call server action `queueInvoiceUpload` or `bulkQueueInvoiceUpload`
       - Show sonner toast with result (queued count, error count)

    3. **Create `src/app/(auth)/orders/excel-import-dialog.tsx`** (client component):
       - shadcn/ui Dialog with file upload input (accept=".xlsx,.xls")
       - Column mapping dropdowns: which Excel column maps to order ID, tracking number, carrier
       - Preview of first 5 rows after file selection
       - On submit: POST to /api/shipping/import with FormData
       - Display results: matched count, unmatched rows, errors
       - "적용" button to apply matched invoices via bulkQueueInvoiceUpload

    4. **Update `src/app/(auth)/orders/page.tsx`**:
       - Import and render ShippingActions component
       - Pass selectedOrderIds from TanStack Table's row selection state

    5. **Update `src/app/(auth)/orders/columns.tsx`**:
       - Add a "송장상태" (Invoice Status) column showing upload status from the shipments relation
       - Add checkbox column for row selection (if not already present from Phase 2)

    6. **Update `src/app/(auth)/orders/actions.ts`**:
       - Add `getOrdersForExport(orderIds: string[])` server action that returns orders with shipping data for Excel export
       - Add `detectCombinedShipping(userId: string)` server action that calls findMergeCandidates and creates shipment groups

    7. **Create `src/app/api/shipping/export/route.ts`** (GET):
       - Accept query params: orderIds (comma-separated), templateId (optional, uses default if not provided), type ('carrier' | 'order-list'), columns (for order-list type)
       - For carrier type: get template, get orders, call exportToCarrierExcel, return as file download (Content-Disposition: attachment)
       - For order-list type: get orders, call exportOrdersToExcel with selected columns
       - Return Excel buffer with proper headers: `Content-Type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`

    8. **Create `src/app/api/shipping/import/route.ts`** (POST):
       - Accept multipart/form-data with Excel file + column mapping
       - Parse with parseInvoiceExcel
       - Match with matchInvoicesToOrders
       - Return JSON: { matched, unmatched, invalid }
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "shipping-actions" src/app/\(auth\)/orders/shipping-actions.tsx
    - grep -q "invoice-upload-dialog" src/app/\(auth\)/orders/invoice-upload-dialog.tsx
    - grep -q "excel-import-dialog" src/app/\(auth\)/orders/excel-import-dialog.tsx
    - grep -q "ShippingActions" src/app/\(auth\)/orders/page.tsx
    - grep -rq "송장상태" src/app/\(auth\)/orders/columns.tsx
    - test -f src/app/api/shipping/export/route.ts
    - test -f src/app/api/shipping/import/route.ts
  </acceptance_criteria>
  <done>Order dashboard has shipping action buttons, invoice upload dialog with carrier selection, Excel import dialog with column mapping preview, Excel export and import API routes work, invoice status column visible in table.</done>
</task>

<task type="auto">
  <name>Task 2: Combined shipping page, template management page, and print page</name>
  <files>src/app/(auth)/shipping/combined/page.tsx, src/app/(auth)/shipping/templates/page.tsx, src/app/(auth)/shipping/print/page.tsx, src/app/(auth)/shipping/layout.tsx</files>
  <read_first>src/lib/shipping/combined-queries.ts, src/lib/shipping/combined-shipping.ts, src/lib/shipping/template-queries.ts, src/lib/shipping/excel/templates.ts, src/lib/shipping/types.ts, src/app/(auth)/orders/page.tsx</read_first>
  <action>
    1. **Create `src/app/(auth)/shipping/layout.tsx`**:
       - Simple layout that wraps shipping sub-pages
       - Breadcrumb navigation back to orders

    2. **Create `src/app/(auth)/shipping/combined/page.tsx`** per D-08:
       - Server component that loads shipment groups via getShipmentGroups(userId, 'suggested')
       - Also has "새로 감지" button that triggers detectCombinedShipping server action
       - Display groups as cards, each showing:
         - Group key info (buyer name, address, date)
         - Fulfillment code badge (normal/frozen/large)
         - List of orders in the group with product names
         - "확인" (Confirm) and "거절" (Reject) buttons per group
       - Confirmed groups show "송장등록" button to assign tracking number for the whole group
       - Use shadcn/ui Card, Badge, Button components
       - Empty state: "합포장 대상이 없습니다"

    3. **Create `src/app/(auth)/shipping/templates/page.tsx`** per D-11:
       - List existing carrier templates for the user (call getCarrierTemplates)
       - "기본 양식 초기화" button to run seedDefaultTemplates
       - Edit template: inline form or dialog to add/remove/reorder columns
         - Column list with drag handles (or up/down buttons for simplicity)
         - Add column: select from AVAILABLE_ORDER_FIELDS dropdown
         - Remove column: X button
         - Each column shows: header name, mapped field, width
       - Create new template: carrier dropdown + name + column builder
       - Delete template with confirmation dialog
       - Use shadcn/ui Table, Dialog, Button, Input components

    4. **Create `src/app/(auth)/shipping/print/page.tsx`** per D-12:
       - Receives order IDs from URL search params (e.g., ?ids=uuid1,uuid2,...)
       - Loads orders with shipping address details
       - Renders shipping labels in a print-optimized layout:
         - Each label: recipient name, phone, zip, full address, sender info, product summary
         - CSS `@media print` styles: hide non-print elements, page-break-after per label
         - Print grid: 2 labels per row on A4 (configurable)
       - "인쇄" button that calls `window.print()`
       - Screen view shows labels in a preview grid
       - Tailwind classes for print: `print:block`, `print:hidden`, etc.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - test -f src/app/\(auth\)/shipping/combined/page.tsx
    - test -f src/app/\(auth\)/shipping/templates/page.tsx
    - test -f src/app/\(auth\)/shipping/print/page.tsx
    - grep -q "@media print" src/app/\(auth\)/shipping/print/page.tsx
    - grep -q "confirmShipmentGroup" src/app/\(auth\)/shipping/combined/page.tsx
    - grep -q "AVAILABLE_ORDER_FIELDS" src/app/\(auth\)/shipping/templates/page.tsx
  </acceptance_criteria>
  <done>Combined shipping review page shows groups with confirm/reject actions. Template management page allows creating/editing/deleting carrier templates. Print page renders labels with CSS @media print for batch printing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify full shipping workflow</name>
  <files>none</files>
  <action>
    Human verification checkpoint. No code changes -- admin verifies the complete shipping workflow end-to-end.

    Steps to verify:
    1. Start dev server: npm run dev (and npx tsx worker.ts for BullMQ worker)
    2. Go to /orders -- verify new shipping action buttons appear below filters
    3. Select orders, click "송장업로드" -- verify dialog shows carrier dropdown and tracking number input
    4. Test invoice upload: enter a tracking number, select CJ대한통운, submit -- verify toast shows "queued"
    5. Check "송장상태" column shows status (pending, uploading, uploaded/failed)
    6. Test Excel import: click "엑셀 송장등록", upload a test .xlsx -- verify preview and matching
    7. Test Excel export: select orders, click "엑셀 내보내기" -- verify .xlsx downloads with correct columns
    8. Go to /shipping/combined -- verify combined shipping groups appear
    9. Go to /shipping/templates -- verify 5 default carrier templates exist, try editing
    10. Test label print: select orders, click "라벨인쇄" -- verify print preview opens

    Resume signal: Type "approved" or describe issues to fix.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Admin has verified that the full shipping workflow works: invoice upload, Excel import/export, combined shipping, template management, and label printing.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` -- no TypeScript errors
- `npx vitest run` -- all tests pass (including prior plans)
- All 5 shipping pages accessible: /orders (with shipping buttons), /shipping/combined, /shipping/templates, /shipping/print
- Excel export downloads a valid .xlsx file
- Invoice upload dialog submits and shows queue status
</verification>

<success_criteria>
- Full shipping workflow is operational end-to-end
- Admin can upload invoices via API (dialog) and Excel (import)
- Admin can review and manage combined shipping groups
- Admin can export to carrier-specific or custom Excel formats
- Admin can print shipping labels in batch
- Admin can create/edit carrier templates
- All UI uses established patterns (shadcn/ui, server actions, native select)
</success_criteria>

<output>
After completion, create `.planning/phases/03-shipping-invoice-processing/03-05-SUMMARY.md`
</output>
