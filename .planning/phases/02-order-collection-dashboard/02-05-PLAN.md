---
phase: 02-order-collection-dashboard
plan: 05
type: execute
wave: 4
depends_on: ["02-04"]
files_modified:
  - src/app/(auth)/orders/actions.ts
  - src/app/(auth)/orders/status-actions.tsx
  - src/app/(auth)/orders/hold-dialog.tsx
  - src/app/(auth)/orders/claims-filter.tsx
  - src/app/(auth)/orders/columns.tsx
  - src/app/(auth)/orders/page.tsx
  - src/app/(auth)/orders/data-table.tsx
autonomous: false
requirements:
  - ORD-04
  - ORD-05
  - ORD-06
  - ORD-07

must_haves:
  truths:
    - "Admin can change order status through the valid workflow transitions"
    - "Admin can hold a problematic order with a reason text"
    - "Admin can release a held order back to its previous status"
    - "Claims (cancel/return/exchange) are visible in a filtered view on the dashboard per D-10"
    - "Bulk status change works for selected rows"
    - "Invalid status transitions are rejected with an error message"
  artifacts:
    - path: "src/app/(auth)/orders/actions.ts"
      provides: "Server actions wrapping order business logic"
      exports: ["changeStatusAction", "holdOrderAction", "releaseOrderAction", "bulkChangeStatusAction"]
    - path: "src/app/(auth)/orders/status-actions.tsx"
      provides: "Status change dropdown and bulk action bar"
      contains: "VALID_TRANSITIONS"
    - path: "src/app/(auth)/orders/hold-dialog.tsx"
      provides: "Hold/release dialog with reason input"
      contains: "holdOrderAction"
    - path: "src/app/(auth)/orders/claims-filter.tsx"
      provides: "Claims tab/filter toggle on dashboard"
      contains: "claimType"
  key_links:
    - from: "src/app/(auth)/orders/actions.ts"
      to: "src/lib/orders/actions.ts"
      via: "Server actions call business logic"
      pattern: "updateOrderStatus|holdOrder|releaseOrder"
    - from: "src/app/(auth)/orders/status-actions.tsx"
      to: "src/app/(auth)/orders/actions.ts"
      via: "Client component calls server actions"
      pattern: "changeStatusAction|bulkChangeStatusAction"
    - from: "src/app/(auth)/orders/hold-dialog.tsx"
      to: "src/app/(auth)/orders/actions.ts"
      via: "Dialog calls hold/release server actions"
      pattern: "holdOrderAction|releaseOrderAction"
---

<objective>
Add order status management, hold/release functionality, claims visibility, and bulk actions to the order dashboard. This completes the order lifecycle management that admins use daily.

Purpose: Enable full order workflow management and claims visibility on the existing dashboard (ORD-04, ORD-05 UI, ORD-06, ORD-07).
Output: Server actions for status/hold/release, UI components for status changes, hold dialog, claims filter, and bulk action bar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-order-collection-dashboard/02-CONTEXT.md
@.planning/phases/02-order-collection-dashboard/02-RESEARCH.md
@.planning/phases/02-order-collection-dashboard/02-01-SUMMARY.md
@.planning/phases/02-order-collection-dashboard/02-04-SUMMARY.md

<interfaces>
<!-- From Plan 01: business logic the server actions wrap -->

From src/lib/orders/actions.ts:
```typescript
export async function updateOrderStatus(orderId: string, newStatus: OrderStatus): Promise<{ success: boolean; error?: string }>
export async function holdOrder(orderId: string, reason: string): Promise<{ success: boolean; error?: string }>
export async function releaseOrder(orderId: string): Promise<{ success: boolean; error?: string }>
export async function bulkUpdateStatus(orderIds: string[], newStatus: OrderStatus): Promise<{ updated: number; errors: Array<{orderId: string; error: string}> }>
```

From src/lib/orders/types.ts:
```typescript
export type OrderStatus = 'new' | 'confirmed' | 'preparing' | 'shipped' | 'delivering' | 'delivered' | 'cancelled'
export type ClaimType = 'cancel' | 'return' | 'exchange'
export const ORDER_STATUS_LABELS: Record<OrderStatus, string>
export const VALID_TRANSITIONS: Record<OrderStatus, OrderStatus[]>
```

From src/app/(auth)/orders/columns.tsx (Plan 04):
```typescript
export type OrderRow = { id, marketplaceId, marketplaceOrderId, buyerName, status, orderedAt, totalAmount, isHeld, holdReason, items }
export const columns: ColumnDef<OrderRow>[]
```

From src/app/(auth)/orders/data-table.tsx (Plan 04):
```typescript
// Client component with useReactTable, row selection enabled
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server actions for status change, hold/release, and bulk operations</name>
  <files>src/app/(auth)/orders/actions.ts</files>
  <read_first>src/lib/orders/actions.ts, src/lib/orders/types.ts, src/app/(auth)/orders/page.tsx</read_first>
  <action>
    1. Create `src/app/(auth)/orders/actions.ts` ("use server"):
       - Import updateOrderStatus, holdOrder, releaseOrder, bulkUpdateStatus from '@/lib/orders/actions'
       - Import { revalidatePath } from 'next/cache'
       - Import OrderStatus from '@/lib/orders/types'

       - Export `async function changeStatusAction(orderId: string, newStatus: OrderStatus)`:
         - Call updateOrderStatus(orderId, newStatus)
         - revalidatePath('/orders') to refresh table data
         - Return result { success, error? }

       - Export `async function holdOrderAction(orderId: string, reason: string)`:
         - Validate reason is not empty
         - Call holdOrder(orderId, reason)
         - revalidatePath('/orders')
         - Return result

       - Export `async function releaseOrderAction(orderId: string)`:
         - Call releaseOrder(orderId)
         - revalidatePath('/orders')
         - Return result

       - Export `async function bulkChangeStatusAction(orderIds: string[], newStatus: OrderStatus)`:
         - Call bulkUpdateStatus(orderIds, newStatus)
         - revalidatePath('/orders')
         - Return result { updated, errors }
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "'use server'" src/app/\(auth\)/orders/actions.ts` finds server directive
    - `grep "changeStatusAction" src/app/\(auth\)/orders/actions.ts` finds status action
    - `grep "holdOrderAction" src/app/\(auth\)/orders/actions.ts` finds hold action
    - `grep "releaseOrderAction" src/app/\(auth\)/orders/actions.ts` finds release action
    - `grep "bulkChangeStatusAction" src/app/\(auth\)/orders/actions.ts` finds bulk action
    - `grep "revalidatePath" src/app/\(auth\)/orders/actions.ts` finds cache invalidation
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Server actions wrap all order business logic with path revalidation for table refresh.</done>
</task>

<task type="auto">
  <name>Task 2: Status change UI, hold/release dialog, claims filter, and bulk action bar</name>
  <files>src/app/(auth)/orders/status-actions.tsx, src/app/(auth)/orders/hold-dialog.tsx, src/app/(auth)/orders/claims-filter.tsx, src/app/(auth)/orders/columns.tsx, src/app/(auth)/orders/page.tsx, src/app/(auth)/orders/data-table.tsx</files>
  <read_first>src/app/(auth)/orders/actions.ts, src/app/(auth)/orders/columns.tsx, src/app/(auth)/orders/data-table.tsx, src/app/(auth)/orders/page.tsx, src/app/(auth)/orders/filters.tsx, src/lib/orders/types.ts</read_first>
  <action>
    1. Create `src/app/(auth)/orders/status-actions.tsx` ("use client"):
       - Import VALID_TRANSITIONS, ORDER_STATUS_LABELS, OrderStatus from '@/lib/orders/types'
       - Import changeStatusAction, bulkChangeStatusAction from './actions'
       - Import { toast } from 'sonner'
       - Export `StatusDropdown` component (props: { orderId, currentStatus, isHeld }):
         - Dropdown button showing current status as Korean label
         - Shows allowed next statuses from VALID_TRANSITIONS[currentStatus]
         - Disabled if isHeld (held orders cannot change status per D-11)
         - On select: call changeStatusAction, show toast on success/error
       - Export `BulkActionBar` component (props: { selectedIds: string[], onClear: () => void }):
         - Shows when selectedIds.length > 0
         - Floating bar at bottom or top: "{N}개 주문 선택됨"
         - Status change dropdown (from VALID_TRANSITIONS, pick common valid transitions)
         - Hold button (opens hold dialog)
         - On bulk status change: call bulkChangeStatusAction, show toast with result counts

    2. Create `src/app/(auth)/orders/hold-dialog.tsx` ("use client"):
       - Import holdOrderAction, releaseOrderAction from './actions'
       - Import { toast } from 'sonner'
       - Export `HoldDialog` component (props: { orderId, isHeld, holdReason? }):
         - If not held: shows "보류" button. On click: opens dialog/modal with textarea for reason. Submit calls holdOrderAction.
         - If held: shows "보류 해제" button with current reason displayed. On click: calls releaseOrderAction.
         - Shows toast on success/error
         - Use simple dialog pattern (state-controlled div with backdrop, or shadcn dialog if available)
       - Export `BulkHoldDialog` for bulk hold from BulkActionBar

    3. Create `src/app/(auth)/orders/claims-filter.tsx` ("use client"):
       - Import { useQueryState, parseAsString } from 'nuqs'
       - Render tab-style filter at top of order list (per D-10: claims shown in separate tab or filter view):
         - "전체 주문" tab (no claimType filter)
         - "취소" tab (claimType=cancel)
         - "반품" tab (claimType=return)
         - "교환" tab (claimType=exchange)
       - Clicking a tab updates URL param `claimType` via nuqs

    4. Update `src/app/(auth)/orders/columns.tsx`:
       - Add actions column (last column) with StatusDropdown and HoldDialog trigger
       - Add claim type badge if order has associated claims (show 취소/반품/교환 badge)
       - Show 보류 badge overlay on status badge when isHeld=true with holdReason tooltip

    5. Update `src/app/(auth)/orders/page.tsx`:
       - Add ClaimsFilter component above the table
       - Pass claimType filter from searchParams to getOrders

    6. Update `src/app/(auth)/orders/data-table.tsx`:
       - Add BulkActionBar that receives selected row IDs from table state
       - Wire row selection state to BulkActionBar visibility
       - Pass selectedIds to BulkActionBar
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "StatusDropdown" src/app/\(auth\)/orders/status-actions.tsx` finds the component
    - `grep "BulkActionBar" src/app/\(auth\)/orders/status-actions.tsx` finds bulk actions
    - `grep "VALID_TRANSITIONS" src/app/\(auth\)/orders/status-actions.tsx` finds transition validation
    - `grep "HoldDialog" src/app/\(auth\)/orders/hold-dialog.tsx` finds the hold dialog
    - `grep "holdOrderAction" src/app/\(auth\)/orders/hold-dialog.tsx` finds hold action call
    - `grep "releaseOrderAction" src/app/\(auth\)/orders/hold-dialog.tsx` finds release action call
    - `grep "claimType" src/app/\(auth\)/orders/claims-filter.tsx` finds claim type filter
    - `grep "취소\|반품\|교환" src/app/\(auth\)/orders/claims-filter.tsx` finds Korean claim labels
    - `grep "StatusDropdown\|HoldDialog" src/app/\(auth\)/orders/columns.tsx` finds action column components
    - `grep "BulkActionBar" src/app/\(auth\)/orders/data-table.tsx` finds bulk action bar integration
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Status change dropdown shows valid transitions per order. Hold dialog captures reason and blocks further status changes. Release restores previous status. Claims filter tabs show cancel/return/exchange orders. Bulk actions work on selected rows.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete order dashboard functionality</name>
  <files>src/app/(auth)/orders/page.tsx</files>
  <action>
    Human verification of the complete order collection and dashboard system built across all 5 plans:
    - Database schema with orders, claims, job_logs tables
    - Coupang adapter (HMAC-SHA256 signing) and Naver adapter (OAuth2 token management)
    - BullMQ worker for scheduled order/claims collection
    - Order dashboard with TanStack Table, server-side pagination, URL-synced filters
    - Status workflow management (신규 -> 확인 -> 출고대기 -> 출고완료 -> 배송중 -> 배송완료)
    - Hold/release functionality with reason text
    - Claims tab filter (취소/반품/교환)
    - Bulk actions on selected orders
  </action>
  <verify>
    Human verifies:
    1. Start the dev server: `npm run dev`
    2. Navigate to /orders -- verify the orders page loads with table and filter controls
    3. Verify sidebar shows "주문 관리" link with active state
    4. Verify filters: marketplace dropdown, status dropdown with Korean labels, date range, search box
    5. Verify URL changes when filters are applied (bookmarkable per D-06)
    6. Verify pagination controls: Previous/Next, page count, page size selector
    7. Verify claims filter tabs: 전체 주문, 취소, 반품, 교환
    8. If test data exists: verify status change and hold/release functionality
    9. Run `npx tsc --noEmit` and `npx vitest run` -- both should pass
  </verify>
  <done>Human approved the order dashboard UI, status management, and claims filter functionality.</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npx vitest run` -- all tests pass
- Orders page renders with full UI at /orders
- Status transitions, hold/release, claims filter all functional
</verification>

<success_criteria>
- Admin can move orders through status workflow with Korean labels (ORD-04 per D-07)
- Claims are visible in filtered tabs on the dashboard (ORD-05 per D-10)
- Admin can hold orders with reason and release them (ORD-06, ORD-07 per D-11)
- Bulk operations work on selected rows
- Human verification confirms visual correctness and interaction flow
</success_criteria>

<output>
After completion, create `.planning/phases/02-order-collection-dashboard/02-05-SUMMARY.md`
</output>
