/**
 * Phase 8 — Deprecated.
 *
 * Replaced by `OrderTabs` (./order-tabs.tsx) per CONTEXT.md D-01.
 * The 6단계 stage 탭 모델은 9단계 status/claim 탭으로 통합되었다.
 *
 * 이 파일은 import 경로가 외부에 남아있을 수 있으므로 빈 export로 보존된다.
 * 새 코드에서는 OrderTabs를 사용한다.
 */
export {}
