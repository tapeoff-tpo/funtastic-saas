---
phase: 08-orders-ux-improvements
plan: 03
type: execute
wave: 3
depends_on: ['08-01', '08-02']
files_modified:
  - src/lib/orders/types.ts
  - src/lib/orders/queries.ts
  - src/app/(auth)/orders/order-tabs.tsx
  - src/app/(auth)/orders/page.tsx
  - src/app/(auth)/orders/columns.tsx
  - src/app/(auth)/orders/stage-tabs.tsx
  - src/app/(auth)/orders/claims-filter.tsx
  - tests/orders/get-order-stats.test.ts
  - tests/orders/get-orders.test.ts
  - tests/orders/order-tabs.test.tsx
  - tests/orders/page-header.test.tsx
  - tests/orders/columns.test.tsx
autonomous: true
requirements:
  - SC-01
  - SC-02
  - SC-03
  - SC-04
  - SC-05
  - SC-06
must_haves:
  truths:
    - "주문관리 페이지 헤더에 '엑셀 업로드' 링크가 더 이상 없다 (SC-02)"
    - "stage-tabs 진입점이 page.tsx에서 제거되고 ClaimsFilter도 9탭 OrderTabs로 대체된다 (CONTEXT.md D-01)"
    - "OrderTabs 컴포넌트가 9개 탭을 렌더하고 카운트 뱃지를 표시한다: 신규/확인/출고대기/출고완료/배송중/배송완료/취소/교환/반품 (SC-02, SC-05)"
    - "취소 탭은 ?status=cancelled OR ?claimType=cancel 분기, 취소 카운트 = (claims.claimType='cancel') ∪ (orders.status='cancelled') distinct order (SC-01)"
    - "getOrderStats가 단일 GROUP BY SQL로 9탭 카운트 + claimCancel/Exchange/Return + cancelTabCount(취소 탭 정확 distinct)을 반환한다 (SC-01, SC-05, anti-pattern 회피)"
    - "columns.tsx에서 CS 컬럼이 제거된다 (SC-03)"
    - "첫 데이터 컬럼(상태/액션)에 클레임 뱃지 + 문의 아이콘 + 미발송(holdReason) 인디케이터가 통합 렌더링된다 (SC-03, RESEARCH § Pitfall 3)"
    - "상품명 컬럼은 매핑된 displayName 우선 + 원본명 보조 표시 (tooltip 또는 작은 회색 글자) (SC-04)"
    - "주문 테이블에 배송구분 / 수집 배송비 / SaaS 배송비(원가) 3개 컬럼이 추가된다 (SC-06)"
    - "queries.ts getOrders가 productNameMappings LEFT JOIN으로 displayName을 가져오고 marketplaceId 매칭 포함 (RESEARCH § Pitfall 2)"
    - "queries.ts getOrders가 inquiry 존재 여부(hasInquiries: boolean)와 shipping fields(shippingType/Fee/products.shippingCost)를 반환한다"
    - "Plan 01의 page-header.test.tsx, columns.test.tsx, order-tabs.test.tsx, get-orders.test.ts, get-order-stats.test.ts 가 모두 GREEN"
  artifacts:
    - path: "src/app/(auth)/orders/order-tabs.tsx"
      provides: "9탭 통합 컴포넌트 (status × claimType 상호 배타)"
      contains: "신규.*확인.*출고대기"
      min_lines: 40
    - path: "src/lib/orders/queries.ts"
      provides: "getOrders displayName join + shipping fields + hasInquiries; getOrderStats 9탭 단일 GROUP BY"
      contains: "productNameMappings"
    - path: "src/lib/orders/types.ts"
      provides: "OrderStats 확장 (status별 + claim별 카운트), OrderListItem.items[].displayName, shippingType/Fee/Cost"
      contains: "OrderStats"
    - path: "src/app/(auth)/orders/page.tsx"
      provides: "엑셀 업로드 링크 제거 + ClaimsFilter→OrderTabs 교체 + stage 진입점 제거"
      contains: "OrderTabs"
    - path: "src/app/(auth)/orders/columns.tsx"
      provides: "CS 컬럼 제거 + 인디케이터 통합 + displayName + 배송 3컬럼"
      contains: "shippingType|배송구분"
  key_links:
    - from: "src/app/(auth)/orders/page.tsx"
      to: "src/app/(auth)/orders/order-tabs.tsx"
      via: "<OrderTabs counts={stats} /> (ClaimsFilter 대체)"
      pattern: "OrderTabs"
    - from: "src/lib/orders/queries.ts"
      to: "src/lib/db/schema.ts (productNameMappings)"
      via: "leftJoin productNameMappings ON userId + marketplaceId + marketplaceName"
      pattern: "leftJoin\\(\\s*productNameMappings"
    - from: "src/app/(auth)/orders/columns.tsx"
      to: "src/lib/orders/queries.ts"
      via: "OrderRow.items[].displayName, hasInquiries, shippingType/Fee/Cost rendered as columns"
      pattern: "displayName|hasInquiries|shippingType"
    - from: "src/app/(auth)/orders/order-tabs.tsx"
      to: "URL search params"
      via: "useQueryState('status') + useQueryState('claimType') — 상호 배타적 set"
      pattern: "useQueryState"
---

<objective>
주문관리 메인 화면을 phase 8 사용자 결정에 따라 전면 리팩터:
- 헤더에서 엑셀 업로드 진입점 제거
- 9탭(신규/확인/출고대기/출고완료/배송중/배송완료/취소/교환/반품) 단일 통합 컴포넌트
- stage-tabs / ClaimsFilter 폐기 (CONTEXT.md D-01)
- CS 컬럼 제거 → 첫 컬럼에 클레임/문의/미발송 인디케이터 통합
- 매핑 displayName 표시 (원본명 fallback)
- 배송구분 / 수집 배송비 / SaaS 배송비(원가) 3개 컬럼

Purpose: 사방넷 mental model에 맞는 9단계 탭 + 한눈에 보이는 클레임 상태 + 매핑 상품명 + 배송 차이.
Output: Plan 01의 5개 RED 테스트 stub이 모두 GREEN으로 전환됨.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-orders-ux-improvements/08-CONTEXT.md
@.planning/phases/08-orders-ux-improvements/08-RESEARCH.md
@.planning/phases/08-orders-ux-improvements/08-01-PLAN.md
@src/app/(auth)/orders/page.tsx
@src/app/(auth)/orders/columns.tsx
@src/app/(auth)/orders/claims-filter.tsx
@src/app/(auth)/orders/stage-tabs.tsx
@src/lib/orders/queries.ts
@src/lib/orders/types.ts

<interfaces>
<!-- Patterns the executor must reuse -->

From src/lib/orders/queries.ts (existing — extend, don't rewrite):
- getOrders(filters: OrderFilters): Promise<{ orders: OrderListItem[]; total: number }>
- getOrderStats(userId: string): Promise<OrderStats>  // currently has claim counts; ADD per-status counts
- buildOrderWhereClause(filters): SQL[]  // userId scope already applied (recent commit 916efc6 pattern)
- matchStage(): post-fetch — keep for backward compat but stage filter is no longer primary

From src/lib/orders/types.ts (existing OrderStats):
- Currently has e.g. cancelClaims/exchangeClaims/returnClaims (verify shape)
- EXTEND with: new, confirmed, preparing, shipped, delivering, delivered, cancelled, claimCancel, claimExchange, claimReturn

From src/app/(auth)/orders/page.tsx (current):
- Lines 127-134: <a href="/orders/import">엑셀 업로드</a> — REMOVE
- Lines 138-142: <ClaimsFilter counts={stats} /> — REPLACE with <OrderTabs counts={stats} />
- Lines 144-...: stage hub UI (prep/mapping/confirm sub-tabs) — REMOVE entirely (CONTEXT.md D-01: stage-tabs 폐기)
- Pass stats to OrderTabs; remove ClaimsFilter import

From src/app/(auth)/orders/columns.tsx (current):
- CS column at lines 388-420 — REMOVE
- statusActions / first column at lines 187-210 — INTEGRATE indicators here
- Product name display at line ~288 (`first.productName`) — switch to displayName fallback
- Use existing Badge from shadcn/ui + lucide-react icons (MessageCircle, AlertCircle, Lock)

From nuqs (already used in claims-filter.tsx):
- useQueryState('status', parseAsString)
- useQueryState('claimType', parseAsString)
- Setting one should clear the other (mutual exclusion in OrderTabs)

From src/lib/orders/inquiry-queries.ts (Plan 02):
- listInquiriesByOrderIds(orderIds): Promise<Inquiry[]> — use in queries.ts to populate hasInquiries
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: queries.ts + types.ts — 9탭 stats + displayName join + shipping fields + hasInquiries</name>
  <files>
    src/lib/orders/types.ts,
    src/lib/orders/queries.ts,
    tests/orders/get-order-stats.test.ts,
    tests/orders/get-orders.test.ts
  </files>
  <read_first>
    - src/lib/orders/queries.ts (full file — getOrders, getOrderStats, buildOrderWhereClause)
    - src/lib/orders/types.ts (OrderStats, OrderListItem, MappingStatus, OrderStage)
    - src/lib/db/schema.ts (orders, claims, productNameMappings, products, inquiries)
    - src/lib/orders/inquiry-queries.ts (Plan 02 — listInquiriesByOrderIds)
    - tests/orders/get-order-stats.test.ts, tests/orders/get-orders.test.ts (Plan 01 RED stubs)
    - .planning/phases/08-orders-ux-improvements/08-RESEARCH.md § Pattern 2 + Pattern 3 + Pitfall 1 + Pitfall 2
    - Recent commit 916efc6 — userId SQL scope filter pattern (search via `git log --grep=916efc6`)
  </read_first>
  <behavior>
    **OrderStats 확장 (B-3 — cancelTabCount included):**
    ```ts
    export interface OrderStats {
      // status counts
      new: number
      confirmed: number
      preparing: number
      shipped: number
      delivering: number
      delivered: number
      cancelled: number
      // claim counts (distinct order)
      claimCancel: number
      claimExchange: number
      claimReturn: number
      // 취소 탭 통합 카운트 — DISTINCT order_id WHERE status='cancelled' OR claim_type='cancel' (B-3)
      cancelTabCount: number
      // (total: optional)
      total?: number
    }
    ```

    **getOrderStats(userId) — must populate cancelTabCount (B-3):**
    - Single GROUP BY on orders.status WHERE user_id = userId → status counts (status NULL → ignored)
    - Single SELECT on claims joined with orders WHERE user_id = userId GROUP BY claim_type → claim counts (DISTINCT order_id)
    - Cancel claim count = `COUNT(DISTINCT claim.order_id) WHERE claim_type='cancel'`
    - **cancelTabCount** = `COUNT(DISTINCT o.id) FROM orders o LEFT JOIN claims c ON c.order_id = o.id AND c.claim_type='cancel' WHERE o.user_id = userId AND (o.status='cancelled' OR c.id IS NOT NULL)` — single SQL, distinct order. This MUST be in the returned OrderStats so OrderTabs (Task 2) can render the 취소 badge accurately.
    - Return aggregated OrderStats with ALL fields populated (default missing keys to 0, but cancelTabCount MUST be a real query result, not hardcoded 0)

    **취소 탭 필터 (getOrders):**
    - 새로운 filter shape: `{ ...existing, cancelTab?: boolean }`
    - When `cancelTab=true`: WHERE `(orders.status='cancelled' OR EXISTS (SELECT 1 FROM claims c WHERE c.order_id = orders.id AND c.claim_type='cancel'))`
    - 또는 단순화: `claimType='cancel'` filter는 그대로 유지 + `status='cancelled'` filter 단독 사용 가능 — UI(OrderTabs)가 둘 중 하나를 set, 그러나 "취소 탭 = OR" 로 정확하려면 cancelTab 별도 필요
    - DECISION: cancelTab boolean 추가 (page.tsx에서 ?cancel=1 → filter.cancelTab = true)

    **displayName join:**
    - getOrders 내부의 items 조회 SQL에 `LEFT JOIN productNameMappings ON (productNameMappings.userId = userId AND productNameMappings.marketplaceId = orders.marketplaceId AND productNameMappings.marketplaceName = orderItems.productName)`
    - select: `displayName: productNameMappings.displayName` (nullable)
    - OrderListItem.items[].displayName: string | null

    **shipping fields:**
    - Include in main getOrders select: `shippingType: orders.shippingType, shippingFee: orders.shippingFee`
    - For each order item, also include products.shippingCost via LEFT JOIN products ON internalSku ↔ orderItems.sku (existing join probably exists; if not, add LEFT JOIN)

    **hasInquiries:**
    - After fetching order rows, call `listInquiriesByOrderIds(orderIds)` once
    - Build Set of orderIds with at least one inquiry
    - Map onto OrderListItem.hasInquiries: boolean
  </behavior>
  <action>
    **Step 1 (RED — replace Plan 01 stubs with concrete asserts):**

    tests/orders/get-order-stats.test.ts — replace placeholder with a vi.mock'd test that ACTUALLY CALLS getOrderStats('test-user-id') (B-5 — count correctness must be automated). Minimum:
    ```ts
    import { describe, it, expect, vi, beforeEach } from 'vitest'

    // Mock the drizzle db — return canned aggregation rows per query call.
    // Adjust the chain (.select().from().where().groupBy()) to match the real query builder used in queries.ts.
    const statusRows = [
      { status: 'new', value: 3 },
      { status: 'confirmed', value: 2 },
      { status: 'preparing', value: 5 },
      { status: 'shipped', value: 1 },
      { status: 'delivering', value: 4 },
      { status: 'delivered', value: 7 },
      { status: 'cancelled', value: 6 },
    ]
    const claimRows = [
      { claimType: 'cancel', value: 2 },
      { claimType: 'exchange', value: 1 },
      { claimType: 'return', value: 3 },
    ]
    // cancelTabCount = DISTINCT order WHERE status='cancelled' OR claim cancel — canned to 8
    const cancelTabRow = [{ value: 8 }]

    let callIndex = 0
    vi.mock('@/lib/db', () => {
      const builder = (rows: any[]) => {
        const chain: any = {
          from: () => chain,
          leftJoin: () => chain,
          innerJoin: () => chain,
          where: () => chain,
          groupBy: () => Promise.resolve(rows),
          limit: () => Promise.resolve(rows),
        }
        // Make the chain await-able to its rows when no .groupBy/.limit is called
        chain.then = (resolve: any) => Promise.resolve(rows).then(resolve)
        return chain
      }
      return {
        db: {
          select: () => {
            // Cycle through the three queries in order: status, claims, cancelTab
            const queues = [statusRows, claimRows, cancelTabRow]
            const rows = queues[Math.min(callIndex, queues.length - 1)]
            callIndex++
            return builder(rows)
          },
        },
      }
    })

    beforeEach(() => { callIndex = 0 })

    describe('getOrderStats — counts (B-5 automated coverage)', () => {
      it('returns OrderStats with 7 status counts + 3 claim counts + cancelTabCount', async () => {
        const { getOrderStats } = await import('@/lib/orders/queries')
        const stats = await getOrderStats('test-user-id')

        // Status counts mapped from groupBy(orders.status)
        expect(stats.new).toBe(3)
        expect(stats.confirmed).toBe(2)
        expect(stats.preparing).toBe(5)
        expect(stats.shipped).toBe(1)
        expect(stats.delivering).toBe(4)
        expect(stats.delivered).toBe(7)
        expect(stats.cancelled).toBe(6)

        // Claim counts mapped from groupBy(claims.claim_type)
        expect(stats.claimCancel).toBe(2)
        expect(stats.claimExchange).toBe(1)
        expect(stats.claimReturn).toBe(3)

        // cancelTabCount from the dedicated DISTINCT query (B-3)
        expect(stats.cancelTabCount).toBe(8)
      })

      it('exposes the full OrderStats shape (type-level guard)', () => {
        // This compile-time assertion guards against accidentally dropping a key.
        const sample = {
          new: 0, confirmed: 0, preparing: 0, shipped: 0,
          delivering: 0, delivered: 0, cancelled: 0,
          claimCancel: 0, claimExchange: 0, claimReturn: 0,
          cancelTabCount: 0,
        } as const
        expect(Object.keys(sample).sort()).toEqual([
          'cancelTabCount','cancelled','claimCancel','claimExchange','claimReturn',
          'confirmed','delivered','delivering','new','preparing','shipped',
        ])
      })
    })
    ```

    NOTE on the mock: the exact chain (.from/.where/.groupBy) MUST match queries.ts. If queries.ts uses a different builder shape, adapt the mock accordingly (the goal is that getOrderStats actually executes through the mocked db.select() three times — once per logical query — and returns a real OrderStats). If mocking the chain proves intractable, fall back to splitting getOrderStats internals into 3 small exported helpers (e.g., `countByStatus(userId)`, `countClaimsByType(userId)`, `countCancelTab(userId)`) and unit-test each helper with a tighter mock — DO NOT DOWNGRADE to type-only assertion.

    tests/orders/get-orders.test.ts — replace placeholder:
    ```ts
    import { describe, it, expect } from 'vitest'
    import type { OrderListItem } from '@/lib/orders/types'

    describe('OrderListItem shape — phase 8', () => {
      it('items[].displayName field exists (nullable)', () => {
        const item: OrderListItem['items'][number] = {
          productName: '원본명',
          displayName: null,
          optionText: null,
          quantity: 1,
          sku: null,
        } as OrderListItem['items'][number]
        expect(item.displayName).toBeNull()
      })
      it('OrderListItem exposes shippingType, shippingFee, hasInquiries', () => {
        const o = {
          shippingType: 'prepaid', shippingFee: 3000, hasInquiries: true,
        } as Pick<OrderListItem, 'shippingType' | 'shippingFee' | 'hasInquiries'>
        expect(o.shippingType).toBe('prepaid')
        expect(o.shippingFee).toBe(3000)
        expect(o.hasInquiries).toBe(true)
      })
    })
    ```

    **Step 2 (types):** Update src/lib/orders/types.ts:
    - Replace OrderStats with the extended shape (above)
    - Add to OrderListItem (or whatever the row type is named): `shippingType: string | null`, `shippingFee: string | null` (numeric returns string from postgres-js), `hasInquiries: boolean`, and on items array `displayName: string | null`, `shippingCost: string | null` (per-item products.shipping_cost)
    - Add to OrderFilters: `cancelTab?: boolean`

    **Step 3 (queries — getOrderStats — B-3 cancelTabCount required):** Rewrite to use focused queries (RESEARCH § Pattern 2):
    - Status counts: `db.select({ status: orders.status, value: count() }).from(orders).where(eq(orders.userId, userId)).groupBy(orders.status)` → reduce into status keys
    - Claim counts: `db.select({ claimType: claims.claimType, value: countDistinct(claims.orderId) }).from(claims).innerJoin(orders, ...).where(eq(orders.userId, userId)).groupBy(claims.claimType)` → reduce
    - **cancelTabCount (B-3 — REQUIRED):** `db.select({ value: countDistinct(orders.id) }).from(orders).leftJoin(claims, and(eq(claims.orderId, orders.id), eq(claims.claimType, 'cancel'))).where(and(eq(orders.userId, userId), or(eq(orders.status, 'cancelled'), isNotNull(claims.id))))` — single SQL, returns one row with the distinct count
    - Compose OrderStats; default missing keys to 0; `cancelTabCount` MUST come from the dedicated query (not hardcoded)

    **Step 4 (queries — getOrders):**
    - In items subquery / join, add `leftJoin(productNameMappings, and(eq(productNameMappings.userId, userId), eq(productNameMappings.marketplaceId, orders.marketplaceId), eq(productNameMappings.marketplaceName, orderItems.productName)))`
    - In items select, add `displayName: productNameMappings.displayName`
    - In items, also leftJoin `products` on `eq(products.internalSku, orderItems.sku)` (or appropriate key) and select `shippingCost: products.shippingCost`
    - Main select: include `orders.shippingType, orders.shippingFee`
    - After fetching orders, query `listInquiriesByOrderIds(orderIds)` — set `hasInquiries: inquirySet.has(orderId)` on each row
    - Apply cancelTab filter when set: `or(eq(orders.status, 'cancelled'), exists(db.select({ x: sql`1` }).from(claims).where(and(eq(claims.orderId, orders.id), eq(claims.claimType, 'cancel')))))`

    **Step 5 (run tests):** `npx vitest run tests/orders/get-order-stats.test.ts tests/orders/get-orders.test.ts` → both GREEN.

    DO NOT remove existing matchStage / OrderStage logic (kept for non-tab usage). DO NOT touch existing claim filtering by claimType — it stays for 교환/반품 탭.
  </action>
  <verify>
    <automated>npx vitest run tests/orders/get-order-stats.test.ts tests/orders/get-orders.test.ts --reporter=dot && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "claimCancel" src/lib/orders/types.ts` matches in OrderStats
    - `grep -n "claimExchange" src/lib/orders/types.ts` matches
    - `grep -n "claimReturn" src/lib/orders/types.ts` matches
    - `grep -n "preparing" src/lib/orders/types.ts` matches in OrderStats
    - `grep -n "displayName" src/lib/orders/types.ts` matches (item shape)
    - `grep -n "hasInquiries" src/lib/orders/types.ts` matches
    - `grep -n "leftJoin(productNameMappings" src/lib/orders/queries.ts` matches
    - `grep -n "marketplaceId.*productNameMappings\\|productNameMappings.*marketplaceId" src/lib/orders/queries.ts` matches (Pitfall 2 — marketplaceId must be in join)
    - `grep -n "listInquiriesByOrderIds" src/lib/orders/queries.ts` matches
    - `grep -nE "groupBy\(orders\.status\)" src/lib/orders/queries.ts` matches in getOrderStats (B-5 — proves GROUP BY exists)
    - `grep -n "cancelTab" src/lib/orders/queries.ts` matches
    - **B-3 cancelTabCount on type:** `grep -n "cancelTabCount" src/lib/orders/types.ts` matches in OrderStats interface
    - **B-3 cancelTabCount in query:** `grep -n "cancelTabCount" src/lib/orders/queries.ts` matches AND `grep -nE "countDistinct\(orders\.id\)|COUNT\(DISTINCT" src/lib/orders/queries.ts` matches (proves the DISTINCT query exists)
    - **B-5 stronger test coverage:** tests/orders/get-order-stats.test.ts MUST call getOrderStats — `grep -n "getOrderStats(" tests/orders/get-order-stats.test.ts` returns ≥ 1 match (not just type literal)
    - vitest GREEN, tsc 0
  </acceptance_criteria>
  <done>queries.ts + types.ts에서 9탭 stats, displayName join (marketplaceId 매칭 포함), shipping fields, hasInquiries, cancelTab 모두 동작. 2개 RED stub GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OrderTabs (9탭 통합 컴포넌트) + page.tsx 헤더/탭 리팩터 (엑셀 업로드 제거, stage-tabs/ClaimsFilter 폐기)</name>
  <files>
    src/app/(auth)/orders/order-tabs.tsx,
    src/app/(auth)/orders/page.tsx,
    src/app/(auth)/orders/stage-tabs.tsx,
    src/app/(auth)/orders/claims-filter.tsx,
    tests/orders/order-tabs.test.tsx,
    tests/orders/page-header.test.tsx
  </files>
  <read_first>
    - src/app/(auth)/orders/page.tsx (entire file — header section + tab section)
    - src/app/(auth)/orders/claims-filter.tsx (existing nuqs pattern to reuse)
    - src/app/(auth)/orders/stage-tabs.tsx (will be deleted/emptied)
    - src/lib/orders/types.ts (OrderStats from Task 1)
    - tests/orders/order-tabs.test.tsx, tests/orders/page-header.test.tsx (Plan 01 RED stubs)
    - .planning/phases/08-orders-ux-improvements/08-RESEARCH.md § Code Example 1 (9탭 component sketch)
    - .planning/phases/08-orders-ux-improvements/08-CONTEXT.md (D-01 stage-tabs 폐기)
  </read_first>
  <behavior>
    **OrderTabs 컴포넌트 (client component):**
    - Renders 10 buttons: 전체 / 신규 / 확인 / 출고대기 / 출고완료 / 배송중 / 배송완료 / 취소 / 교환 / 반품
    - Active state from URL (`status` or `claimType` or `cancel` param)
    - Click handlers (mutual exclusion):
      - 전체 → clear all
      - 신규 → setStatus('new'), clear claimType + cancel
      - 확인 → setStatus('confirmed'), clear ...
      - 출고대기 → setStatus('preparing')
      - 출고완료 → setStatus('shipped')
      - 배송중 → setStatus('delivering')
      - 배송완료 → setStatus('delivered')
      - 취소 → set ?cancel=1, clear status + claimType (cancelTab covers OR)
      - 교환 → setClaimType('exchange'), clear status + cancel
      - 반품 → setClaimType('return'), clear status + cancel
    - Each tab shows count badge from OrderStats:
      - 신규 → counts.new, 확인 → counts.confirmed, ..., 배송완료 → counts.delivered
      - 취소 → counts.claimCancel + counts.cancelled (distinct — note: simple sum approximates; SQL DISTINCT covered server-side via cancelTab filter — display sum is acceptable rough indicator OR pass distinct count from server)
        DECISION: extend OrderStats with `cancelled` (status) and `claimCancel` (claim) — display the server-provided `cancelled OR claimCancel distinct` count as a single value. Add a derived `cancelTabCount: number` to OrderStats so server returns it precomputed.
        ACTION: in Task 1 queries.ts ALSO compute `cancelTabCount = SELECT COUNT(DISTINCT o.id) FROM orders o LEFT JOIN claims c ON c.order_id = o.id AND c.claim_type='cancel' WHERE o.user_id=? AND (o.status='cancelled' OR c.id IS NOT NULL)`. Use this for 취소 탭 badge.
      - 교환 → counts.claimExchange
      - 반품 → counts.claimReturn

    **page.tsx changes:**
    - Remove the entire `<a href="/orders/import">엑셀 업로드</a>` block (lines ~127-134)
    - Remove `<ClaimsFilter counts={stats} />` block — replace with `<OrderTabs counts={stats} />`
    - Remove the entire stage hub UI (lines 144+ — stage prep/mapping/confirm sub-tab block)
    - Remove `STAGE_LABELS` / `stage` derivation if no longer used in title (or keep title only when stage param explicitly set externally — confirm by grep)
    - Add `cancel` to searchParamsCache: `cancel: parseAsBoolean`
    - Pass `cancelTab: params.cancel ?? undefined` into getOrders filters
    - Add `OrderTabs` import

    **Empty out stage-tabs.tsx + claims-filter.tsx:**
    - Replace each file with a simple deprecation export to avoid breaking other imports:
      ```tsx
      // Deprecated as of Phase 8 — replaced by OrderTabs (CONTEXT.md D-01).
      // File kept as placeholder until all imports are migrated.
      export {}
      ```
    - Verify no other file imports them via `grep -r "from.*claims-filter\\|from.*stage-tabs" src` — if matches found OUTSIDE page.tsx, leave the component as a no-op default export instead of empty.
  </behavior>
  <action>
    **Step 1 (RED test refinement):** Update tests/orders/order-tabs.test.tsx — remove `it.todo` lines, ensure it asserts:
    - 9 tab labels render (신규/확인/출고대기/출고완료/배송중/배송완료/취소/교환/반품)
    - 카운트 뱃지 노출: `expect(screen.getByText('100')).toBeInTheDocument()` (전체) etc — adapt to actual implementation
    - 취소 탭 클릭 시 URL ?cancel=1 set (use NuqsTestingAdapter onUrlUpdate hook)
    Run → RED.

    Update tests/orders/page-header.test.tsx — already covers the file-content assertion (no change needed; it remains RED until page.tsx is edited).

    **Step 2 (OrderTabs component):** Create src/app/(auth)/orders/order-tabs.tsx (client component) per behavior spec. Use shadcn/ui Badge for counts. Follow claims-filter.tsx structural pattern (nuqs + Tailwind classes).

    **Step 3 (page.tsx edits):**
    - Delete header link block (lines ~127-134)
    - Delete `<ClaimsFilter ... />` Suspense block (lines ~138-142) — replace with `<Suspense><OrderTabs counts={stats} /></Suspense>`
    - Delete entire stage hub block (line 144+ until end of stage rendering)
    - Update `searchParamsCache` definition: add `cancel: parseAsBoolean`
    - Forward `cancelTab: params.cancel ?? undefined` to getOrders
    - Update imports (remove ClaimsFilter, add OrderTabs, drop unused STAGE_LABELS if applicable)

    **Step 4 (deprecate stage-tabs + claims-filter):** Replace contents per behavior spec. Run `grep -r "from.*claims-filter\|from.*stage-tabs" src` — for each remaining importer, either remove the import or keep the file as a no-op stub.

    **Step 5:** Run `npx vitest run tests/orders/order-tabs.test.tsx tests/orders/page-header.test.tsx` → GREEN.
  </action>
  <verify>
    <automated>npx vitest run tests/orders/order-tabs.test.tsx tests/orders/page-header.test.tsx --reporter=dot && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/app/(auth)/orders/order-tabs.tsx`
    - `grep -n "use client" src/app/(auth)/orders/order-tabs.tsx` matches
    - `grep -E "신규|확인|출고대기|출고완료|배송중|배송완료|취소|교환|반품" src/app/(auth)/orders/order-tabs.tsx` matches all 9 labels
    - `grep -n "useQueryState" src/app/(auth)/orders/order-tabs.tsx` matches
    - page.tsx: `grep -c "엑셀 업로드" src/app/(auth)/orders/page.tsx` returns 0
    - page.tsx: `grep -c "/orders/import" src/app/(auth)/orders/page.tsx` returns 0
    - page.tsx: `grep -c "ClaimsFilter" src/app/(auth)/orders/page.tsx` returns 0
    - page.tsx: `grep -n "OrderTabs" src/app/(auth)/orders/page.tsx` matches
    - page.tsx: `grep -n "cancel: parseAsBoolean" src/app/(auth)/orders/page.tsx` matches
    - **W-3 filter bar preserved:** `grep -nE "marketplaceId|dateRange|searchQuery|search " src/app/(auth)/orders/page.tsx` returns ≥ 1 match (CONTEXT.md decision: marketplace/날짜/검색 필터바는 유지). If filter bar is in a separate component, ensure that component import is still present in page.tsx — `grep -nE "FilterBar|MarketplaceFilter|DateFilter|SearchFilter" src/app/(auth)/orders/page.tsx` returns ≥ 1 match.
    - tests/orders/order-tabs.test.tsx + page-header.test.tsx GREEN
  </acceptance_criteria>
  <done>OrderTabs 9탭 컴포넌트가 nuqs 기반 mutual-exclusion으로 동작. page.tsx 헤더에서 엑셀 업로드 제거, ClaimsFilter→OrderTabs 교체, stage 진입점 제거. 2개 RED stub GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: columns.tsx — CS 컬럼 제거, 인디케이터 통합, displayName, 배송 3컬럼</name>
  <files>
    src/app/(auth)/orders/columns.tsx,
    tests/orders/columns.test.tsx
  </files>
  <read_first>
    - src/app/(auth)/orders/columns.tsx (entire file — locate CS column, status/first column, product name cell)
    - src/lib/orders/types.ts (OrderListItem from Task 1 — items[].displayName, hasInquiries, shippingType/Fee, items[].shippingCost)
    - tests/orders/columns.test.tsx (Plan 01 RED stub — file-content assertions)
    - .planning/phases/08-orders-ux-improvements/08-RESEARCH.md § Pitfall 3 (holdReason 보존)
    - src/components/ui/badge (existing shadcn Badge)
    - lucide-react: MessageCircle, AlertCircle, Lock 아이콘
  </read_first>
  <behavior>
    **Remove:**
    - The entire CS column object (header: 'CS', cell rendering claims+holdReason in a stacked layout) at lines ~388-420

    **Modify first data column (status/액션 — lines ~187-210) to integrate indicators:**
    - Inline cluster of small badges/icons at the start of the cell:
      - If `row.original.claimType` (cancel/exchange/return) → colored Badge ('취소' red / '교환' amber / '반품' blue)
      - If `row.original.hasInquiries` → MessageCircle icon (small, blue) with title="문의 있음"
      - If `row.original.isHeld` → Lock icon (small, red) with title=`row.original.holdReason ?? '미발송'`
    - Then render the existing status actions below/inline

    **Product name cell (line ~288):**
    - Currently shows `first.productName` (raw)
    - Change to: `first.displayName ?? first.productName` as primary text
    - When `first.displayName && first.displayName !== first.productName` → render `first.productName` as small grey subtext (e.g., `<span className="text-xs text-muted-foreground">{first.productName}</span>`)
    - Truncate via Tailwind `truncate` if too long; preserve `title={first.displayName ?? first.productName}` for tooltip

    **Add 3 new columns (insert AFTER existing payment/amount columns, BEFORE actions):**
    1. `header: '배송구분'`, accessor: row → row.shippingType, cell: small Badge with Korean label:
       - 'prepaid' → '선결제'
       - 'cod' → '착불'
       - 'free' → '무료'
       - 'unknown' / null → '—'
    2. `header: '수집 배송비'`, accessor: row → row.shippingFee, cell: `Number(value).toLocaleString('ko-KR') + '원'` or '—' if null
    3. `header: 'SaaS 배송비(원가)'`, accessor: row.items[0].shippingCost (use first item's product mapping cost; if multiple items, sum or show first — prefer SUM since order may have N items): `cell: items.reduce((s, i) => s + Number(i.shippingCost ?? 0), 0).toLocaleString('ko-KR') + '원'` or '—' if all NULL

    DO NOT change any other column. DO NOT remove holdReason — it must appear in the integrated indicator (RESEARCH § Pitfall 3).
  </behavior>
  <action>
    **Step 1 (RED — concrete asserts):** Replace tests/orders/columns.test.tsx with file-content assertions:
    ```ts
    import { describe, it, expect } from 'vitest'
    import { readFileSync } from 'node:fs'
    const src = readFileSync('src/app/(auth)/orders/columns.tsx', 'utf8')

    describe('columns.tsx — phase 8', () => {
      it('CS 헤더가 제거되었다', () => {
        expect(src).not.toMatch(/header:\s*['"`]CS['"`]/)
      })
      it('배송구분 컬럼이 추가되었다', () => {
        expect(src).toMatch(/header:\s*['"`]배송구분['"`]/)
      })
      it('수집 배송비 컬럼이 추가되었다', () => {
        expect(src).toMatch(/header:\s*['"`]수집 배송비['"`]/)
      })
      it('SaaS 배송비(원가) 컬럼이 추가되었다', () => {
        expect(src).toMatch(/header:\s*['"`]SaaS 배송비\(원가\)['"`]/)
      })
      it('displayName fallback 패턴 사용 (displayName ?? productName)', () => {
        expect(src).toMatch(/displayName\s*\?\?\s*[a-zA-Z.]*productName/)
      })
      it('hasInquiries 인디케이터 렌더링 (MessageCircle 또는 문의 텍스트)', () => {
        expect(src).toMatch(/hasInquiries/)
        expect(src).toMatch(/MessageCircle|문의/)
      })
      it('holdReason 인디케이터가 보존된다 (Pitfall 3)', () => {
        expect(src).toMatch(/holdReason/)
      })
      it('claimType 뱃지 (취소|교환|반품 라벨)', () => {
        expect(src).toMatch(/취소|교환|반품/)
      })
    })
    ```
    Run → RED.

    **Step 2 (GREEN edits):**
    1. Locate and DELETE the CS column object block (search for `header: 'CS'`)
    2. Modify the first data column cell to render the indicator cluster (Badges + lucide icons) before the existing content
    3. Modify the product name cell to use `first.displayName ?? first.productName` + grey subtext for the original
    4. Insert 3 new column objects (배송구분, 수집 배송비, SaaS 배송비(원가)) at the end (or before actions column)
    5. Add lucide-react imports if missing: `import { MessageCircle, Lock } from 'lucide-react'`

    Run vitest + tsc → GREEN.
  </action>
  <verify>
    <automated>npx vitest run tests/orders/columns.test.tsx --reporter=dot && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "header: 'CS'" src/app/(auth)/orders/columns.tsx` returns 0
    - `grep -c "header: '배송구분'" src/app/(auth)/orders/columns.tsx` returns 1
    - `grep -c "header: '수집 배송비'" src/app/(auth)/orders/columns.tsx` returns 1
    - `grep -c "header: 'SaaS 배송비(원가)'" src/app/(auth)/orders/columns.tsx` returns 1
    - `grep -E "displayName\s*\?\?\s*\w*productName" src/app/(auth)/orders/columns.tsx` matches
    - `grep -n "hasInquiries" src/app/(auth)/orders/columns.tsx` matches
    - `grep -n "holdReason" src/app/(auth)/orders/columns.tsx` matches (Pitfall 3 — preserved)
    - `grep -n "MessageCircle\|Lock" src/app/(auth)/orders/columns.tsx` matches (lucide icons)
    - tests/orders/columns.test.tsx 8개 assertion 모두 GREEN
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>CS 컬럼 제거 + 첫 컬럼 인디케이터 통합 + displayName fallback + 배송 3컬럼 + holdReason 보존. columns.test.tsx GREEN. Production build 통과.</done>
</task>

</tasks>

<verification>
- queries.ts: 9탭 stats (단일 GROUP BY), displayName join (marketplaceId 포함), shipping fields, hasInquiries, cancelTab filter
- types.ts: OrderStats 확장, OrderListItem.items[].displayName, hasInquiries, shippingType/Fee/Cost
- order-tabs.tsx: 9탭 + 카운트 뱃지 + nuqs 상호 배타적 set
- page.tsx: 엑셀 업로드 제거, OrderTabs 적용, stage 진입점 제거
- columns.tsx: CS 제거, 인디케이터 통합 (claim + inquiry + hold), displayName fallback, 배송 3컬럼
- All Plan 01 RED stubs GREEN: page-header, columns, order-tabs, get-orders, get-order-stats
- `npx tsc --noEmit && npx vitest run && npm run build` 통과
</verification>

<success_criteria>
- SC-01 ✅ 취소 탭 정확 카운트 + 클릭 시 status='cancelled' OR claimType='cancel' 필터
- SC-02 ✅ 엑셀 업로드 제거 + 9탭 단일 컴포넌트
- SC-03 ✅ CS 컬럼 제거 + 첫 컬럼 클레임/문의/미발송 인디케이터
- SC-04 ✅ displayName 우선 + 원본명 보조
- SC-05 ✅ 9탭 카운트 SQL 레벨 정확 (GROUP BY)
- SC-06 (UI 부분) ✅ 배송구분 + 수집 배송비 + SaaS 배송비(원가) 3컬럼 노출
</success_criteria>

<output>
After completion, create `.planning/phases/08-orders-ux-improvements/08-03-SUMMARY.md` with:
- queries.ts API 변경 (getOrderStats 9탭 single GROUP BY, getOrders 신규 join/필드)
- types.ts 변경 (OrderStats, OrderListItem)
- OrderTabs 컴포넌트 location + 9탭 ↔ URL param 매핑 표
- page.tsx 제거된 라인 (엑셀 업로드 / ClaimsFilter / stage 진입점)
- columns.tsx 변경 (제거된 CS 컬럼 + 추가된 3 배송 컬럼 + 인디케이터 위치)
- 다음 plan(04 — inventory)이 reference 할 products.shippingCost 입력 acceptance
</output>
