---
phase: 260424-oz0-localstorage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/tapeoff/funtastic-saas/src/components/layout/app-shell.tsx
  - /Users/tapeoff/funtastic-saas/src/components/layout/sidebar.tsx
autonomous: false
requirements:
  - OZ0-01
  - OZ0-02
  - OZ0-03

must_haves:
  truths:
    - "사이드바 펼친 상태에서 상단 << 버튼을 클릭하면 사이드바가 완전히 숨겨진다"
    - "사이드바 접힌 상태에서는 >> 버튼이 노출되어 다시 펼칠 수 있다"
    - "접기 상태는 localStorage에 저장되어 새로고침 후에도 유지된다"
    - "SSR 하이드레이션 경고가 발생하지 않는다 (초기 렌더는 펼침 상태로 일치)"
    - "사이드바가 숨겨지면 메인 컨텐츠가 전체 화면 너비를 차지한다"
  artifacts:
    - path: "/Users/tapeoff/funtastic-saas/src/components/layout/app-shell.tsx"
      provides: "클라이언트 컴포넌트로 전환되어 collapse 상태 관리 + Sidebar 조건부 렌더 + 펼치기 버튼"
      contains: "'use client'"
    - path: "/Users/tapeoff/funtastic-saas/src/components/layout/sidebar.tsx"
      provides: "상단 << 접기 버튼 추가 + onCollapse prop 수신"
      contains: "onCollapse"
  key_links:
    - from: "AppShell (client component)"
      to: "localStorage"
      via: "useEffect로 mount 후 읽기/쓰기"
      pattern: "funtastic-sidebar-collapsed"
    - from: "AppShell"
      to: "Sidebar"
      via: "onCollapse prop 전달"
      pattern: "onCollapse={.*}"
    - from: "AppShell"
      to: "접힌 상태 >> 펼치기 버튼"
      via: "!collapsed ? <Sidebar /> : <ExpandButton />"
      pattern: "collapsed"
---

<objective>
funtastic-saas의 사이드바에 접기/펼치기 토글 기능을 추가한다.
- 펼친 상태: 사이드바 상단에 << 버튼 노출, 클릭 시 완전히 숨김
- 접힌 상태: 메인 영역 좌상단에 >> 버튼 노출, 클릭 시 다시 펼침
- localStorage("funtastic-sidebar-collapsed")에 상태 저장, 새로고침 후에도 유지
- SSR 하이드레이션 mismatch 방지: 초기 렌더는 항상 "펼침"으로 시작, mount 후 useEffect에서 localStorage 읽어 반영

Purpose: 좁은 화면/집중 작업 시 전체 화면 폭 확보, 사용자 환경 설정 유지
Output: app-shell.tsx (client 전환 + 토글 버튼), sidebar.tsx (상단 collapse 버튼 추가)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tapeoff/funtastic-saas/.planning/STATE.md
@/Users/tapeoff/funtastic-saas/CLAUDE.md
@/Users/tapeoff/funtastic-saas/src/components/layout/sidebar.tsx
@/Users/tapeoff/funtastic-saas/src/components/layout/app-shell.tsx
@/Users/tapeoff/funtastic-saas/src/app/(auth)/layout.tsx

<interfaces>
<!-- 기존 구조 요약 (executor가 코드베이스 탐색 없이 작업 가능하도록) -->

### AppShell (현재 server component)
```tsx
// src/components/layout/app-shell.tsx
import { Sidebar } from './sidebar'

interface AppShellProps { children: React.ReactNode }

export function AppShell({ children }: AppShellProps) {
  return (
    <div className="flex h-screen">
      <Sidebar />
      <main className="flex-1 overflow-auto bg-gray-50 p-6">{children}</main>
    </div>
  )
}
```

### Sidebar (이미 client component, 'use client')
```tsx
// src/components/layout/sidebar.tsx
export function Sidebar() {
  // ... navSections 렌더, 로그아웃 버튼
  return (
    <aside className="flex h-full w-48 flex-col bg-gray-900 text-white">
      <div className="flex h-10 items-center justify-between border-b border-gray-800 px-3">
        <span className="text-sm font-bold">Funtastic</span>
        <span className="text-[9px] text-gray-500">v2.1</span>
      </div>
      <nav>...</nav>
      <div>...로그아웃...</div>
    </aside>
  )
}
```

### Auth layout (AppShell 사용처 — 변경 불필요)
```tsx
// src/app/(auth)/layout.tsx
return <AppShell>{children}</AppShell>
```

### 사용 가능한 아이콘 (lucide-react, 이미 설치됨)
- `ChevronsLeft` (<<)  펼친 상태에서 사이드바 상단에 배치
- `ChevronsRight` (>>) 접힌 상태에서 메인 영역 좌상단에 배치
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppShell을 client component로 전환하여 collapse 상태 관리 추가</name>
  <files>/Users/tapeoff/funtastic-saas/src/components/layout/app-shell.tsx</files>
  <action>
  AppShell을 client component로 변환하고 사이드바 접기/펼치기 상태를 관리하는 컨테이너로 만든다.

  구현 상세:
  1. 파일 최상단에 `'use client'` 디렉티브 추가.
  2. React `useState`, `useEffect`, `lucide-react`에서 `ChevronsRight` import.
  3. `Sidebar`는 기존 import 유지.
  4. 컴포넌트 내부에 `const [collapsed, setCollapsed] = useState(false)` 선언 (초기값 false — SSR과 일치시켜 하이드레이션 mismatch 방지).
  5. `useEffect(() => { ... }, [])`에서 `localStorage.getItem('funtastic-sidebar-collapsed')`을 읽어 `'true'`면 `setCollapsed(true)` 호출. try/catch로 localStorage 접근 오류(프라이빗 모드 등) 방어.
  6. `toggleCollapsed` 함수 정의: `setCollapsed`로 상태 토글 + `localStorage.setItem('funtastic-sidebar-collapsed', String(next))`. try/catch로 감싸기.
  7. JSX 구조:
     - 최상위 `<div className="flex h-screen">` 유지
     - `collapsed`가 false이면 `<Sidebar onCollapse={toggleCollapsed} />` 렌더
     - `collapsed`가 true이면 사이드바 자리에 펼치기 버튼 렌더 (메인 영역 좌상단 고정):
       ```tsx
       <button
         type="button"
         onClick={toggleCollapsed}
         aria-label="사이드바 펼치기"
         className="fixed left-2 top-2 z-50 flex h-7 w-7 items-center justify-center rounded bg-gray-900 text-white shadow hover:bg-gray-800"
       >
         <ChevronsRight className="h-4 w-4" />
       </button>
       ```
     - `<main className="flex-1 overflow-auto bg-gray-50 p-6">{children}</main>` 유지

  주의:
  - localStorage는 반드시 useEffect 내부에서만 접근 (SSR 시 undefined 방지).
  - 초기 useState 값은 서버 렌더와 동일하게 false로 고정 (하이드레이션 mismatch 방지).
  - Auth layout(`src/app/(auth)/layout.tsx`)은 변경하지 않는다 — server component에서 client component를 자식으로 렌더하는 것은 정상 패턴.
  </action>
  <behavior>
    - 초기 마운트 시 collapsed=false, Sidebar가 보임
    - useEffect에서 localStorage 값이 'true'이면 collapsed=true로 설정되어 Sidebar 숨기고 >> 버튼 노출
    - >> 버튼 클릭 시 collapsed=false로 전환 + localStorage에 'false' 저장
    - localStorage 접근 실패 시 기본 상태(펼침)로 동작, 에러 throw 안 함
  </behavior>
  <verify>
    <automated>cd /Users/tapeoff/funtastic-saas && npx tsc --noEmit 2>&1 | grep -E "app-shell|sidebar" || echo "no type errors in target files"</automated>
  </verify>
  <done>
    - app-shell.tsx 최상단에 'use client' 존재
    - useState + useEffect 기반 collapse 상태 관리 구현
    - collapsed=true일 때 Sidebar 렌더 안 하고 ChevronsRight 버튼 노출
    - collapsed=false일 때 Sidebar에 onCollapse prop 전달
    - localStorage 읽기는 useEffect 내부, 쓰기는 toggle 함수 내부에서만 실행
    - TypeScript 타입 에러 없음
  </done>
</task>

<task type="auto">
  <name>Task 2: Sidebar에 상단 << 접기 버튼 추가</name>
  <files>/Users/tapeoff/funtastic-saas/src/components/layout/sidebar.tsx</files>
  <action>
  Sidebar에 접기 버튼을 추가하고 AppShell로부터 onCollapse 콜백을 받는다.

  구현 상세:
  1. `lucide-react` import에 `ChevronsLeft` 추가.
  2. `Sidebar` 함수 시그니처 변경:
     ```tsx
     interface SidebarProps {
       onCollapse?: () => void
     }
     export function Sidebar({ onCollapse }: SidebarProps) {
     ```
  3. 기존 Logo 영역(<div className="flex h-10 items-center justify-between border-b border-gray-800 px-3">) 내부에 접기 버튼 추가.
     - 현재 구조: `<span>Funtastic</span><span>v2.1</span>`
     - 변경 후 구조:
       ```tsx
       <div className="flex h-10 items-center justify-between border-b border-gray-800 px-3">
         <div className="flex items-center gap-1.5">
           <span className="text-sm font-bold">Funtastic</span>
           <span className="text-[9px] text-gray-500">v2.1</span>
         </div>
         {onCollapse && (
           <button
             type="button"
             onClick={onCollapse}
             aria-label="사이드바 접기"
             className="flex h-5 w-5 items-center justify-center rounded text-gray-400 hover:bg-gray-800 hover:text-white"
           >
             <ChevronsLeft className="h-3.5 w-3.5" />
           </button>
         )}
       </div>
       ```
  4. 나머지 navSections 렌더 로직, 로그아웃 버튼 등은 모두 그대로 유지.
  5. 기존 `w-48` 너비, `bg-gray-900`, 레이아웃 유지.

  주의:
  - `onCollapse`는 optional prop — 혹시 다른 곳에서 Sidebar를 쓰게 될 때도 동작하도록.
  - navSections 배열, 활성 경로 계산 로직, handleSignOut 등은 변경하지 않는다.
  - 'use client' 디렉티브 유지.
  </action>
  <behavior>
    - onCollapse prop이 전달되면 상단 로고 영역 우측에 ChevronsLeft 버튼 렌더
    - onCollapse prop이 없으면 버튼 렌더 안 함 (기존 레이아웃 유지)
    - 버튼 클릭 시 onCollapse 콜백 호출
    - 기존 내비게이션/로그아웃 기능은 그대로 동작
  </behavior>
  <verify>
    <automated>cd /Users/tapeoff/funtastic-saas && npx tsc --noEmit 2>&1 | grep -E "app-shell|sidebar" || echo "no type errors in target files"</automated>
  </verify>
  <done>
    - SidebarProps 인터페이스에 optional `onCollapse?: () => void` 정의
    - ChevronsLeft 아이콘 import 및 로고 영역 우측에 버튼 렌더
    - onCollapse 없을 때 버튼 미노출
    - 기존 navSections/로그아웃 로직 변경 없음
    - TypeScript 타입 에러 없음
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 접기/펼치기 동작 및 영속성 수동 검증</name>
  <what-built>
    AppShell이 client component로 전환되어 사이드바 접기/펼치기 상태를 관리한다.
    - 펼친 상태: Sidebar 상단에 << 버튼 노출
    - 접힌 상태: 메인 영역 좌상단에 >> 버튼 노출, Sidebar는 DOM에서 제거
    - 상태는 localStorage("funtastic-sidebar-collapsed")에 저장되어 새로고침 후 유지
  </what-built>
  <how-to-verify>
    1. dev 서버 실행: `cd /Users/tapeoff/funtastic-saas && npm run dev`
    2. 브라우저에서 http://localhost:3000/dashboard (또는 로그인 후 인증이 필요한 페이지) 접속
    3. 사이드바 상단 로고 우측에 `<<` 버튼이 보이는지 확인 (ChevronsLeft 아이콘)
    4. `<<` 버튼 클릭 → 사이드바가 완전히 사라지고 메인 영역 좌상단에 `>>` 버튼이 나타나는지 확인
    5. 메인 컨텐츠가 전체 화면 너비를 차지하는지 확인
    6. `>>` 버튼 클릭 → 사이드바가 다시 펼쳐지는지 확인
    7. `<<` 버튼 클릭해서 접힌 상태로 만든 뒤, 페이지 새로고침(F5) → 접힌 상태가 유지되는지 확인
    8. 다시 펼쳐서 새로고침 → 펼친 상태가 유지되는지 확인
    9. 브라우저 개발자 도구 Application > Local Storage에서 `funtastic-sidebar-collapsed` 키 값이 'true'/'false'로 저장되는지 확인
    10. 콘솔에 SSR 하이드레이션 경고("Hydration failed", "did not match")가 없는지 확인

    기대 결과:
    - 토글 동작이 즉시 반영됨 (깜빡임/레이아웃 shift 없음은 허용 범위 내)
    - 새로고침 시 초기 짧은 순간 펼침 상태로 렌더될 수 있음 (SSR mismatch 방지를 위한 의도된 동작) → 그 후 localStorage 값대로 반영
    - 하이드레이션 에러 없음
    - 기존 내비게이션 링크 및 로그아웃 버튼 정상 동작
  </how-to-verify>
  <resume-signal>"approved" 또는 문제점 설명</resume-signal>
</task>

</tasks>

<verification>
- TypeScript 컴파일 에러 없음: `cd /Users/tapeoff/funtastic-saas && npx tsc --noEmit`
- 수동 검증 통과 (Task 3 체크리스트 전체)
- localStorage 키 `funtastic-sidebar-collapsed`에 'true'/'false' 저장 확인
- 브라우저 콘솔에 하이드레이션 에러 없음
</verification>

<success_criteria>
- 사이드바 상단 << 버튼으로 접을 수 있다
- 접힌 상태에서 메인 영역 좌상단 >> 버튼으로 펼칠 수 있다
- localStorage에 상태 저장되어 새로고침 후에도 유지된다
- SSR 하이드레이션 mismatch 없음 (초기 렌더는 항상 펼침)
- 기존 내비게이션/로그아웃 동작은 변경 없음
</success_criteria>

<output>
완료 후 `.planning/quick/260424-oz0-localstorage/260424-oz0-SUMMARY.md` 작성
</output>
