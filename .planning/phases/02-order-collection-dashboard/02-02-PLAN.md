---
phase: 02-order-collection-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/lib/marketplace/adapters/coupang/client.ts
  - src/lib/marketplace/adapters/coupang/adapter.ts
  - src/lib/marketplace/adapters/coupang/types.ts
  - src/lib/marketplace/adapters/coupang/status-map.ts
  - src/lib/marketplace/adapters/naver/client.ts
  - src/lib/marketplace/adapters/naver/adapter.ts
  - src/lib/marketplace/adapters/naver/types.ts
  - src/lib/marketplace/adapters/naver/status-map.ts
  - src/lib/marketplace/adapters/configs.ts
  - tests/marketplace/coupang.test.ts
  - tests/marketplace/naver.test.ts
  - tests/helpers/msw-handlers.ts
autonomous: true
requirements:
  - MKT-01
  - MKT-02

must_haves:
  truths:
    - "Coupang adapter can sign requests with HMAC-SHA256 and fetch orders from the PO list endpoint"
    - "Naver adapter can manage OAuth2 tokens and fetch orders from the lastChangedStatuses endpoint"
    - "Both adapters normalize marketplace-specific order data into the shared NormalizedOrder interface"
    - "Both adapters map marketplace status values to internal OrderStatus with fallback for unknown statuses"
    - "Both adapters fetch claims (cancel/return/exchange) and normalize to NormalizedClaim"
  artifacts:
    - path: "src/lib/marketplace/adapters/coupang/adapter.ts"
      provides: "CoupangAdapter implementing MarketplaceAdapter"
      exports: ["CoupangAdapter"]
    - path: "src/lib/marketplace/adapters/coupang/client.ts"
      provides: "HMAC-SHA256 signing and ky HTTP client for Coupang"
      exports: ["createCoupangClient", "generateCoupangAuth"]
    - path: "src/lib/marketplace/adapters/naver/adapter.ts"
      provides: "NaverAdapter implementing MarketplaceAdapter"
      exports: ["NaverAdapter"]
    - path: "src/lib/marketplace/adapters/naver/client.ts"
      provides: "OAuth2 token management and ky HTTP client for Naver"
      exports: ["createNaverClient"]
    - path: "tests/helpers/msw-handlers.ts"
      provides: "MSW handlers mocking Coupang and Naver API responses"
  key_links:
    - from: "src/lib/marketplace/adapters/coupang/adapter.ts"
      to: "src/lib/marketplace/types.ts"
      via: "implements MarketplaceAdapter interface"
      pattern: "implements MarketplaceAdapter|MarketplaceAdapter"
    - from: "src/lib/marketplace/adapters/naver/adapter.ts"
      to: "src/lib/marketplace/types.ts"
      via: "implements MarketplaceAdapter interface"
      pattern: "implements MarketplaceAdapter|MarketplaceAdapter"
    - from: "src/lib/marketplace/adapters/coupang/client.ts"
      to: "node:crypto"
      via: "HMAC-SHA256 signing"
      pattern: "createHmac.*sha256"
---

<objective>
Implement real Coupang and Naver marketplace adapters with actual API client code. Coupang uses HMAC-SHA256 request signing; Naver uses OAuth2 token management. Both normalize marketplace-specific order/claim data into the shared NormalizedOrder/NormalizedClaim interfaces.

Purpose: Replace Phase 1 placeholder adapters with real API implementations that can fetch orders and claims.
Output: Production Coupang and Naver adapters with API clients, status mapping, response types, and MSW-based tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-order-collection-dashboard/02-CONTEXT.md
@.planning/phases/02-order-collection-dashboard/02-RESEARCH.md
@.planning/phases/02-order-collection-dashboard/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: types the adapters must produce -->

From src/lib/marketplace/types.ts (expanded in Plan 01):
```typescript
export interface NormalizedOrder {
  marketplaceOrderId: string
  marketplaceId: MarketplaceId
  marketplaceStatus: string
  status: OrderStatus
  buyerName: string
  buyerPhone?: string
  recipientName: string
  recipientPhone?: string
  shippingAddress: { zipCode: string; address1: string; address2?: string }
  items: NormalizedOrderItem[]
  orderedAt: Date
  totalAmount: number
  rawData: Record<string, unknown>
}

export interface NormalizedClaim {
  marketplaceClaimId: string
  marketplaceId: MarketplaceId
  marketplaceOrderId: string
  claimType: 'cancel' | 'return' | 'exchange'
  claimStatus: 'requested' | 'processing' | 'completed' | 'rejected'
  reason?: string
  requestedAt: Date
  rawData: Record<string, unknown>
}

export interface MarketplaceAdapter {
  readonly config: MarketplaceConfig
  testConnection(credentials: MarketplaceCredentials): Promise<{success: boolean; error?: string; expiresAt?: Date}>
  authenticate(): Promise<{ success: boolean; expiresAt?: Date }>
  getOrders(since: Date): Promise<NormalizedOrder[]>
  getClaimsOrders(since: Date): Promise<NormalizedClaim[]>
  uploadInvoice(orderId: string, invoice: InvoiceData): Promise<{ success: boolean; error?: string }>
  getProducts(): Promise<NormalizedProduct[]>
}
```

From src/lib/orders/types.ts (created in Plan 01):
```typescript
export type OrderStatus = 'new' | 'confirmed' | 'preparing' | 'shipped' | 'delivering' | 'delivered' | 'cancelled'
```

From src/lib/marketplace/errors.ts:
```typescript
export class MarketplaceAuthError extends Error { ... }
export class MarketplaceRateLimitError extends Error { ... }
export class MarketplaceApiError extends Error { ... }
```

From src/lib/marketplace/adapters/configs.ts:
```typescript
// Coupang config: id='coupang', authType='hmac', rateLimitPerSecond=100, requiredCredentials=['access_key','secret_key','vendor_id']
// Naver config: id='naver', authType='oauth2', rateLimitPerSecond=50, requiredCredentials=['client_id','client_secret']
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Coupang adapter — HMAC-SHA256 client, order/claims fetching, status mapping</name>
  <files>src/lib/marketplace/adapters/coupang/client.ts, src/lib/marketplace/adapters/coupang/adapter.ts, src/lib/marketplace/adapters/coupang/types.ts, src/lib/marketplace/adapters/coupang/status-map.ts, tests/marketplace/coupang.test.ts, tests/helpers/msw-handlers.ts</files>
  <read_first>src/lib/marketplace/types.ts, src/lib/marketplace/errors.ts, src/lib/marketplace/adapters/configs.ts, src/lib/orders/types.ts, .planning/phases/02-order-collection-dashboard/02-RESEARCH.md</read_first>
  <behavior>
    - generateCoupangAuth('GET', '/path', 'query=1', 'accessKey', 'secretKey') returns string starting with 'CEA algorithm=HmacSHA256'
    - Coupang datetime format is yyMMddTHHmmssZ (2-digit year)
    - mapCoupangStatus('ACCEPT') returns 'new'
    - mapCoupangStatus('INSTRUCT') returns 'preparing'
    - mapCoupangStatus('DEPARTURE') returns 'shipped'
    - mapCoupangStatus('DELIVERING') returns 'delivering'
    - mapCoupangStatus('FINAL_DELIVERY') returns 'delivered'
    - mapCoupangStatus('UNKNOWN_VALUE') returns 'new' (fallback with warning)
    - CoupangAdapter.getOrders() normalizes Coupang response to NormalizedOrder[]
    - CoupangAdapter.getClaimsOrders() normalizes return requests to NormalizedClaim[]
  </behavior>
  <action>
    1. Create `src/lib/marketplace/adapters/coupang/types.ts`:
       - Define Coupang-specific API response types based on documented endpoints:
         - `CoupangOrderSheet`: orderId, vendorItemId, shippingCount, orderer {name, email}, receiver {name, phone, addr1, addr2, zipCode, postCode}, paidAt, status, vendorItemName, etc.
         - `CoupangOrderSheetsResponse`: { code: string, message: string, data: CoupangOrderSheet[] }
         - `CoupangReturnRequest`: returnId, orderId, returnStatus, returnReason, createdAt, etc.
         - `CoupangReturnRequestsResponse`: { code: string, message: string, data: CoupangReturnRequest[] }

    2. Create `src/lib/marketplace/adapters/coupang/status-map.ts`:
       - Export `COUPANG_STATUS_MAP` record: ACCEPT->'new', INSTRUCT->'preparing', DEPARTURE->'shipped', DELIVERING->'delivering', FINAL_DELIVERY->'delivered'
       - Export `mapCoupangStatus(coupangStatus: string): OrderStatus` — returns mapped status or 'new' as fallback with console.warn for unknown statuses (per research Pitfall 4)
       - Export `COUPANG_CLAIM_STATUS_MAP` for return request statuses

    3. Create `src/lib/marketplace/adapters/coupang/client.ts`:
       - Import `createHmac` from 'node:crypto' (NOT a third-party library — per research "Don't Hand-Roll" section)
       - Import ky from 'ky'
       - Export `formatCoupangDatetime(date: Date): string` — format as `yyMMddTHHmmssZ` (2-DIGIT YEAR, not 4-digit — per research Pitfall 2)
       - Export `generateCoupangAuth(method, path, query, accessKey, secretKey): string` — creates HMAC-SHA256 signature. Message = `${datetime}\n${method}\n${path}\n${query}`. Returns `CEA algorithm=HmacSHA256, access-key=${accessKey}, signed-date=${datetime}, signature=${hex}`
       - Export `createCoupangClient(accessKey, secretKey)` — returns ky instance with beforeRequest hook that adds Authorization header via generateCoupangAuth. Base URL: `https://api-gateway.coupang.com`

    4. Create `src/lib/marketplace/adapters/coupang/adapter.ts`:
       - Export `CoupangAdapter` class implementing MarketplaceAdapter
       - Constructor takes credentials: { access_key, secret_key, vendor_id }
       - `config` property: use the Coupang config from configs.ts (id: 'coupang', authType: 'hmac', rateLimitPerSecond: 100, requiredCredentials: ['access_key', 'secret_key', 'vendor_id'])
       - `testConnection()`: make a lightweight API call (e.g., GET ordersheets with 1-minute window) to verify credentials work
       - `authenticate()`: return { success: true } (HMAC doesn't need separate auth flow)
       - `getOrders(since: Date)`: call GET `/v2/providers/openapi/apis/api/v5/vendors/{vendorId}/ordersheets` with createdAtFrom/createdAtTo params. Parse response, normalize each CoupangOrderSheet to NormalizedOrder using mapCoupangStatus. Paginate if needed (maxPerPage parameter).
       - `getClaimsOrders(since: Date)`: call GET `/v2/providers/openapi/apis/api/v4/vendors/{vendorId}/returnRequests` with date params. Normalize to NormalizedClaim[].
       - `uploadInvoice()` / `getProducts()`: throw 'Not implemented (Phase 3/5)' for now

    5. Create `tests/helpers/msw-handlers.ts`:
       - Set up MSW handlers for Coupang API endpoints
       - Mock GET ordersheets endpoint returning sample CoupangOrderSheetsResponse with 2-3 test orders
       - Mock GET returnRequests endpoint returning sample return requests
       - Export handlers array for use in test setup

    6. Create `tests/marketplace/coupang.test.ts`:
       - Test generateCoupangAuth produces correct signature format (starts with 'CEA algorithm=HmacSHA256')
       - Test formatCoupangDatetime uses 2-digit year format
       - Test mapCoupangStatus for all known statuses and fallback behavior
       - Test CoupangAdapter.getOrders normalizes mock response to NormalizedOrder[]
       - Test CoupangAdapter.getClaimsOrders normalizes to NormalizedClaim[]
       - Use MSW for HTTP-level mocking
  </action>
  <verify>
    <automated>npx vitest run tests/marketplace/coupang.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "CEA algorithm=HmacSHA256" src/lib/marketplace/adapters/coupang/client.ts` finds the auth header format
    - `grep "createHmac.*sha256" src/lib/marketplace/adapters/coupang/client.ts` finds Node.js crypto usage
    - `grep "ACCEPT.*new" src/lib/marketplace/adapters/coupang/status-map.ts` finds status mapping
    - `grep "class CoupangAdapter" src/lib/marketplace/adapters/coupang/adapter.ts` finds the class
    - `grep "getClaimsOrders" src/lib/marketplace/adapters/coupang/adapter.ts` finds claims method
    - `npx vitest run tests/marketplace/coupang.test.ts` shows all tests passing
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Coupang adapter fully implements getOrders and getClaimsOrders with HMAC-SHA256 signing, status mapping, and order normalization. All tests pass with MSW mocks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Naver adapter — OAuth2 token management, order/claims fetching, status mapping</name>
  <files>src/lib/marketplace/adapters/naver/client.ts, src/lib/marketplace/adapters/naver/adapter.ts, src/lib/marketplace/adapters/naver/types.ts, src/lib/marketplace/adapters/naver/status-map.ts, src/lib/marketplace/adapters/configs.ts, tests/marketplace/naver.test.ts, tests/helpers/msw-handlers.ts</files>
  <read_first>src/lib/marketplace/types.ts, src/lib/marketplace/errors.ts, src/lib/marketplace/adapters/configs.ts, src/lib/orders/types.ts, src/lib/marketplace/adapters/coupang/adapter.ts, tests/helpers/msw-handlers.ts</read_first>
  <behavior>
    - NaverAdapter.authenticate() exchanges client_id+client_secret for access token
    - Token is cached and refreshed proactively 5 minutes before expiry (per research Pitfall 3)
    - mapNaverStatus('PAYED') returns 'new'
    - mapNaverStatus('DELIVERING') returns 'delivering'
    - mapNaverStatus('UNKNOWN') returns 'new' (fallback)
    - NaverAdapter.getOrders() fetches changed product orders and normalizes to NormalizedOrder[]
    - NaverAdapter.getClaimsOrders() fetches claim-type changes and normalizes to NormalizedClaim[]
  </behavior>
  <action>
    1. Create `src/lib/marketplace/adapters/naver/types.ts`:
       - Define Naver-specific API response types:
         - `NaverTokenResponse`: { access_token: string, expires_in: number, token_type: string }
         - `NaverProductOrder`: productOrderId, orderId, productOrderStatus, quantity, unitPrice, productName, optionInfo, ordererName, ordererTel, shippingAddress {zipCode, baseAddress, detailedAddress}, paymentDate, claimType?, claimStatus?, etc.
         - `NaverLastChangedStatusesResponse`: { data: { lastChangeStatuses: Array<{productOrderId: string, lastChangedType: string, lastChangedDate: string}> } }
         - `NaverProductOrderDetailResponse`: { data: NaverProductOrder[] }

    2. Create `src/lib/marketplace/adapters/naver/status-map.ts`:
       - Export `NAVER_STATUS_MAP` record: PAYED->'new', PAYMENT_WAITING->'new', DELIVERING->'delivering', DELIVERED->'delivered', PURCHASE_DECIDED->'delivered', EXCHANGED->'confirmed', CANCELED->'cancelled', RETURNED->'cancelled'
       - Export `mapNaverStatus(naverStatus: string): OrderStatus`
       - Export `NAVER_CLAIM_TYPE_MAP`: CANCEL->'cancel', RETURN->'return', EXCHANGE->'exchange'

    3. Create `src/lib/marketplace/adapters/naver/client.ts`:
       - Import ky from 'ky'
       - Export `createNaverClient(clientId, clientSecret)`:
         - Internal state: `accessToken`, `tokenExpiresAt`
         - `getToken()`: if token exists and not expiring within 5 minutes, return cached. Otherwise call POST `https://api.commerce.naver.com/external/v1/oauth2/token` with `client_id`, `client_secret`, `grant_type=client_credentials`, `type=SELF`. Parse NaverTokenResponse, cache token and expiry. (per research Pitfall 3: refresh proactively 5 min before expiry)
         - Returns ky instance with `beforeRequest` hook that calls getToken() and adds `Authorization: Bearer ${token}` header
         - Base URL: `https://api.commerce.naver.com`

    4. Create `src/lib/marketplace/adapters/naver/adapter.ts`:
       - Export `NaverAdapter` class implementing MarketplaceAdapter
       - Constructor takes credentials: { client_id, client_secret }
       - `config`: use Naver config (id: 'naver', authType: 'oauth2', rateLimitPerSecond: 50, requiredCredentials: ['client_id', 'client_secret'])
       - `testConnection()`: try to get a token, return success/error
       - `authenticate()`: call getToken(), return { success: true, expiresAt }
       - `getOrders(since: Date)`: two-step per research Pattern 6:
         a. GET `/v1/pay-order/seller/product-orders/last-changed-statuses` with lastChangedFrom/To params, lastChangedType=PAYED,DELIVERED,PURCHASE_DECIDED etc.
         b. Batch fetch full details: POST `/v1/pay-order/seller/product-orders/query` with productOrderIds
         c. Normalize to NormalizedOrder[]
       - `getClaimsOrders(since: Date)`: same pattern but with claim-type lastChangedType values (CANCEL, RETURN, EXCHANGE). Normalize to NormalizedClaim[].
       - `uploadInvoice()` / `getProducts()`: throw 'Not implemented (Phase 3/5)'

    5. Update `src/lib/marketplace/adapters/configs.ts`:
       - Replace placeholder adapters with factory functions that create real CoupangAdapter/NaverAdapter instances
       - Keep `registerDefaultAdapters()` but make it register factory-backed adapters
       - Note: adapters need credentials at construction time, so the registry stores config-only adapters. Actual credential-based instances are created by the worker when collecting orders. Update the pattern: registry stores adapter factories or config, worker creates instances.
       - ALTERNATIVE simpler approach: keep placeholder adapters in registry (for config lookup), but the worker creates real adapter instances directly with credentials. This avoids changing the registry pattern.

    6. Update `tests/helpers/msw-handlers.ts`:
       - Add MSW handlers for Naver OAuth token endpoint
       - Add handlers for lastChangedStatuses and product order detail endpoints
       - Keep existing Coupang handlers

    7. Create `tests/marketplace/naver.test.ts`:
       - Test token caching and proactive refresh
       - Test mapNaverStatus for all known statuses and fallback
       - Test NaverAdapter.getOrders normalizes mock response
       - Test NaverAdapter.getClaimsOrders normalizes claims
       - Use MSW handlers
  </action>
  <verify>
    <automated>npx vitest run tests/marketplace/naver.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "class NaverAdapter" src/lib/marketplace/adapters/naver/adapter.ts` finds the class
    - `grep "oauth2/token" src/lib/marketplace/adapters/naver/client.ts` finds token endpoint
    - `grep "Bearer" src/lib/marketplace/adapters/naver/client.ts` finds auth header pattern
    - `grep "PAYED.*new" src/lib/marketplace/adapters/naver/status-map.ts` finds status mapping
    - `grep "getClaimsOrders" src/lib/marketplace/adapters/naver/adapter.ts` finds claims method
    - `grep "last-changed-statuses" src/lib/marketplace/adapters/naver/adapter.ts` finds Naver endpoint
    - `npx vitest run tests/marketplace/ --reporter=verbose` shows all marketplace tests passing
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Naver adapter fully implements getOrders and getClaimsOrders with OAuth2 token management, proactive refresh, status mapping, and order normalization. Both Coupang and Naver adapters pass all tests.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/marketplace/ --reporter=verbose` -- all adapter tests pass
- `npx tsc --noEmit` -- no type errors
- Both adapters implement full MarketplaceAdapter interface
- Status mappings cover all known marketplace statuses with safe fallbacks
</verification>

<success_criteria>
- Coupang adapter can sign HMAC-SHA256 requests and normalize orders/claims (MKT-01 order collection scope)
- Naver adapter can manage OAuth2 tokens and normalize orders/claims (MKT-02 order collection scope)
- Both adapters produce NormalizedOrder[] and NormalizedClaim[] matching the shared interfaces
- MSW test infrastructure ready for continued use in integration tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-order-collection-dashboard/02-02-SUMMARY.md`
</output>
