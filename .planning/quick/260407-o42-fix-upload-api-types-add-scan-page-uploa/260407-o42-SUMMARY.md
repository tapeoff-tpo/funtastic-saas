---
phase: quick
plan: 260407-o42
subsystem: shipping, settings, marketplace
tags: [typescript-fix, company-settings, scan-upload, cj-export]
dependency_graph:
  requires: []
  provides: [companySettings-table, company-settings-page, scan-upload-button]
  affects: [cj-export, naver-adapter, scan-page]
tech_stack:
  added: []
  patterns: [useActionState-form, drizzle-upsert]
key_files:
  created:
    - src/app/(auth)/settings/company/page.tsx
    - src/app/(auth)/settings/company/actions.ts
    - src/app/(auth)/settings/company/company-settings-form.tsx
    - supabase/migrations/003_add_company_settings.sql
  modified:
    - src/lib/db/schema.ts
    - src/lib/marketplace/adapters/naver/adapter.ts
    - src/app/api/shipping/cj/export/route.ts
    - src/app/(auth)/shipping/scan/page.tsx
decisions:
  - companySettings table uses userId unique index for one-row-per-user upsert pattern
  - useActionState for form submission with Sonner toast feedback
metrics:
  duration: 3min
  completed: 2026-04-07
---

# Quick Task 260407-o42: Fix Upload API Types, Add Scan Page Upload, Company Settings

Company settings table + page for sender info, scan page upload button, Naver adapter TS fix removing undefined shippingAddress return.

## Changes Made

### Task 1: Fix TypeScript errors + add company_settings table
- **Naver adapter**: Fixed `shippingAddress` returning `undefined` when `addr` is falsy -- now returns `{ zipCode: '', address1: '' }` default object to satisfy `NormalizedOrder` type requirement
- **Schema**: Added `companySettings` table with fields: companyName, phone, address, zipCode, unique on userId
- **Migration**: SQL migration with RLS policy restricting access to own rows

### Task 2: Company settings page + wire CJ export
- **Server actions** (`actions.ts`): `getCompanySettings` (select) and `saveCompanySettings` (upsert via `onConflictDoUpdate`)
- **Page** (`page.tsx`): Server component with auth check, loads settings, renders `CompanySettingsForm`
- **Client form** (`company-settings-form.tsx`): Uses `useActionState` for form submission, Sonner toast for feedback
- **CJ export**: Removed hardcoded `SENDER` const, now queries `companySettings` table and falls back to empty strings

### Task 3: Add upload button to scan page
- Added "오늘 출고분 전송" button in scan page header between title and today count
- Calls `POST /api/shipping/upload` with loading/disabled states
- Shows result count (uploaded/failed) with color coding (green/amber)
- TTS feedback on completion
- Auto-dismiss result after 10 seconds

## Deviations from Plan

None -- plan executed exactly as written.

## Verification

- `npx tsc --noEmit` shows no errors in any modified files
- Pre-existing TS errors remain in `tests/shipping/invoice-upload.test.ts` and `worker.ts` (not touched by this task)

## Commits

| Task | Commit | Description |
|------|--------|-------------|
| 1 | c58613f | fix: Naver adapter TS error, add companySettings table + migration |
| 2 | b27b010 | feat: company settings page, wire CJ export to DB |
| 3 | b65be33 | feat: upload button on scan page |

## Known Stubs

None -- all features are fully wired. The `/api/shipping/upload` endpoint is assumed to exist (not created by this plan).

## Self-Check: PASSED
