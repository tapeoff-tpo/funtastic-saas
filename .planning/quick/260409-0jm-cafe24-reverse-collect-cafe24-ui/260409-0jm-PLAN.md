---
phase: quick
plan: 260409-0jm
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/marketplace/adapters/cafe24/adapter.ts
  - src/lib/products/reverse-collect.ts
  - src/app/(auth)/products/import/marketplace/page.tsx
  - src/app/(auth)/products/page.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Cafe24 연결이 있으면 마켓플레이스 가져오기 드롭다운에 표시된다"
    - "가져오기 실행 후 건수 결과(가져옴/스킵/오류)가 화면에 표시된다"
    - "reverse-collect.ts가 cafe24 케이스를 switch에서 처리한다"
  artifacts:
    - path: "src/lib/marketplace/adapters/cafe24/adapter.ts"
      provides: "requiredCredentials에 access_token 추가"
      contains: "'client_id', 'client_secret', 'mall_id', 'access_token'"
    - path: "src/lib/products/reverse-collect.ts"
      provides: "Cafe24Adapter 인스턴스화 케이스"
      contains: "case 'cafe24'"
    - path: "src/app/(auth)/products/import/marketplace/page.tsx"
      provides: "마켓플레이스 역수집 UI 페이지"
  key_links:
    - from: "src/app/(auth)/products/import/marketplace/page.tsx"
      to: "reverseCollectAction"
      via: "dynamic import of @/lib/products/ui-actions"
---

<objective>
Cafe24 상품 역수집 지원 추가: adapter에 access_token 자격증명 추가, reverse-collect 스위치에 cafe24 케이스 추가, 마켓플레이스 역수집 UI 페이지 생성.

Purpose: 사용자가 Cafe24에 연결된 상품을 내부 DB로 역수집할 수 있다.
Output: 수정된 adapter.ts + reverse-collect.ts, 신규 marketplace import 페이지, products 페이지에 링크 추가.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cafe24 adapter에 access_token 추가 + reverse-collect 스위치 확장</name>
  <files>
    src/lib/marketplace/adapters/cafe24/adapter.ts
    src/lib/products/reverse-collect.ts
  </files>
  <action>
    **adapter.ts (line 35):**
    `requiredCredentials` 배열에 `'access_token'` 추가:
    ```
    requiredCredentials: ['client_id', 'client_secret', 'mall_id', 'access_token'],
    ```
    이렇게 하면 readCredential()이 vault에서 access_token을 읽어서 credentials 맵에 넣어준다.

    **reverse-collect.ts:**
    1. 파일 상단 import에 Cafe24Adapter 추가:
       ```ts
       import { Cafe24Adapter } from '@/lib/marketplace/adapters/cafe24/adapter'
       ```
    2. `createAdapterWithCredentials` 함수의 switch 문 `default:` 앞에 cafe24 케이스 추가:
       ```ts
       case 'cafe24':
         return new Cafe24Adapter({
           access_token: credentials.access_token ?? '',
           mall_id: credentials.mall_id ?? '',
         })
       ```
    3. `skuPrefix` 맵에 cafe24 추가: `cafe24: 'C24'`

    주의: Cafe24Adapter constructor는 `{ access_token, mall_id }` 만 받는다 (client.ts 참조).
    client_id/client_secret은 OAuth 토큰 갱신용이지만 현재 adapter 생성자에는 전달하지 않는다.
  </action>
  <verify>
    <automated>cd /Users/ian/Desktop/funtastic-saas && npx tsc --noEmit 2>&1 | grep -E "cafe24|reverse-collect|adapter" | head -20</automated>
  </verify>
  <done>
    TypeScript 컴파일 에러 없이 cafe24 케이스가 switch에 추가되고 adapter.ts requiredCredentials에 access_token이 포함된다.
  </done>
</task>

<task type="auto">
  <name>Task 2: 마켓플레이스 역수집 UI 페이지 + products 페이지 링크</name>
  <files>
    src/app/(auth)/products/import/marketplace/page.tsx
    src/app/(auth)/products/page.tsx
  </files>
  <action>
    **신규 파일: src/app/(auth)/products/import/marketplace/page.tsx**

    `'use client'` 클라이언트 컴포넌트. 기존 `/products/import/page.tsx` 패턴 참고 (useTransition + dynamic import).

    구현 내용:
    1. 페이지 마운트 시 연결된 마켓플레이스 목록을 불러온다.
       - `import { createClient } from '@/lib/supabase/server'` 불가 (클라이언트 컴포넌트).
       - 대신 Supabase browser client 사용: `import { createBrowserClient } from '@supabase/ssr'`
       - `useEffect`에서 `supabase.from('marketplace_connections').select('id, marketplace_id, display_name, status').eq('status', 'connected')` 쿼리.
       - 또는 더 단순하게: **서버 컴포넌트로 구현** — `createClient()` 사용해서 connections 쿼리 후 클라이언트 자식에게 props로 전달.

       **권장: 서버/클라이언트 분리 패턴 사용.**
       - `page.tsx` (서버): connections 쿼리 → `<MarketplaceImportClient connections={connections} />` 렌더
       - `marketplace-import-client.tsx` (클라이언트): UI + reverseCollectAction 호출

       단, quick task이므로 단일 파일로 구현한다:
       `page.tsx` = `async` 서버 컴포넌트. connections 조회 후 인라인 클라이언트 컴포넌트(별도 파일)에 전달.

       최종 구현: **2개 파일**
       - `src/app/(auth)/products/import/marketplace/page.tsx` — 서버 컴포넌트, connections 조회
       - `src/app/(auth)/products/import/marketplace/import-client.tsx` — 클라이언트 컴포넌트

       **page.tsx (서버):**
       ```tsx
       import { createClient } from '@/lib/supabase/server'
       import { db } from '@/lib/db'
       import { marketplaceConnections } from '@/lib/db/schema'
       import { eq } from 'drizzle-orm'
       import { MarketplaceImportClient } from './import-client'
       import type { Metadata } from 'next'

       export const metadata: Metadata = { title: '마켓플레이스 가져오기' }

       export default async function MarketplaceImportPage() {
         const supabase = await createClient()
         const { data: { user } } = await supabase.auth.getUser()
         if (!user) return null

         const connections = await db
           .select({
             id: marketplaceConnections.id,
             marketplaceId: marketplaceConnections.marketplaceId,
             displayName: marketplaceConnections.displayName,
             status: marketplaceConnections.status,
           })
           .from(marketplaceConnections)
           .where(eq(marketplaceConnections.userId, user.id))

         const connected = connections.filter(c => c.status === 'connected')

         return (
           <div className="mx-auto max-w-2xl space-y-6">
             <div>
               <h1 className="text-2xl font-bold">마켓플레이스 가져오기</h1>
               <p className="mt-1 text-sm text-muted-foreground">
                 연결된 마켓플레이스에서 상품을 내부 DB로 가져옵니다.
               </p>
             </div>
             {connected.length === 0 ? (
               <div className="rounded-md border p-6 text-center text-sm text-muted-foreground">
                 연결된 마켓플레이스가 없습니다.{' '}
                 <a href="/settings/marketplaces" className="text-blue-600 underline">
                   마켓플레이스 연결
                 </a>
                 에서 먼저 연결하세요.
               </div>
             ) : (
               <MarketplaceImportClient connections={connected} />
             )}
           </div>
         )
       }
       ```

       **import-client.tsx (클라이언트):**
       ```tsx
       'use client'
       import { useState, useTransition } from 'react'

       interface Connection {
         id: string
         marketplaceId: string
         displayName: string
         status: string
       }

       interface ImportResult {
         imported: number
         skipped: number
         errors: string[]
       }

       export function MarketplaceImportClient({ connections }: { connections: Connection[] }) {
         const [selectedId, setSelectedId] = useState(connections[0]?.id ?? '')
         const [result, setResult] = useState<ImportResult | null>(null)
         const [isPending, startTransition] = useTransition()

         const selectedConnection = connections.find(c => c.id === selectedId)

         const handleImport = () => {
           if (!selectedConnection) return
           setResult(null)
           startTransition(async () => {
             const { reverseCollectAction } = await import('@/lib/products/ui-actions')
             const res = await reverseCollectAction(selectedConnection.id, selectedConnection.marketplaceId)
             if (res.success) {
               setResult(res.data)
             } else {
               alert(`가져오기 실패: ${res.error}`)
             }
           })
         }

         return (
           <div className="space-y-6">
             <div className="space-y-2">
               <label className="text-sm font-medium">마켓플레이스 선택</label>
               <select
                 value={selectedId}
                 onChange={e => { setSelectedId(e.target.value); setResult(null) }}
                 className="w-full rounded-md border px-3 py-2 text-sm"
               >
                 {connections.map(c => (
                   <option key={c.id} value={c.id}>{c.displayName} ({c.marketplaceId})</option>
                 ))}
               </select>
             </div>

             <div className="flex justify-end">
               <button
                 type="button"
                 onClick={handleImport}
                 disabled={isPending || !selectedId}
                 className="rounded-md bg-black px-4 py-2 text-sm text-white hover:bg-gray-800 disabled:opacity-50"
               >
                 {isPending ? '가져오는 중...' : '가져오기 실행'}
               </button>
             </div>

             {result && (
               <div className="space-y-3 rounded-md border p-4">
                 <h2 className="font-semibold">가져오기 결과</h2>
                 <div className="grid grid-cols-3 gap-3 text-sm">
                   <div className="rounded-md border p-3 text-center">
                     <p className="text-2xl font-bold">{result.imported}</p>
                     <p className="text-muted-foreground">가져옴</p>
                   </div>
                   <div className="rounded-md border p-3 text-center">
                     <p className="text-2xl font-bold">{result.skipped}</p>
                     <p className="text-muted-foreground">스킵</p>
                   </div>
                   <div className="rounded-md border p-3 text-center">
                     <p className="text-2xl font-bold text-red-600">{result.errors.length}</p>
                     <p className="text-muted-foreground">오류</p>
                   </div>
                 </div>
                 {result.errors.length > 0 && (
                   <ul className="mt-2 list-inside list-disc text-xs text-red-500">
                     {result.errors.map((err, i) => <li key={i}>{err}</li>)}
                   </ul>
                 )}
                 <div className="flex justify-end gap-2">
                   <a href="/products" className="rounded-md bg-black px-3 py-1.5 text-sm text-white hover:bg-gray-800">
                     상품 목록으로
                   </a>
                 </div>
               </div>
             )}
           </div>
         )
       }
       ```

    **products/page.tsx 수정:**
    line 80 근처 `<a href="/products/import" ...>엑셀 가져오기</a>` 앞에 마켓플레이스 가져오기 링크 추가:
    ```tsx
    <a
      href="/products/import/marketplace"
      className="rounded-md border px-3 py-1.5 text-sm hover:bg-muted"
    >
      마켓플레이스 가져오기
    </a>
    ```

    실제 파일은 3개임: page.tsx (서버), import-client.tsx (클라이언트), products/page.tsx (수정).
    files_modified에 import-client.tsx를 빠트렸지만 page.tsx와 함께 같은 디렉토리에 생성한다.
  </action>
  <verify>
    <automated>cd /Users/ian/Desktop/funtastic-saas && npx tsc --noEmit 2>&1 | grep -E "marketplace/page|import-client|MarketplaceImport" | head -20</automated>
  </verify>
  <done>
    /products/import/marketplace 페이지가 생성되고, TypeScript 에러 없이 컴파일된다.
    /products 페이지 헤더에 "마켓플레이스 가져오기" 링크가 추가된다.
  </done>
</task>

</tasks>

<verification>
TypeScript 전체 컴파일:
```bash
cd /Users/ian/Desktop/funtastic-saas && npx tsc --noEmit 2>&1 | tail -20
```
에러 0건이면 완료.
</verification>

<success_criteria>
- reverse-collect.ts switch에 `case 'cafe24':` 케이스 존재
- Cafe24Adapter requiredCredentials에 `'access_token'` 포함
- /products/import/marketplace 페이지 접근 가능 (연결 목록 표시)
- /products 페이지에 "마켓플레이스 가져오기" 링크 표시
- TypeScript 컴파일 에러 없음
</success_criteria>

<output>
After completion, create `.planning/quick/260409-0jm-cafe24-reverse-collect-cafe24-ui/260409-0jm-SUMMARY.md`
</output>
