---
phase: 07-cafe24-cj-ns-b2b
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/marketplace/adapters/domeggook/adapter.ts
  - src/lib/marketplace/adapters/domeggook/client.ts
  - src/lib/marketplace/adapters/domeggook/types.ts
  - src/lib/marketplace/adapters/domeggook/status-map.ts
  - src/lib/marketplace/adapters/onchannel/adapter.ts
  - src/lib/marketplace/adapters/onchannel/client.ts
  - src/lib/marketplace/adapters/onchannel/types.ts
  - src/lib/marketplace/adapters/onchannel/status-map.ts
  - src/lib/marketplace/adapters/ownerclan/adapter.ts
  - src/lib/marketplace/adapters/ownerclan/client.ts
  - src/lib/marketplace/adapters/ownerclan/types.ts
  - src/lib/marketplace/adapters/ownerclan/status-map.ts
  - src/lib/marketplace/adapters/ssgmall/adapter.ts
  - src/lib/marketplace/adapters/ssgmall/client.ts
  - src/lib/marketplace/adapters/ssgmall/types.ts
  - src/lib/marketplace/adapters/ssgmall/status-map.ts
  - src/lib/marketplace/adapters/ably/adapter.ts
  - src/lib/marketplace/adapters/ably/client.ts
  - src/lib/marketplace/adapters/ably/types.ts
  - src/lib/marketplace/adapters/ably/status-map.ts
autonomous: true
requirements:
  - MKT-V2

must_haves:
  truths:
    - "도매꾹 adapter supports XML+JSON API with OpenAPI auth"
    - "온채널 adapter implements MarketplaceAdapter with API key auth"
    - "오너클랜 adapter implements MarketplaceAdapter with API key auth"
    - "신세계몰 adapter implements MarketplaceAdapter with API key auth"
    - "에이블리 adapter implements MarketplaceAdapter with API key auth"
  artifacts:
    - path: "src/lib/marketplace/adapters/domeggook/adapter.ts"
      provides: "도매꾹 MarketplaceAdapter implementation"
      contains: "class DomeggookAdapter"
    - path: "src/lib/marketplace/adapters/onchannel/adapter.ts"
      provides: "온채널 MarketplaceAdapter implementation"
      contains: "class OnchannelAdapter"
    - path: "src/lib/marketplace/adapters/ownerclan/adapter.ts"
      provides: "오너클랜 MarketplaceAdapter implementation"
      contains: "class OwnerclanAdapter"
    - path: "src/lib/marketplace/adapters/ssgmall/adapter.ts"
      provides: "신세계몰 MarketplaceAdapter implementation"
      contains: "class SsgmallAdapter"
    - path: "src/lib/marketplace/adapters/ably/adapter.ts"
      provides: "에이블리 MarketplaceAdapter implementation"
      contains: "class AblyAdapter"
  key_links:
    - from: "src/lib/marketplace/adapters/domeggook/adapter.ts"
      to: "src/lib/marketplace/types.ts"
      via: "implements MarketplaceAdapter"
      pattern: "MarketplaceAdapter"
---

<objective>
Create 5 Tier 2 marketplace adapters: 도매꾹, 온채널, 오너클랜, 신세계몰, 에이블리.

Purpose: These marketplaces have known APIs but less documentation than Tier 1. Each gets a full 4-file implementation with best-effort API endpoints (per D-03).
Output: 5 adapter directories, each with adapter.ts, client.ts, types.ts, status-map.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-cafe24-cj-ns-b2b/07-CONTEXT.md

@src/lib/marketplace/types.ts
@src/lib/marketplace/errors.ts
@src/lib/marketplace/adapters/elevenst/adapter.ts
@src/lib/marketplace/adapters/ohouse/adapter.ts

<interfaces>
<!-- Same MarketplaceAdapter contract as Plan 01 -->
```typescript
export interface MarketplaceAdapter {
  readonly config: MarketplaceConfig
  testConnection(credentials: MarketplaceCredentials): Promise<{ success: boolean; error?: string; expiresAt?: Date }>
  authenticate(): Promise<{ success: boolean; expiresAt?: Date }>
  getOrders(since: Date): Promise<NormalizedOrder[]>
  getClaimsOrders(since: Date): Promise<NormalizedClaim[]>
  uploadInvoice(orderId: string, invoice: InvoiceData): Promise<{ success: boolean; error?: string }>
  getProducts(): Promise<NormalizedProduct[]>
  registerProduct(product: NormalizedProduct): Promise<{ success: boolean; marketplaceProductId?: string; error?: string }>
  updateProduct(marketplaceProductId: string, product: Partial<NormalizedProduct>): Promise<{ success: boolean; error?: string }>
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 도매꾹 and 온채널 and 오너클랜 adapters</name>
  <files>
    src/lib/marketplace/adapters/domeggook/adapter.ts
    src/lib/marketplace/adapters/domeggook/client.ts
    src/lib/marketplace/adapters/domeggook/types.ts
    src/lib/marketplace/adapters/domeggook/status-map.ts
    src/lib/marketplace/adapters/onchannel/adapter.ts
    src/lib/marketplace/adapters/onchannel/client.ts
    src/lib/marketplace/adapters/onchannel/types.ts
    src/lib/marketplace/adapters/onchannel/status-map.ts
    src/lib/marketplace/adapters/ownerclan/adapter.ts
    src/lib/marketplace/adapters/ownerclan/client.ts
    src/lib/marketplace/adapters/ownerclan/types.ts
    src/lib/marketplace/adapters/ownerclan/status-map.ts
  </files>
  <action>
    Read the ohouse adapter as reference pattern (Tier 3 best-effort style). Create 3 adapters following the 4-file pattern (per D-01).

    **도매꾹** (id: 'domeggook'):
    - `client.ts`: Use ky. 도매꾹 has an OpenAPI with both XML and JSON endpoints. Base URL: `https://domeggook.com/api/v1` (best-effort). Accept both XML and JSON -- use fast-xml-parser for XML responses (per D-05). Create `createDomeggookClient(apiKey)` with API key in header.
    - `types.ts`: Define DomeggookOrder, DomeggookClaim, DomeggookProduct. Include both XML-parsed and JSON shapes.
    - `status-map.ts`: Map statuses: '결제완료' -> 'new', '상품준비중' -> 'confirmed', '배송준비' -> 'preparing', '배송중' -> 'shipped', '배송완료' -> 'delivered'. Export mapDomeggookStatus, mapDomeggookClaimType, mapDomeggookClaimStatus.
    - `adapter.ts`: Export class DomeggookAdapter. Config: id='domeggook', name='도매꾹', authType='api_key', rateLimitPerSecond=20, requiredCredentials=['api_key', 'seller_id']. Implement all methods.

    **온채널** (id: 'onchannel'):
    - `client.ts`: Use ky with API key auth. Base URL: `https://api.onchannel.com/v1` (best-effort per D-03). Create `createOnchannelClient(apiKey)`.
    - `types.ts`: Define OnchannelOrder, OnchannelClaim, OnchannelProduct. JSON API.
    - `status-map.ts`: Map statuses: PAID -> 'new', CONFIRMED -> 'confirmed', READY_TO_SHIP -> 'preparing', SHIPPED -> 'shipped', DELIVERED -> 'delivered'. Export mapOnchannelStatus, etc.
    - `adapter.ts`: Export class OnchannelAdapter. Config: id='onchannel', name='온채널', authType='api_key', rateLimitPerSecond=20, requiredCredentials=['api_key', 'shop_id']. Implement all methods.

    **오너클랜** (id: 'ownerclan'):
    - `client.ts`: Use ky with API key auth. Base URL: `https://api.ownerclan.com/v1` (best-effort per D-03). Create `createOwnerclanClient(apiKey)`.
    - `types.ts`: Define OwnerclanOrder, OwnerclanClaim, OwnerclanProduct. JSON API.
    - `status-map.ts`: Same pattern as onchannel status mapping. Export mapOwnerclanStatus, etc.
    - `adapter.ts`: Export class OwnerclanAdapter. Config: id='ownerclan', name='오너클랜', authType='api_key', rateLimitPerSecond=20, requiredCredentials=['api_key', 'seller_id']. Implement all methods.

    Per D-05: Use ky for HTTP, fast-xml-parser for 도매꾹 XML endpoints.
  </action>
  <verify>
    <automated>npx tsc --noEmit --strict src/lib/marketplace/adapters/domeggook/adapter.ts src/lib/marketplace/adapters/onchannel/adapter.ts src/lib/marketplace/adapters/ownerclan/adapter.ts 2>&1 | head -20</automated>
  </verify>
  <done>도매꾹, 온채널, 오너클랜 adapter directories each have 4 files, all TypeScript-clean</done>
</task>

<task type="auto">
  <name>Task 2: Create 신세계몰 and 에이블리 adapters</name>
  <files>
    src/lib/marketplace/adapters/ssgmall/adapter.ts
    src/lib/marketplace/adapters/ssgmall/client.ts
    src/lib/marketplace/adapters/ssgmall/types.ts
    src/lib/marketplace/adapters/ssgmall/status-map.ts
    src/lib/marketplace/adapters/ably/adapter.ts
    src/lib/marketplace/adapters/ably/client.ts
    src/lib/marketplace/adapters/ably/types.ts
    src/lib/marketplace/adapters/ably/status-map.ts
  </files>
  <action>
    Create 2 more Tier 2 adapters following the 4-file pattern (per D-01).

    **신세계몰** (id: 'ssgmall'):
    - `client.ts`: Use ky with API key auth. Base URL: `https://api.ssg.com/v1` (best-effort per D-03). Create `createSsgmallClient(apiKey)`.
    - `types.ts`: Define SsgmallOrder, SsgmallClaim, SsgmallProduct. JSON API.
    - `status-map.ts`: Map statuses: ORDER_PLACED -> 'new', ORDER_CONFIRMED -> 'confirmed', READY_TO_SHIP -> 'preparing', IN_TRANSIT -> 'shipped', DELIVERED -> 'delivered'. Export mapSsgmallStatus, mapSsgmallClaimType, mapSsgmallClaimStatus.
    - `adapter.ts`: Export class SsgmallAdapter. Config: id='ssgmall', name='신세계몰', authType='api_key', rateLimitPerSecond=30, requiredCredentials=['api_key', 'vendor_id']. Implement all methods.

    **에이블리** (id: 'ably'):
    - `client.ts`: Use ky with API key auth. Base URL: `https://api.a-bly.com/v1` (best-effort per D-03). Create `createAblyClient(apiKey)`.
    - `types.ts`: Define AblyOrder, AblyClaim, AblyProduct. JSON API.
    - `status-map.ts`: Map statuses: ORDERED -> 'new', CONFIRMED -> 'confirmed', PACKING -> 'preparing', SHIPPING -> 'shipped', COMPLETE -> 'delivered'. Export mapAblyStatus, mapAblyClaimType, mapAblyClaimStatus.
    - `adapter.ts`: Export class AblyAdapter. Config: id='ably', name='에이블리', authType='api_key', rateLimitPerSecond=30, requiredCredentials=['api_key', 'shop_id']. Implement all methods.
  </action>
  <verify>
    <automated>npx tsc --noEmit --strict src/lib/marketplace/adapters/ssgmall/adapter.ts src/lib/marketplace/adapters/ably/adapter.ts 2>&1 | head -20</automated>
  </verify>
  <done>신세계몰 and 에이블리 adapter directories each have 4 files, all TypeScript-clean</done>
</task>

</tasks>

<verification>
- All 5 adapter directories exist with 4 files each (20 files total)
- `npx tsc --noEmit` passes for all adapter files
- Each adapter.ts exports a class implementing MarketplaceAdapter
- 도매꾹 client.ts imports fast-xml-parser for XML support
</verification>

<success_criteria>
- 5 Tier 2 adapters created: domeggook, onchannel, ownerclan, ssgmall, ably
- Each follows exact 4-file pattern (per D-01)
- All use API key auth
- 도매꾹 supports XML+JSON (per D-05)
- All TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-cafe24-cj-ns-b2b/07-02-SUMMARY.md`
</output>
