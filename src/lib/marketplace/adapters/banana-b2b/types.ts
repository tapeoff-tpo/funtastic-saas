/**
 * Banana B2B (바나나B2B) API response types.
 *
 * TODO: Update types when API documentation becomes available.
 * These are minimal placeholder types for the stub adapter.
 */

/** Placeholder order type */
export interface BananaB2bOrder {
  id: string
  status: string
  rawData: Record<string, unknown>
}

/** Placeholder claim type */
export interface BananaB2bClaim {
  id: string
  status: string
  rawData: Record<string, unknown>
}

/** Placeholder product type */
export interface BananaB2bProduct {
  id: string
  status: string
  rawData: Record<string, unknown>
}
