---
phase: 07-cafe24-cj-ns-b2b
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/marketplace/adapters/always/adapter.ts
  - src/lib/marketplace/adapters/always/client.ts
  - src/lib/marketplace/adapters/always/types.ts
  - src/lib/marketplace/adapters/always/status-map.ts
  - src/lib/marketplace/adapters/10x10/adapter.ts
  - src/lib/marketplace/adapters/10x10/client.ts
  - src/lib/marketplace/adapters/10x10/types.ts
  - src/lib/marketplace/adapters/10x10/status-map.ts
  - src/lib/marketplace/adapters/toss-shopping/adapter.ts
  - src/lib/marketplace/adapters/toss-shopping/client.ts
  - src/lib/marketplace/adapters/toss-shopping/types.ts
  - src/lib/marketplace/adapters/toss-shopping/status-map.ts
  - src/lib/marketplace/adapters/tobizon/adapter.ts
  - src/lib/marketplace/adapters/tobizon/client.ts
  - src/lib/marketplace/adapters/tobizon/types.ts
  - src/lib/marketplace/adapters/tobizon/status-map.ts
autonomous: true
requirements:
  - MKT-V2

must_haves:
  truths:
    - "올웨이즈 adapter has stub implementation with TODO markers"
    - "텐바이텐 adapter has stub implementation with TODO markers"
    - "토스쇼핑 adapter has stub implementation with TODO markers"
    - "투비즈온 adapter has stub implementation with TODO markers"
  artifacts:
    - path: "src/lib/marketplace/adapters/always/adapter.ts"
      provides: "올웨이즈 stub adapter"
      contains: "class AlwaysAdapter"
    - path: "src/lib/marketplace/adapters/10x10/adapter.ts"
      provides: "텐바이텐 stub adapter"
      contains: "class TenByTenAdapter"
    - path: "src/lib/marketplace/adapters/toss-shopping/adapter.ts"
      provides: "토스쇼핑 stub adapter"
      contains: "class TossShoppingAdapter"
    - path: "src/lib/marketplace/adapters/tobizon/adapter.ts"
      provides: "투비즈온 stub adapter"
      contains: "class TobizonAdapter"
  key_links:
    - from: "src/lib/marketplace/adapters/always/adapter.ts"
      to: "src/lib/marketplace/types.ts"
      via: "implements MarketplaceAdapter"
      pattern: "MarketplaceAdapter"
---

<objective>
Create 4 Tier 3 marketplace adapters (batch B): 올웨이즈, 텐바이텐, 토스쇼핑, 투비즈온.

Purpose: These marketplaces have limited or unknown APIs. Each gets a stub implementation with TODO markers per D-03.
Output: 4 adapter directories with stub 4-file pattern
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-cafe24-cj-ns-b2b/07-CONTEXT.md

@src/lib/marketplace/types.ts
@src/lib/marketplace/errors.ts
@src/lib/marketplace/adapters/ohouse/adapter.ts

<interfaces>
```typescript
export interface MarketplaceAdapter {
  readonly config: MarketplaceConfig
  testConnection(credentials: MarketplaceCredentials): Promise<{ success: boolean; error?: string; expiresAt?: Date }>
  authenticate(): Promise<{ success: boolean; expiresAt?: Date }>
  getOrders(since: Date): Promise<NormalizedOrder[]>
  getClaimsOrders(since: Date): Promise<NormalizedClaim[]>
  uploadInvoice(orderId: string, invoice: InvoiceData): Promise<{ success: boolean; error?: string }>
  getProducts(): Promise<NormalizedProduct[]>
  registerProduct(product: NormalizedProduct): Promise<{ success: boolean; marketplaceProductId?: string; error?: string }>
  updateProduct(marketplaceProductId: string, product: Partial<NormalizedProduct>): Promise<{ success: boolean; error?: string }>
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 올웨이즈 and 텐바이텐 stub adapters</name>
  <files>
    src/lib/marketplace/adapters/always/adapter.ts
    src/lib/marketplace/adapters/always/client.ts
    src/lib/marketplace/adapters/always/types.ts
    src/lib/marketplace/adapters/always/status-map.ts
    src/lib/marketplace/adapters/10x10/adapter.ts
    src/lib/marketplace/adapters/10x10/client.ts
    src/lib/marketplace/adapters/10x10/types.ts
    src/lib/marketplace/adapters/10x10/status-map.ts
  </files>
  <action>
    Create 2 stub adapters with the minimal stub pattern (per D-01, D-03). Same pattern as Plan 03.

    For EACH adapter, create 4 files:
    - `client.ts`: ky instance with placeholder base URL and API key header. `// TODO: Update when API docs available`
    - `types.ts`: Minimal placeholder types. `// TODO: Update when API docs available`
    - `status-map.ts`: Always returns 'new' with console.warn. `// TODO: Map real statuses`
    - `adapter.ts`: testConnection returns pending message, all data methods throw MarketplaceApiError with TODO

    Specific configs:
    - **올웨이즈** (id: 'always'): name='올웨이즈', authType='api_key', rateLimitPerSecond=20, requiredCredentials=['api_key', 'seller_id']
    - **텐바이텐** (id: '10x10'): name='텐바이텐', authType='api_key', rateLimitPerSecond=20, requiredCredentials=['api_key', 'shop_id']

    NOTE: Directory name `10x10` may need special handling in imports. Use the directory name as-is but ensure the class name is `TenByTenAdapter` (valid JS identifier).
  </action>
  <verify>
    <automated>npx tsc --noEmit --strict src/lib/marketplace/adapters/always/adapter.ts src/lib/marketplace/adapters/10x10/adapter.ts 2>&1 | head -20</automated>
  </verify>
  <done>올웨이즈 and 텐바이텐 stub adapters created, TypeScript-clean</done>
</task>

<task type="auto">
  <name>Task 2: Create 토스쇼핑 and 투비즈온 stub adapters</name>
  <files>
    src/lib/marketplace/adapters/toss-shopping/adapter.ts
    src/lib/marketplace/adapters/toss-shopping/client.ts
    src/lib/marketplace/adapters/toss-shopping/types.ts
    src/lib/marketplace/adapters/toss-shopping/status-map.ts
    src/lib/marketplace/adapters/tobizon/adapter.ts
    src/lib/marketplace/adapters/tobizon/client.ts
    src/lib/marketplace/adapters/tobizon/types.ts
    src/lib/marketplace/adapters/tobizon/status-map.ts
  </files>
  <action>
    Create 2 more stub adapters with the minimal stub pattern (per D-01, D-03).

    Specific configs:
    - **토스쇼핑** (id: 'toss-shopping'): name='토스쇼핑', authType='api_key', rateLimitPerSecond=20, requiredCredentials=['api_key', 'seller_id']
    - **투비즈온** (id: 'tobizon'): name='투비즈온', authType='api_key', rateLimitPerSecond=20, requiredCredentials=['api_key', 'partner_id']

    Same stub pattern: testConnection returns pending, all data methods throw MarketplaceApiError with TODO markers.
  </action>
  <verify>
    <automated>npx tsc --noEmit --strict src/lib/marketplace/adapters/toss-shopping/adapter.ts src/lib/marketplace/adapters/tobizon/adapter.ts 2>&1 | head -20</automated>
  </verify>
  <done>토스쇼핑 and 투비즈온 stub adapters created, TypeScript-clean</done>
</task>

</tasks>

<verification>
- All 4 stub adapter directories exist with 4 files each (16 files total)
- `npx tsc --noEmit` passes for all adapter files
- All have TODO markers for future implementation
- testConnection returns informative pending message
</verification>

<success_criteria>
- 4 Tier 3b stub adapters: always, 10x10, toss-shopping, tobizon
- Each has 4 files following the pattern (per D-01)
- All stubs have TODO markers (per D-03)
- All TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-cafe24-cj-ns-b2b/07-04-SUMMARY.md`
</output>
