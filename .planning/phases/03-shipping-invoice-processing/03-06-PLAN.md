---
phase: 03-shipping-invoice-processing
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema.ts
  - src/lib/products/apply-mappings.ts
  - src/lib/shipping/excel/cj-export.ts
  - src/app/api/shipping/cj/export/route.ts
autonomous: true
requirements: [SHIP-07]
gap_closure: true

must_haves:
  truths:
    - "inventory table has warehouseZone and sectorCode columns for picking location"
    - "productNameMappings table has pickingLocation column"
    - "CJ export Excel includes picking location in the 위치 column (column 24)"
    - "apply-mappings returns pickingLocation alongside displayName"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "warehouseZone + sectorCode on inventory, pickingLocation on productNameMappings"
      contains: "warehouseZone"
    - path: "src/lib/products/apply-mappings.ts"
      provides: "Extended mapping lookup with pickingLocation"
      exports: ["loadMappingLookup", "applyMappings"]
    - path: "src/lib/shipping/excel/cj-export.ts"
      provides: "CjOrderRow with pickingLocation field, written to column 24"
      exports: ["CjOrderRow", "generateCjExcel"]
  key_links:
    - from: "src/app/api/shipping/cj/export/route.ts"
      to: "src/lib/products/apply-mappings.ts"
      via: "loadMappingLookup returns pickingLocation"
      pattern: "pickingLocation"
    - from: "src/lib/shipping/excel/cj-export.ts"
      to: "CjOrderRow.pickingLocation"
      via: "Written to 위치 column in Excel output"
      pattern: "pickingLocation"
---

<objective>
Add picking location (피킹위치) support to the inventory and product mapping schema, and wire it through to CJ export Excel output.

Purpose: Warehouse workers need picking location (e.g. A-01-03) on the CJ packing slip to find products efficiently. Currently the 위치 column in CJ export is blank.
Output: Schema migration fields, extended mapping lookup, CJ Excel with populated 위치 column.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/db/schema.ts
@src/lib/products/apply-mappings.ts
@src/lib/shipping/excel/cj-export.ts
@src/app/api/shipping/cj/export/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add picking location fields to schema and extend mapping lookup</name>
  <files>src/lib/db/schema.ts, src/lib/products/apply-mappings.ts</files>
  <action>
1. In schema.ts, add two columns to the `inventory` table:
   - `warehouseZone`: varchar('warehouse_zone', { length: 50 }) -- values like '1창고', '쿠팡전용창고', '중국창고'
   - `sectorCode`: varchar('sector_code', { length: 50 }) -- values like 'A-01-03'

2. In schema.ts, add one column to the `productNameMappings` table:
   - `pickingLocation`: varchar('picking_location', { length: 100 }) -- composite display like '1창고 A-01-03'

3. In apply-mappings.ts:
   - Extend the `loadMappingLookup` return type to include pickingLocation. Change the Map value from `string` (displayName) to `{ displayName: string; pickingLocation: string | null }`.
   - Update `applyMappings` to return pickingLocation on each item. Add `pickingLocation?: string | null` to the `OrderItemLike` interface return.
   - The function should spread pickingLocation onto the returned item when a mapping exists.

4. Run `npx drizzle-kit generate` to create migration SQL, then apply with `npx drizzle-kit push`.

Note: These are additive column changes (nullable), so no data migration needed.
  </action>
  <verify>
    <automated>cd /Users/ian/Desktop/funtastic-saas && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>inventory table has warehouseZone + sectorCode, productNameMappings has pickingLocation, mapping lookup returns pickingLocation</done>
</task>

<task type="auto">
  <name>Task 2: Wire picking location into CJ export Excel</name>
  <files>src/lib/shipping/excel/cj-export.ts, src/app/api/shipping/cj/export/route.ts</files>
  <action>
1. In cj-export.ts:
   - Add `pickingLocation?: string` to the `CjOrderRow` interface.
   - In `generateCjExcel`, write `row.pickingLocation ?? ''` to column 24 (위치 column, currently empty string at index 23 in the row array).

2. In the CJ export route (route.ts):
   - The `loadMappingLookup` now returns `{ displayName, pickingLocation }` per mapping entry.
   - After `applyMappings`, the first mapped item will have `pickingLocation`.
   - Pass `pickingLocation: firstItem?.pickingLocation ?? ''` into the CjOrderRow.
   - Update the type of `mapped` variable to account for the new pickingLocation field.

3. Also update the `applyMappings` call usage to extract pickingLocation from the first mapped item for the CJ row.
  </action>
  <verify>
    <automated>cd /Users/ian/Desktop/funtastic-saas && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>CJ export Excel 위치 column (column 24) is populated with pickingLocation from product mapping when available</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- CJ export route compiles and pickingLocation flows from mapping -> CjOrderRow -> Excel column 24
- Schema changes are additive (no breaking changes to existing data)
</verification>

<success_criteria>
- inventory table has warehouseZone and sectorCode columns
- productNameMappings table has pickingLocation column
- CJ export Excel writes pickingLocation to 위치 column
- All TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-shipping-invoice-processing/03-06-SUMMARY.md`
</output>
