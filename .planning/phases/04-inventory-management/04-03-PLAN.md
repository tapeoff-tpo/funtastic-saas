---
phase: 04-inventory-management
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - src/app/(auth)/inventory/page.tsx
  - src/app/(auth)/inventory/inventory-table.tsx
  - src/app/(auth)/inventory/adjust-stock-dialog.tsx
  - src/app/(auth)/inventory/excel-upload-dialog.tsx
autonomous: true
requirements: [INV-01, INV-04]

must_haves:
  truths:
    - "Inventory table shows warehouseZone and sectorCode columns"
    - "Admin can filter inventory list by warehouse zone"
    - "Stock registration form includes warehouseZone and sectorCode input fields"
    - "Admin can upload an Excel file via a dialog to bulk-register inventory"
    - "Excel upload dialog shows success/failure count after upload"
  artifacts:
    - path: "src/app/(auth)/inventory/inventory-table.tsx"
      provides: "Table with picking location columns and warehouse zone filter"
      contains: "warehouseZone"
    - path: "src/app/(auth)/inventory/adjust-stock-dialog.tsx"
      provides: "Stock set dialog with picking location input fields"
      contains: "warehouseZone"
    - path: "src/app/(auth)/inventory/excel-upload-dialog.tsx"
      provides: "Excel file upload dialog for bulk inventory registration"
      min_lines: 50
  key_links:
    - from: "src/app/(auth)/inventory/excel-upload-dialog.tsx"
      to: "/api/inventory/bulk-upload"
      via: "fetch POST with FormData"
      pattern: "fetch.*bulk-upload"
    - from: "src/app/(auth)/inventory/inventory-table.tsx"
      to: "nuqs"
      via: "warehouseZone URL query state for filtering"
      pattern: "warehouseZone"
---

<objective>
Update inventory management UI to display picking location columns, add warehouse zone filtering, include picking location fields in the registration form, and add an Excel bulk upload dialog.

Purpose: Admin needs to see and manage picking locations (warehouseZone, sectorCode) in the inventory table, filter by warehouse zone (e.g., '1창고', '쿠팡전용창고'), and bulk-register inventory via Excel upload on each incoming shipment.

Output: Updated inventory table, registration form, and new Excel upload dialog.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-inventory-management/04-01-PLAN.md
@.planning/phases/04-inventory-management/04-02-PLAN.md
@src/app/(auth)/inventory/page.tsx
@src/app/(auth)/inventory/inventory-table.tsx
@src/app/(auth)/inventory/adjust-stock-dialog.tsx

<interfaces>
<!-- After 04-01: Updated InventoryRecord -->
interface InventoryRecord {
  id: string; userId: string; sku: string; productName: string;
  warehouseZone: string | null; sectorCode: string | null;
  totalStock: number; reservedStock: number; availableStock: number;
  createdAt: Date; updatedAt: Date;
}

<!-- After 04-01: Updated InventoryFilters -->
interface InventoryFilters {
  page?: number; pageSize?: number; search?: string;
  sort?: string; order?: 'asc' | 'desc';
  warehouseZone?: string;
}

<!-- After 04-02: setStockAction accepts warehouseZone/sectorCode in FormData -->
<!-- After 04-02: POST /api/inventory/bulk-upload accepts file FormData -->
<!-- Returns: { total, success, failed, errors: Array<{ sku, error }> } -->

<!-- Existing InventoryRow in inventory-table.tsx -->
interface InventoryRow {
  id: string; sku: string; productName: string;
  totalStock: number; reservedStock: number; availableStock: number;
  updatedAt: Date;
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add picking location columns, warehouse filter, and Excel upload button to inventory table</name>
  <files>src/app/(auth)/inventory/page.tsx, src/app/(auth)/inventory/inventory-table.tsx</files>
  <action>
1. In `src/app/(auth)/inventory/inventory-table.tsx`:
   - Add `warehouseZone: string | null` and `sectorCode: string | null` to `InventoryRow` interface.
   - Add two columns to the TanStack Table column definitions, between `productName` and `totalStock`:
     - `warehouseZone` column: header '창고' with sort button, cell renders the value or '-' if null.
     - `sectorCode` column: header '피킹위치' with sort button, cell renders value in monospace or '-'.
   - Add `warehouseZone` to nuqs query state (`parseAsString`).
   - Add a warehouse zone filter dropdown above the table (next to search input). The dropdown shows "전체 창고" as default option. Populate options by extracting unique warehouseZone values from data prop (pass `warehouseZones: string[]` from page). On change, set nuqs `warehouseZone` param (null for '전체 창고') and reset page to 1.
   - Add an "엑셀 업로드" button next to the "재고 등록" button in the toolbar. This button opens the ExcelUploadDialog (import from `./excel-upload-dialog`).
   - Update `InventoryTableProps` to accept `warehouseZones: string[]`.

2. In `src/app/(auth)/inventory/page.tsx`:
   - Add `warehouseZone` to nuqs `searchParamsCache`.
   - Pass `warehouseZone` filter to `getInventoryList` via InventoryFilters.
   - Pass `warehouseZone` and `sectorCode` through in the `data` mapping to InventoryTable.
   - Query distinct warehouse zones for the filter dropdown. Use a simple query: `db.selectDistinct({ warehouseZone: inventory.warehouseZone }).from(inventory).where(and(eq(inventory.userId, user.id), isNotNull(inventory.warehouseZone)))`. Pass the result as `warehouseZones` prop.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Inventory table shows 창고 and 피킹위치 columns, warehouse zone filter dropdown works via nuqs, Excel upload button visible in toolbar</done>
</task>

<task type="auto">
  <name>Task 2: Add picking location fields to registration form and create Excel upload dialog</name>
  <files>src/app/(auth)/inventory/adjust-stock-dialog.tsx, src/app/(auth)/inventory/excel-upload-dialog.tsx</files>
  <action>
1. In `src/app/(auth)/inventory/adjust-stock-dialog.tsx`:
   - In 'set' mode (stock registration), add two input fields after the totalStock field:
     - `warehouseZone` input: label '창고', placeholder '예: 1창고, 쿠팡전용창고', name='warehouseZone'
     - `sectorCode` input: label '피킹위치', placeholder '예: A-01-03', name='sectorCode'
   - These fields should NOT appear in 'adjust' mode (delta adjustment).

2. Create `src/app/(auth)/inventory/excel-upload-dialog.tsx`:
   - 'use client' component.
   - Props: `onClose: () => void`.
   - State: `file` (File | null), `uploading` (boolean), `result` ({ total, success, failed, errors }[] | null).
   - Render a dialog overlay (same pattern as adjust-stock-dialog: fixed inset-0 backdrop).
   - Dialog content:
     - Title: '엑셀 재고 업로드'
     - File input accepting `.xlsx,.xls` files.
     - Info text: '필수 컬럼: SKU(품번), 상품명, 수량(재고). 선택 컬럼: 창고, 위치(피킹위치)'
     - Upload button (disabled when no file or uploading).
     - On upload: create FormData with file, POST to `/api/inventory/bulk-upload`, parse JSON response.
     - After upload: show result summary — "총 {total}건 중 {success}건 성공, {failed}건 실패". If errors array is non-empty, show error list with SKU and error message.
     - Close button and "완료" button (which calls `router.refresh()` then `onClose()`).
   - Use `useRouter` from 'next/navigation' for `router.refresh()`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Stock registration form shows 창고/피킹위치 inputs in set mode, Excel upload dialog handles file selection, upload, and result display</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Inventory table has 창고 and 피킹위치 columns visible
- Warehouse zone filter dropdown renders
- Excel upload dialog renders and calls /api/inventory/bulk-upload
- Registration form has warehouseZone and sectorCode fields in set mode only
</verification>

<success_criteria>
- Inventory table displays warehouseZone (창고) and sectorCode (피킹위치) columns
- Warehouse zone filter dropdown filters the list via URL params
- Stock registration form includes 창고 and 피킹위치 input fields
- Excel upload dialog accepts .xlsx files, uploads to API, shows success/failure results
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-inventory-management/04-03-SUMMARY.md`
</output>
