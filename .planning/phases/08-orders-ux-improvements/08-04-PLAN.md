---
phase: 08-orders-ux-improvements
plan: 04
type: execute
wave: 2
depends_on: ['08-01']
files_modified:
  - src/app/(auth)/inventory/actions.ts
  - src/app/(auth)/inventory/inventory-table.tsx
  - src/app/(auth)/inventory/page.tsx
  - tests/inventory/shipping-cost-edit.test.tsx
autonomous: true
requirements:
  - SC-07
must_haves:
  truths:
    - "재고관리 화면에 상품별 SaaS 배송비(원가) 컬럼이 노출된다 (products.shipping_cost 값)"
    - "각 행의 배송비 셀을 인라인 편집할 수 있다 (input + onBlur server action)"
    - "비숫자/음수 입력은 거부되고 이전 값으로 되돌아간다"
    - "성공 시 sonner toast로 알림, 실패 시 error toast"
    - "updateShippingCost server action이 user 소유 product만 update 한다 (RLS 또는 명시적 userId 체크)"
    - "Plan 01의 tests/inventory/shipping-cost-edit.test.tsx RED stub이 GREEN으로 전환된다"
  artifacts:
    - path: "src/app/(auth)/inventory/actions.ts"
      provides: "updateShippingCost(productId, value | null) server action"
      contains: "updateShippingCost"
    - path: "src/app/(auth)/inventory/inventory-table.tsx"
      provides: "ShippingCostCell 인라인 편집 컴포넌트 + 컬럼 등록"
      contains: "ShippingCostCell|배송비"
    - path: "src/app/(auth)/inventory/page.tsx"
      provides: "page-level 데이터 페치에 products.shipping_cost 포함"
      contains: "shippingCost"
  key_links:
    - from: "src/app/(auth)/inventory/inventory-table.tsx"
      to: "src/app/(auth)/inventory/actions.ts"
      via: "ShippingCostCell onBlur → updateShippingCost(productId, value) — useTransition"
      pattern: "updateShippingCost"
    - from: "src/app/(auth)/inventory/actions.ts"
      to: "src/lib/db/schema.ts (products.shippingCost)"
      via: "drizzle update(products).set({ shippingCost }).where(productId AND userId)"
      pattern: "products.*shippingCost|update\\(products\\)"
---

<objective>
재고관리(`/inventory`) 화면에 상품별 SaaS 배송비(원가) 컬럼을 추가하고 인라인 편집 가능하게 만든다.

Purpose: Phase 8 SC-07 — 재고관리에서 상품별 배송비(원가)를 입력/수정할 수 있다.
Output: 인벤토리 테이블의 새 컬럼 + onBlur server action + RED 테스트 GREEN 전환.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-orders-ux-improvements/08-CONTEXT.md
@.planning/phases/08-orders-ux-improvements/08-RESEARCH.md
@.planning/phases/08-orders-ux-improvements/08-01-PLAN.md
@src/app/(auth)/inventory/actions.ts
@src/app/(auth)/inventory/inventory-table.tsx
@src/app/(auth)/inventory/page.tsx

<interfaces>
<!-- Patterns to reuse — DO NOT reinvent -->

From src/app/(auth)/inventory/actions.ts (existing):
- "use server" file with similar mutation actions (e.g., updateInventory, adjustStock)
- Pattern: validate user via supabase auth, then drizzle update with userId scope
- revalidatePath('/inventory') at the end

From src/app/(auth)/inventory/inventory-table.tsx (existing):
- Table renders rows; uses `useTransition` already (or follows shadcn/ui dialog pattern)
- Cells like number inputs may already exist (e.g., adjust stock — check for prior pattern to mirror)

From src/app/(auth)/inventory/page.tsx (existing):
- Server component fetches inventory rows + product info
- Passes data into inventory-table.tsx as props
- Likely joins products to inventory by SKU — confirm and extend select to include products.shippingCost

From src/lib/db/schema.ts (Plan 01):
- products.shippingCost: numeric('shipping_cost', { precision: 12, scale: 2 }) — nullable

From sonner (existing):
- import { toast } from 'sonner'
- toast.success() / toast.error()

From RESEARCH § Pattern 4 (lines 225-243):
- Inline cell pattern with useState draft + useTransition + onBlur
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: updateShippingCost server action + page.tsx select 확장 + inventory-table 컬럼 + ShippingCostCell 인라인 편집</name>
  <files>
    src/app/(auth)/inventory/actions.ts,
    src/app/(auth)/inventory/page.tsx,
    src/app/(auth)/inventory/inventory-table.tsx,
    tests/inventory/shipping-cost-edit.test.tsx
  </files>
  <read_first>
    - src/app/(auth)/inventory/actions.ts (existing server actions — mirror structure: 'use server', supabase auth, drizzle update, revalidatePath)
    - src/app/(auth)/inventory/page.tsx (data fetch — find products select / join)
    - src/app/(auth)/inventory/inventory-table.tsx (column definitions — find existing TanStack column shape)
    - src/lib/db/schema.ts (products.shippingCost from Plan 01 — confirm field name)
    - tests/inventory/shipping-cost-edit.test.tsx (Plan 01 RED stub — extend with concrete asserts)
    - .planning/phases/08-orders-ux-improvements/08-RESEARCH.md § Pattern 4 (인라인 셀 코드 예시)
    - src/components/ui/input (shadcn Input)
  </read_first>
  <behavior>
    **updateShippingCost(productId: string, value: number | null) → { ok: true } | { ok: false; error: string }**
    - 'use server'
    - Get user via createClient + auth.getUser; if no user → `{ ok: false, error: 'unauthorized' }`
    - Validate: value === null OR (typeof value === 'number' && !Number.isNaN(value) && value >= 0); else `{ ok: false, error: 'invalid value' }`
    - drizzle: `db.update(products).set({ shippingCost: value === null ? null : String(value), updatedAt: new Date() }).where(and(eq(products.id, productId), eq(products.userId, user.id)))`
    - If `result.rowCount === 0` → `{ ok: false, error: 'not found or not owner' }`
    - revalidatePath('/inventory'); return `{ ok: true }`

    **page.tsx:**
    - Extend the products query (or join select) to include `shippingCost: products.shippingCost`
    - Pass through to inventory-table as part of row data

    **inventory-table.tsx:**
    - Add a new column `header: 'SaaS 배송비(원가)'` near other product-related columns
    - Cell renders `<ShippingCostCell productId={row.original.productId} value={row.original.shippingCost} />`
    - ShippingCostCell ('use client') component:
      - useState draft (string)
      - useTransition for pending
      - On change: update draft (only digits + dot)
      - On blur: parse number; if NaN OR <0 → revert draft + toast.error('숫자만 입력 가능합니다'); else call `updateShippingCost(productId, draft === '' ? null : Number(draft))` inside startTransition
      - On success: toast.success('배송비 저장됨'); on failure: toast.error(result.error) + revert
      - Disabled while pending
      - Width: `w-24` (Tailwind), text-right, suffix '원' label
  </behavior>
  <action>
    **Step 1 (RED — replace Plan 01 stub):** tests/inventory/shipping-cost-edit.test.tsx:
    ```ts
    import { describe, it, expect } from 'vitest'
    import { readFileSync } from 'node:fs'

    describe('inventory shipping_cost edit', () => {
      it('updateShippingCost server action exists with userId scope', () => {
        const src = readFileSync('src/app/(auth)/inventory/actions.ts', 'utf8')
        expect(src).toMatch(/['"]use server['"]/)
        expect(src).toMatch(/export\s+async\s+function\s+updateShippingCost/)
        expect(src).toMatch(/eq\(products\.userId,\s*user\.id\)/)
        expect(src).toMatch(/eq\(products\.id,\s*productId\)/)
      })
      it('updateShippingCost validates input (rejects NaN / negative)', () => {
        const src = readFileSync('src/app/(auth)/inventory/actions.ts', 'utf8')
        expect(src).toMatch(/Number\.isNaN|isNaN/)
        expect(src).toMatch(/value\s*>=\s*0|value\s*<\s*0/)
      })
      it('inventory-table renders ShippingCostCell for new column', () => {
        const src = readFileSync('src/app/(auth)/inventory/inventory-table.tsx', 'utf8')
        expect(src).toMatch(/ShippingCostCell/)
        expect(src).toMatch(/SaaS 배송비\(원가\)|배송비/)
      })
      it('ShippingCostCell uses useTransition + onBlur + updateShippingCost', () => {
        const src = readFileSync('src/app/(auth)/inventory/inventory-table.tsx', 'utf8')
        expect(src).toMatch(/useTransition/)
        expect(src).toMatch(/onBlur/)
        expect(src).toMatch(/updateShippingCost/)
      })
      it('page.tsx 데이터 페치에 shippingCost 포함', () => {
        const src = readFileSync('src/app/(auth)/inventory/page.tsx', 'utf8')
        expect(src).toMatch(/shippingCost/)
      })
    })
    ```
    Run: `npx vitest run tests/inventory/shipping-cost-edit.test.tsx` → RED.

    **Step 2 (GREEN — actions.ts):** Add at the bottom of src/app/(auth)/inventory/actions.ts (or near other update actions, mirroring style):
    ```ts
    'use server'  // (likely already at file top — do not duplicate)
    // ... existing imports
    import { products } from '@/lib/db/schema'
    import { eq, and } from 'drizzle-orm'
    import { revalidatePath } from 'next/cache'

    export async function updateShippingCost(
      productId: string,
      value: number | null,
    ): Promise<{ ok: true } | { ok: false; error: string }> {
      const supabase = await createClient()
      const { data: { user } } = await supabase.auth.getUser()
      if (!user) return { ok: false, error: 'unauthorized' }

      if (value !== null) {
        if (typeof value !== 'number' || Number.isNaN(value) || value < 0) {
          return { ok: false, error: 'invalid value' }
        }
      }

      const result = await db
        .update(products)
        .set({ shippingCost: value === null ? null : String(value), updatedAt: new Date() })
        .where(and(eq(products.id, productId), eq(products.userId, user.id)))

      // postgres-js returns count via .length / pg returns rowCount; check accordingly
      // If your db wrapper returns { rowCount }, use that:
      // if (!result || (result as any).rowCount === 0) return { ok: false, error: 'not found' }

      revalidatePath('/inventory')
      return { ok: true }
    }
    ```
    Adjust `createClient` and `db` import paths to match existing actions.

    **Step 3 (page.tsx):** In the products/inventory select, ensure `shippingCost` is selected and forwarded into the table data. Example:
    ```ts
    .select({ /* ...existing fields... */, shippingCost: products.shippingCost })
    ```
    Then in the row mapping passed to `<InventoryTable rows={...} />`, include `shippingCost`.

    **Step 4 (inventory-table.tsx):**
    - Add ShippingCostCell client component (top of file or split file):
    ```tsx
    'use client'
    import { useState, useTransition } from 'react'
    import { toast } from 'sonner'
    import { updateShippingCost } from './actions'
    import { Input } from '@/components/ui/input'

    function ShippingCostCell({ productId, value }: { productId: string; value: string | number | null }) {
      const initial = value == null ? '' : String(value)
      const [draft, setDraft] = useState(initial)
      const [pending, startTransition] = useTransition()
      return (
        <Input
          type="number"
          min={0}
          step="1"
          value={draft}
          disabled={pending}
          onChange={(e) => setDraft(e.target.value)}
          onBlur={() => {
            if (draft === initial) return
            const num = draft === '' ? null : Number(draft)
            if (num !== null && (Number.isNaN(num) || num < 0)) {
              setDraft(initial)
              toast.error('숫자만 입력 가능합니다')
              return
            }
            startTransition(async () => {
              const result = await updateShippingCost(productId, num)
              if (result.ok) toast.success('배송비 저장됨')
              else { setDraft(initial); toast.error(result.error) }
            })
          }}
          className="w-24 text-right"
        />
      )
    }
    ```
    - Add the new TanStack column object:
    ```ts
    { header: 'SaaS 배송비(원가)', accessorKey: 'shippingCost',
      cell: ({ row }) => <ShippingCostCell productId={row.original.productId} value={row.original.shippingCost} /> }
    ```
    Place near other product-info columns (after stock or before actions).

    **Step 5 (verify):** `npx vitest run tests/inventory/shipping-cost-edit.test.tsx` → GREEN. `npx tsc --noEmit` → 0. `npm run build` → succeeds.

    DO NOT add bulk update / Excel import for shipping_cost (out of scope — Phase 8 = inline edit only). DO NOT modify other inventory fields.
  </action>
  <verify>
    <automated>npx vitest run tests/inventory/shipping-cost-edit.test.tsx --reporter=dot && npx tsc --noEmit 2>&1 | head -10 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export async function updateShippingCost" src/app/(auth)/inventory/actions.ts` matches
    - `grep -n "eq(products.userId" src/app/(auth)/inventory/actions.ts` matches (RLS — user scope)
    - `grep -n "eq(products.id, productId)" src/app/(auth)/inventory/actions.ts` matches
    - `grep -E "Number\\.isNaN|isNaN" src/app/(auth)/inventory/actions.ts` matches
    - `grep -n "revalidatePath\\('/inventory'\\)" src/app/(auth)/inventory/actions.ts` matches
    - `grep -n "ShippingCostCell" src/app/(auth)/inventory/inventory-table.tsx` matches
    - `grep -n "useTransition" src/app/(auth)/inventory/inventory-table.tsx` matches
    - `grep -n "updateShippingCost" src/app/(auth)/inventory/inventory-table.tsx` matches
    - `grep -E "SaaS 배송비\\(원가\\)|배송비" src/app/(auth)/inventory/inventory-table.tsx` matches
    - `grep -n "shippingCost" src/app/(auth)/inventory/page.tsx` matches
    - tests/inventory/shipping-cost-edit.test.tsx 5개 assertion 모두 GREEN
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>updateShippingCost server action + ShippingCostCell 인라인 편집 셀 + page.tsx 데이터 확장. 모든 acceptance grep 매칭, vitest GREEN, build 성공.</done>
</task>

</tasks>

<verification>
- updateShippingCost: 'use server' + auth + userId scope + 입력 검증 + revalidatePath
- inventory-table.tsx: 새 컬럼 + ShippingCostCell + useTransition + sonner toast
- page.tsx: products.shippingCost 데이터 페치 포함
- Plan 01 RED stub (tests/inventory/shipping-cost-edit.test.tsx) GREEN
- `npx tsc --noEmit && npx vitest run tests/inventory && npm run build` 통과
</verification>

<success_criteria>
- SC-07 ✅ 재고관리 화면에서 상품별 배송비(원가)를 입력/수정 가능 (인라인 + onBlur)
- 비정상 입력 거부 (NaN, 음수) — toast.error
- 권한 체크 (userId scope) — 다른 사용자 product 수정 불가
</success_criteria>

<output>
After completion, create `.planning/phases/08-orders-ux-improvements/08-04-SUMMARY.md` with:
- updateShippingCost server action signature + 검증 규칙
- ShippingCostCell 컴포넌트 위치 + UX 흐름 (draft → onBlur → server → toast)
- page.tsx select 변경 라인
- inventory-table.tsx 컬럼 위치
- 향후 매출관리 phase 사용 hint: products.shippingCost ↔ orders.shippingFee 차이 계산
</output>
