---
phase: 07-cafe24-cj-ns-b2b
plan: 05
type: execute
wave: 2
depends_on: ["07-01", "07-02", "07-03", "07-04"]
files_modified:
  - src/lib/marketplace/adapters/configs.ts
  - src/lib/marketplace/types.ts
autonomous: true
requirements:
  - MKT-V2

must_haves:
  truths:
    - "All 18 new marketplaces are registered in configs.ts and appear in the registry"
    - "MarketplaceId type includes all 18 new marketplace IDs"
    - "registerDefaultAdapters() registers all 24 adapters (6 existing + 18 new)"
  artifacts:
    - path: "src/lib/marketplace/adapters/configs.ts"
      provides: "Registry config for all 24 marketplaces"
      contains: "registerDefaultAdapters"
    - path: "src/lib/marketplace/types.ts"
      provides: "Updated MarketplaceId union type"
      contains: "cafe24"
  key_links:
    - from: "src/lib/marketplace/adapters/configs.ts"
      to: "src/lib/marketplace/registry.ts"
      via: "marketplaceRegistry.register()"
      pattern: "marketplaceRegistry\\.register"
    - from: "src/lib/marketplace/adapters/configs.ts"
      to: "src/lib/marketplace/types.ts"
      via: "imports MarketplaceAdapter type"
      pattern: "import.*MarketplaceAdapter"
---

<objective>
Register all 18 new marketplace adapters in configs.ts and update the MarketplaceId type.

Purpose: This is the wiring step (per D-04) that makes all new adapters discoverable by the registry, credential management UI, and worker system.
Output: Updated configs.ts with 24 total adapters, updated MarketplaceId type
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-cafe24-cj-ns-b2b/07-CONTEXT.md

@src/lib/marketplace/adapters/configs.ts
@src/lib/marketplace/types.ts
@src/lib/marketplace/registry.ts

<interfaces>
<!-- Current MarketplaceId type to extend -->
```typescript
export type MarketplaceId =
  | 'coupang'
  | 'naver'
  | 'elevenst'
  | 'gmarket'
  | 'auction'
  | 'ohouse'
  | (string & {})
```

<!-- Current registerDefaultAdapters pattern from configs.ts -->
```typescript
export function registerDefaultAdapters() {
  if (!marketplaceRegistry.has('coupang')) {
    marketplaceRegistry.register(coupangAdapter)
  }
  // ... etc for each adapter
}
registerDefaultAdapters() // Auto-register on import
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update MarketplaceId type and register all 18 adapters in configs.ts</name>
  <files>
    src/lib/marketplace/types.ts
    src/lib/marketplace/adapters/configs.ts
  </files>
  <action>
    **Step 1: Update MarketplaceId in types.ts**

    Read `src/lib/marketplace/types.ts`. Add all 18 new marketplace IDs to the MarketplaceId union type:

    ```typescript
    export type MarketplaceId =
      | 'coupang'
      | 'naver'
      | 'elevenst'
      | 'gmarket'
      | 'auction'
      | 'ohouse'
      | 'cafe24'
      | 'cjonestyle'
      | 'kakao-gift'
      | 'kakao-store'
      | 'domeggook'
      | 'onchannel'
      | 'ownerclan'
      | 'ssgmall'
      | 'ably'
      | 'hyundai-hmall'
      | 'nsmall'
      | 'domesin'
      | 'domechango'
      | 'banana-b2b'
      | 'always'
      | '10x10'
      | 'toss-shopping'
      | 'tobizon'
      | (string & {})
    ```

    **Step 2: Update configs.ts to register all 18 new adapters**

    Read `src/lib/marketplace/adapters/configs.ts`. For each of the 18 new adapters, add a placeholder adapter object (same pattern as existing coupang/naver/elevenst/gmarket/auction/ohouse entries) with the correct config:

    Add 18 new const adapter objects following the EXACT same pattern as existing entries. Each has:
    - config with correct id, name, authType, rateLimitPerSecond, requiredCredentials (matching what was defined in plans 01-04)
    - All methods as notImplemented stubs

    The full list of new adapters to add to configs.ts:
    1. cafe24Adapter: id='cafe24', name='Cafe24', authType='oauth2', rate=40, creds=['client_id','client_secret','mall_id']
    2. cjonestyleAdapter: id='cjonestyle', name='CJ온스타일', authType='api_key', rate=30, creds=['api_key','seller_code']
    3. kakaoGiftAdapter: id='kakao-gift', name='카카오선물하기', authType='api_key', rate=30, creds=['api_key','store_id']
    4. kakaoStoreAdapter: id='kakao-store', name='카카오톡스토어', authType='api_key', rate=30, creds=['api_key','store_id']
    5. domeggookAdapter: id='domeggook', name='도매꾹', authType='api_key', rate=20, creds=['api_key','seller_id']
    6. onchannelAdapter: id='onchannel', name='온채널', authType='api_key', rate=20, creds=['api_key','shop_id']
    7. ownerclanAdapter: id='ownerclan', name='오너클랜', authType='api_key', rate=20, creds=['api_key','seller_id']
    8. ssgmallAdapter: id='ssgmall', name='신세계몰', authType='api_key', rate=30, creds=['api_key','vendor_id']
    9. ablyAdapter: id='ably', name='에이블리', authType='api_key', rate=30, creds=['api_key','shop_id']
    10. hyundaiHmallAdapter: id='hyundai-hmall', name='현대홈쇼핑', authType='api_key', rate=20, creds=['api_key','seller_id']
    11. nsmallAdapter: id='nsmall', name='NS홈쇼핑', authType='api_key', rate=20, creds=['api_key','vendor_code']
    12. domesinAdapter: id='domesin', name='도매의신', authType='api_key', rate=20, creds=['api_key','seller_id']
    13. domechangoAdapter: id='domechango', name='도매창고', authType='api_key', rate=20, creds=['api_key','seller_id']
    14. bananaB2bAdapter: id='banana-b2b', name='바나나B2B', authType='api_key', rate=20, creds=['api_key','partner_id']
    15. alwaysAdapter: id='always', name='올웨이즈', authType='api_key', rate=20, creds=['api_key','seller_id']
    16. tenByTenAdapter: id='10x10', name='텐바이텐', authType='api_key', rate=20, creds=['api_key','shop_id']
    17. tossShoppingAdapter: id='toss-shopping', name='토스쇼핑', authType='api_key', rate=20, creds=['api_key','seller_id']
    18. tobizonAdapter: id='tobizon', name='투비즈온', authType='api_key', rate=20, creds=['api_key','partner_id']

    Update `registerDefaultAdapters()` to register all 18 new adapters using the same `if (!marketplaceRegistry.has('id'))` guard pattern.

    The notImplemented error message should be updated from "Phase 2" to just "Not implemented yet" for the new adapters.
  </action>
  <verify>
    <automated>npx tsc --noEmit --strict src/lib/marketplace/adapters/configs.ts src/lib/marketplace/types.ts 2>&1 | head -20</automated>
  </verify>
  <done>configs.ts registers all 24 adapters (6 existing + 18 new), MarketplaceId type includes all 24 IDs, registerDefaultAdapters() handles all</done>
</task>

</tasks>

<verification>
- `grep -c "marketplaceRegistry.register" src/lib/marketplace/adapters/configs.ts` returns 24
- `grep -c "marketplaceRegistry.has" src/lib/marketplace/adapters/configs.ts` returns 24
- MarketplaceId type in types.ts has 24 literal string entries + (string & {})
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
- All 18 new marketplaces registered in configs.ts (per D-04)
- MarketplaceId type extended with all 18 new IDs
- registerDefaultAdapters() registers all 24 total adapters
- TypeScript compiles without errors
- Existing 6 adapter registrations unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/07-cafe24-cj-ns-b2b/07-05-SUMMARY.md`
</output>
