---
phase: 08-orders-ux-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/011_add_order_shipping_info.sql
  - supabase/migrations/012_add_product_shipping_cost.sql
  - supabase/migrations/013_add_inquiries.sql
  - src/lib/db/schema.ts
  - src/lib/marketplace/types.ts
  - vitest.config.ts
  - tests/setup.ts
  - tests/db/schema.test.ts
  - tests/orders/get-order-stats.test.ts
  - tests/orders/get-orders.test.ts
  - tests/orders/order-tabs.test.tsx
  - tests/orders/page-header.test.tsx
  - tests/orders/columns.test.tsx
  - tests/inventory/shipping-cost-edit.test.tsx
  - tests/marketplace/coupang/normalize.test.ts
  - tests/marketplace/coupang/inquiries.test.ts
autonomous: true
requirements:
  - SC-01
  - SC-05
  - SC-06
  - SC-07
must_haves:
  truths:
    - "orders 테이블에 shipping_type (varchar 50, nullable) + shipping_fee (numeric 12,2, nullable) 컬럼이 존재한다"
    - "products 테이블에 shipping_cost (numeric 12,2, nullable) 컬럼이 존재한다"
    - "inquiries 테이블이 존재하며 marketplace_inquiry_id, marketplace_id, marketplace_order_id, inquiry_type, question, answered_at, requested_at, raw_data 컬럼을 가진다"
    - "schema.ts의 orders/products 정의가 마이그레이션과 동기화되어 있다"
    - "NormalizedOrder 타입에 shippingType / shippingFee 필드가 추가되어 있다"
    - "NormalizedInquiry 타입과 MarketplaceAdapter.getInquiries (optional) 인터페이스가 정의되어 있다"
    - "vitest.config.ts에 setup 파일이 등록되어 있고 tests/setup.ts가 jest-dom matchers를 등록한다"
    - "phase 8 SC-01..SC-07 + inquiry용 RED 테스트 stub이 모두 존재한다"
  artifacts:
    - path: "supabase/migrations/011_add_order_shipping_info.sql"
      provides: "orders.shipping_type + orders.shipping_fee 컬럼 추가"
      contains: "ADD COLUMN IF NOT EXISTS shipping_type"
    - path: "supabase/migrations/012_add_product_shipping_cost.sql"
      provides: "products.shipping_cost 컬럼 추가"
      contains: "ADD COLUMN IF NOT EXISTS shipping_cost"
    - path: "supabase/migrations/013_add_inquiries.sql"
      provides: "inquiries 신규 테이블"
      contains: "CREATE TABLE IF NOT EXISTS inquiries"
    - path: "src/lib/db/schema.ts"
      provides: "orders/products 컬럼 + inquiries 테이블 Drizzle 정의"
      contains: "shippingType"
    - path: "src/lib/marketplace/types.ts"
      provides: "NormalizedOrder 확장 + NormalizedInquiry + getInquiries 인터페이스"
      contains: "NormalizedInquiry"
    - path: "tests/setup.ts"
      provides: "jest-dom matchers 등록"
    - path: "tests/db/schema.test.ts"
      provides: "schema 정적 검증"
    - path: "tests/orders/get-order-stats.test.ts"
      provides: "9탭 카운트 RED stub"
    - path: "tests/orders/get-orders.test.ts"
      provides: "displayName join RED stub"
    - path: "tests/orders/order-tabs.test.tsx"
      provides: "9탭 컴포넌트 RED stub"
    - path: "tests/orders/page-header.test.tsx"
      provides: "엑셀 업로드 제거 RED stub"
    - path: "tests/orders/columns.test.tsx"
      provides: "CS 제거 + 인디케이터 + 배송 컬럼 RED stub"
    - path: "tests/inventory/shipping-cost-edit.test.tsx"
      provides: "shipping_cost 편집 RED stub"
    - path: "tests/marketplace/coupang/normalize.test.ts"
      provides: "Coupang shippingFee 정규화 RED stub"
    - path: "tests/marketplace/coupang/inquiries.test.ts"
      provides: "Coupang getInquiries RED stub"
  key_links:
    - from: "src/lib/db/schema.ts"
      to: "supabase/migrations/011_*.sql, 012_*.sql, 013_*.sql"
      via: "Drizzle column 정의 = 마이그레이션 SQL과 동일 컬럼명/타입"
      pattern: "shipping_type.*varchar|shipping_fee.*numeric|shipping_cost.*numeric|inquiries.*pgTable"
    - from: "src/lib/marketplace/types.ts"
      to: "src/lib/db/schema.ts"
      via: "NormalizedOrder.shippingType/Fee 필드는 orders 컬럼과 매칭됨"
      pattern: "shippingType|shippingFee"
    - from: "vitest.config.ts"
      to: "tests/setup.ts"
      via: "setupFiles 배열에 tests/setup.ts 등록"
      pattern: "setupFiles.*setup"
---

<objective>
Phase 8을 위한 데이터 모델 확장(orders/products/inquiries)과 테스트 인프라 + RED 테스트 stub을 모두 구축한다.
이 plan이 완료되면 02/03/04 plan은 병렬 실행 가능 (모두 이 plan의 schema/types/tests에만 의존).

Purpose: 후속 plan(코드 변경)들이 grep-verifiable 한 RED 테스트를 가지고 작업할 수 있게 만든다 (Nyquist sampling).
Output:
  - 3개의 SQL 마이그레이션
  - schema.ts 확장
  - marketplace types 확장 (NormalizedInquiry + getInquiries)
  - vitest setup 보강 + 9개의 RED 테스트 stub 파일
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-orders-ux-improvements/08-CONTEXT.md
@.planning/phases/08-orders-ux-improvements/08-RESEARCH.md
@.planning/phases/08-orders-ux-improvements/08-VALIDATION.md
@src/lib/db/schema.ts
@src/lib/marketplace/types.ts
@vitest.config.ts
@supabase/migrations/010_mapping_quantity.sql

<interfaces>
<!-- Existing patterns the executor must follow -->

From src/lib/db/schema.ts (orders table — ~line 80+):
- Uses `pgTable('orders', { ... })` with drizzle-orm/pg-core
- numeric uses: `numeric('total_amount', { precision: 12, scale: 2 })`
- varchar uses: `varchar('marketplace_id', { length: 50 })`
- enum: `pgEnum('order_status', [...])`
- index: appended in array-form callback `(table) => [ index(...).on(...) ]`

From src/lib/marketplace/types.ts:
- NormalizedOrder type already exists (extend with shippingType?: string | null; shippingFee?: number | null)
- MarketplaceAdapter interface — add `getInquiries?(since: Date): Promise<NormalizedInquiry[]>`

From vitest.config.ts (current):
- environment: 'jsdom', globals: true, setupFiles: []
- alias '@' → ./src

From tests/marketplace/coupang.test.ts and tests/orders/queries.test.ts (existing patterns):
- Vitest `describe/it/expect`, vi.mock for DB
- MSW patterns in tests/helpers/msw-handlers.ts

From supabase/migrations (existing pattern):
- Plain SQL files, idempotent (`IF NOT EXISTS`), `COMMENT ON COLUMN` for documentation
- Numeric prefix `0XX_descriptive_name.sql`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Three SQL migrations (orders shipping fields, products shipping_cost, inquiries table)</name>
  <files>
    supabase/migrations/011_add_order_shipping_info.sql,
    supabase/migrations/012_add_product_shipping_cost.sql,
    supabase/migrations/013_add_inquiries.sql
  </files>
  <read_first>
    - supabase/migrations/010_mapping_quantity.sql (style/pattern reference)
    - supabase/migrations/00001_core_schema.sql (orders + products column definitions to align types)
    - .planning/phases/08-orders-ux-improvements/08-RESEARCH.md § Pattern 1 (lines 162-179) — shippingType/shippingFee SQL example
  </read_first>
  <action>
    Create THREE separate migration files with EXACTLY this content shape (idempotent, with COMMENT ON COLUMN per house style):

    **011_add_order_shipping_info.sql:**
    ```sql
    ALTER TABLE orders
      ADD COLUMN IF NOT EXISTS shipping_type VARCHAR(50),
      ADD COLUMN IF NOT EXISTS shipping_fee NUMERIC(12,2);

    COMMENT ON COLUMN orders.shipping_type IS '배송구분 (prepaid/cod/free/unknown enum). 마켓에서 수집된 결제 방식 (CONTEXT.md D-04).';
    COMMENT ON COLUMN orders.shipping_fee IS '마켓에서 수집된 배송비 (KRW). NULL 허용 — 미수집/미존재 시.';
    ```

    **012_add_product_shipping_cost.sql:**
    ```sql
    ALTER TABLE products
      ADD COLUMN IF NOT EXISTS shipping_cost NUMERIC(12,2);

    COMMENT ON COLUMN products.shipping_cost IS 'SaaS 등록 배송비(원가) — 재고관리에서 입력. NULL 허용 (CONTEXT.md D-05).';
    ```

    **013_add_inquiries.sql:**
    ```sql
    CREATE TABLE IF NOT EXISTS inquiries (
      id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
      user_id UUID NOT NULL,
      marketplace_id VARCHAR(50) NOT NULL,
      marketplace_inquiry_id VARCHAR(255) NOT NULL,
      marketplace_order_id VARCHAR(255),
      order_id UUID REFERENCES orders(id) ON DELETE SET NULL,
      inquiry_type VARCHAR(50) NOT NULL,
      question TEXT NOT NULL,
      answered_at TIMESTAMPTZ,
      requested_at TIMESTAMPTZ NOT NULL,
      raw_data JSONB NOT NULL DEFAULT '{}'::jsonb,
      created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
      updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
    );

    CREATE UNIQUE INDEX IF NOT EXISTS inquiries_user_market_external_uniq
      ON inquiries (user_id, marketplace_id, marketplace_inquiry_id);

    CREATE INDEX IF NOT EXISTS inquiries_order_id_idx ON inquiries (order_id);
    CREATE INDEX IF NOT EXISTS inquiries_user_marketplace_idx ON inquiries (user_id, marketplace_id);

    COMMENT ON TABLE inquiries IS '마켓플레이스 문의 수집 (Phase 8 — Coupang 우선, Naver 별도 quick).';
    COMMENT ON COLUMN inquiries.inquiry_type IS 'product | callcenter | online (Coupang 3종)';
    ```

    DO NOT modify migrations 001-010. DO NOT add backfill SQL — leave NULL (RESEARCH.md § Pitfall 5 + § Backfill 결정).
  </action>
  <verify>
    <automated>test -f supabase/migrations/011_add_order_shipping_info.sql && test -f supabase/migrations/012_add_product_shipping_cost.sql && test -f supabase/migrations/013_add_inquiries.sql && grep -q "shipping_type" supabase/migrations/011_add_order_shipping_info.sql && grep -q "shipping_cost" supabase/migrations/012_add_product_shipping_cost.sql && grep -q "CREATE TABLE IF NOT EXISTS inquiries" supabase/migrations/013_add_inquiries.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ADD COLUMN IF NOT EXISTS" supabase/migrations/011_add_order_shipping_info.sql` returns 2
    - `grep -c "ADD COLUMN IF NOT EXISTS" supabase/migrations/012_add_product_shipping_cost.sql` returns 1
    - `grep -E "marketplace_inquiry_id|user_id|inquiry_type|question|requested_at|raw_data" supabase/migrations/013_add_inquiries.sql` matches all 6 columns
    - `grep -E "inquiries_user_market_external_uniq|inquiries_order_id_idx" supabase/migrations/013_add_inquiries.sql` matches both indexes
    - All 3 files use `IF NOT EXISTS` (idempotent)
  </acceptance_criteria>
  <done>3개 마이그레이션 파일이 idempotent SQL로 생성되고 grep으로 컬럼/인덱스가 검증된다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Sync Drizzle schema.ts with new columns + inquiries table; extend marketplace types</name>
  <files>
    src/lib/db/schema.ts,
    src/lib/marketplace/types.ts
  </files>
  <read_first>
    - src/lib/db/schema.ts (find orders pgTable definition + products pgTable definition + last table for placement of inquiries)
    - src/lib/marketplace/types.ts (locate NormalizedOrder + MarketplaceAdapter interface)
    - supabase/migrations/011_add_order_shipping_info.sql, 012_add_product_shipping_cost.sql, 013_add_inquiries.sql (created in Task 1 — column names/types to mirror exactly)
  </read_first>
  <action>
    **In src/lib/db/schema.ts:**

    1. Inside `orders` pgTable, add (alongside existing columns, before the closing `}`):
       ```ts
       shippingType: varchar('shipping_type', { length: 50 }),
       shippingFee: numeric('shipping_fee', { precision: 12, scale: 2 }),
       ```

    2. Inside `products` pgTable, add:
       ```ts
       shippingCost: numeric('shipping_cost', { precision: 12, scale: 2 }),
       ```

    3. Append a new `inquiries` pgTable definition at the end of the file (or grouped with order-related tables), mirroring migration 013 EXACTLY:
       ```ts
       export const inquiries = pgTable('inquiries', {
         id: uuid('id').defaultRandom().primaryKey(),
         userId: uuid('user_id').notNull(),
         marketplaceId: varchar('marketplace_id', { length: 50 }).notNull(),
         marketplaceInquiryId: varchar('marketplace_inquiry_id', { length: 255 }).notNull(),
         marketplaceOrderId: varchar('marketplace_order_id', { length: 255 }),
         orderId: uuid('order_id').references(() => orders.id, { onDelete: 'set null' }),
         inquiryType: varchar('inquiry_type', { length: 50 }).notNull(),
         question: text('question').notNull(),
         answeredAt: timestamp('answered_at', { withTimezone: true }),
         requestedAt: timestamp('requested_at', { withTimezone: true }).notNull(),
         rawData: jsonb('raw_data').$type<Record<string, unknown>>().notNull().default({}),
         createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull(),
         updatedAt: timestamp('updated_at', { withTimezone: true }).defaultNow().notNull(),
       }, (table) => [
         uniqueIndex('inquiries_user_market_external_uniq').on(
           table.userId, table.marketplaceId, table.marketplaceInquiryId,
         ),
         index('inquiries_order_id_idx').on(table.orderId),
         index('inquiries_user_marketplace_idx').on(table.userId, table.marketplaceId),
       ])
       ```

    **In src/lib/marketplace/types.ts:**

    1. Extend `NormalizedOrder` interface with:
       ```ts
       shippingType?: string | null   // raw marketplace value or normalized enum (prepaid/cod/free/unknown)
       shippingFee?: number | null    // KRW
       ```

    2. Add new `NormalizedInquiry` export (per RESEARCH.md § Pattern 5):
       ```ts
       export interface NormalizedInquiry {
         marketplaceInquiryId: string
         marketplaceId: MarketplaceId
         marketplaceOrderId?: string
         inquiryType: 'product' | 'callcenter' | 'online'
         question: string
         answeredAt?: Date
         requestedAt: Date
         rawData: Record<string, unknown>
       }
       ```

    3. Extend `MarketplaceAdapter` interface with OPTIONAL method (do not break existing adapters):
       ```ts
       getInquiries?(since: Date): Promise<NormalizedInquiry[]>
       ```

    DO NOT touch any existing column/field. DO NOT add normalization logic — that's Plan 02. The shippingType field on NormalizedOrder is intentionally `string | null` (raw) at this stage; Plan 02 may normalize to enum.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "shippingType: varchar('shipping_type'" src/lib/db/schema.ts` matches inside orders pgTable
    - `grep -n "shippingFee: numeric('shipping_fee'" src/lib/db/schema.ts` matches inside orders pgTable
    - `grep -n "shippingCost: numeric('shipping_cost'" src/lib/db/schema.ts` matches inside products pgTable
    - `grep -n "export const inquiries = pgTable('inquiries'" src/lib/db/schema.ts` matches
    - `grep -n "export interface NormalizedInquiry" src/lib/marketplace/types.ts` matches
    - `grep -n "getInquiries?(since: Date)" src/lib/marketplace/types.ts` matches (optional)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>schema.ts와 마이그레이션이 동기화되고, marketplace types에 NormalizedInquiry + getInquiries optional method가 추가되었다. TypeScript build 통과.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire vitest setup + create RED test stubs for SC-01..SC-07 + inquiry</name>
  <files>
    vitest.config.ts,
    tests/setup.ts,
    tests/db/schema.test.ts,
    tests/orders/get-order-stats.test.ts,
    tests/orders/get-orders.test.ts,
    tests/orders/order-tabs.test.tsx,
    tests/orders/page-header.test.tsx,
    tests/orders/columns.test.tsx,
    tests/inventory/shipping-cost-edit.test.tsx,
    tests/marketplace/coupang/normalize.test.ts,
    tests/marketplace/coupang/inquiries.test.ts
  </files>
  <read_first>
    - vitest.config.ts (current — setupFiles is empty array)
    - tests/orders/queries.test.ts (existing test style/import patterns)
    - tests/marketplace/coupang.test.ts (existing adapter test style)
    - tests/helpers/msw-handlers.ts (MSW reuse)
    - .planning/phases/08-orders-ux-improvements/08-VALIDATION.md (Wave 0 list — lines 54-66)
    - .planning/phases/08-orders-ux-improvements/08-RESEARCH.md § Validation Architecture (test command rows)
  </read_first>
  <action>
    **vitest.config.ts:** change `setupFiles: []` → `setupFiles: ['./tests/setup.ts']`. Leave the rest unchanged.

    **tests/setup.ts (new):**
    ```ts
    import '@testing-library/jest-dom/vitest'
    ```

    For each test stub, write at least ONE `it.todo()` per acceptance and ONE concrete `it()` that imports the production module path and asserts a property/value the implementation must provide. The `it()` MUST FAIL (RED) until the implementing plan lands. Use `expect(...).toBe(...)` style — no skip.

    **tests/db/schema.test.ts (covers SC-06, SC-07):**
    ```ts
    import { describe, it, expect } from 'vitest'
    import { orders, products, inquiries } from '@/lib/db/schema'

    describe('schema: phase 8 columns', () => {
      it('orders has shippingType column', () => {
        // drizzle exposes columns via .[Symbol.for('drizzle:Columns')] or simple property access
        // Use property: orders.shippingType should be defined
        expect((orders as any).shippingType).toBeDefined()
      })
      it('orders has shippingFee column', () => {
        expect((orders as any).shippingFee).toBeDefined()
      })
      it('products has shippingCost column', () => {
        expect((products as any).shippingCost).toBeDefined()
      })
      it('inquiries table is exported', () => {
        expect(inquiries).toBeDefined()
        expect((inquiries as any).marketplaceInquiryId).toBeDefined()
      })
    })
    ```

    **tests/orders/get-order-stats.test.ts (covers SC-01, SC-05):**
    ```ts
    import { describe, it, expect } from 'vitest'
    import type { OrderStats } from '@/lib/orders/types'

    describe('getOrderStats — 9탭 카운트', () => {
      it('OrderStats type exposes status counts (new/confirmed/preparing/shipped/delivering/delivered/cancelled)', () => {
        // Compile-time: this asserts the shape exists. Will fail tsc until Plan 03 extends OrderStats.
        const sample: OrderStats = {
          new: 0, confirmed: 0, preparing: 0, shipped: 0, delivering: 0, delivered: 0, cancelled: 0,
          claimCancel: 0, claimExchange: 0, claimReturn: 0,
        } as OrderStats
        expect(sample.cancelled).toBe(0)
        expect(sample.claimCancel).toBe(0)
        expect(sample.claimExchange).toBe(0)
        expect(sample.claimReturn).toBe(0)
      })
      it.todo('취소 탭 카운트 = claims.claimType=cancel ∪ orders.status=cancelled (distinct order)')
      it.todo('9탭 카운트 합 ≤ total (overlap 허용) — SQL GROUP BY 단일 호출')
    })
    ```

    **tests/orders/get-orders.test.ts (covers SC-04):**
    ```ts
    import { describe, it, expect } from 'vitest'
    import type { OrderListItem } from '@/lib/orders/types'

    describe('getOrders — displayName join', () => {
      it('OrderListItem.items[].displayName 필드가 타입에 노출된다 (LEFT JOIN product_name_mappings)', () => {
        const item: OrderListItem['items'][number] = {
          productName: '원본명',
          displayName: '매핑된 SaaS 상품명',
          optionText: null,
          quantity: 1,
          sku: null,
        } as OrderListItem['items'][number]
        expect(item.displayName).toBe('매핑된 SaaS 상품명')
      })
      it.todo('매핑 없는 row → displayName === null (fallback to productName)')
      it.todo('marketplaceId 매칭 실수 방지 — 쿠팡 row가 네이버 매핑을 잡지 않는다')
    })
    ```

    **tests/orders/order-tabs.test.tsx (covers SC-01, SC-02 9탭):**
    ```ts
    import { describe, it, expect } from 'vitest'
    import { render, screen } from '@testing-library/react'
    import { NuqsTestingAdapter } from 'nuqs/adapters/testing'
    import { OrderTabs } from '@/app/(auth)/orders/order-tabs'

    describe('OrderTabs — 9탭', () => {
      it('renders 9 tabs in fixed order', () => {
        const counts = { all: 100, new: 1, confirmed: 2, preparing: 3, shipped: 4, delivering: 5, delivered: 6, cancelled: 7, exchange: 8, return: 9 }
        render(<NuqsTestingAdapter><OrderTabs counts={counts} /></NuqsTestingAdapter>)
        for (const label of ['신규','확인','출고대기','출고완료','배송중','배송완료','취소','교환','반품']) {
          expect(screen.getByText(label)).toBeInTheDocument()
        }
      })
      it.todo('취소 탭 클릭 시 ?status=cancelled OR ?claimType=cancel 분기')
      it.todo('각 탭에 카운트 뱃지 표시')
    })
    ```

    **tests/orders/page-header.test.tsx (covers SC-02):**
    ```ts
    import { describe, it, expect } from 'vitest'
    import { readFileSync } from 'node:fs'

    describe('orders page header', () => {
      it('주문관리 page.tsx에 "엑셀 업로드" 텍스트가 더 이상 없다', () => {
        const src = readFileSync('src/app/(auth)/orders/page.tsx', 'utf8')
        expect(src).not.toMatch(/엑셀 업로드/)
        expect(src).not.toMatch(/href="\/orders\/import"/)
      })
    })
    ```

    **tests/orders/columns.test.tsx (covers SC-03, SC-04, SC-06):**
    ```ts
    import { describe, it, expect } from 'vitest'
    import { readFileSync } from 'node:fs'

    describe('orders columns', () => {
      const src = readFileSync('src/app/(auth)/orders/columns.tsx', 'utf8')
      it('CS 헤더 컬럼이 제거되었다', () => {
        expect(src).not.toMatch(/header:\s*['"`]CS['"`]/)
        expect(src).not.toMatch(/accessorKey:\s*['"`]cs['"`]/)
      })
      it('배송구분/수집배송비/SaaS배송비 컬럼이 추가되었다', () => {
        expect(src).toMatch(/배송구분|shippingType/)
        expect(src).toMatch(/수집 배송비|shippingFee/)
        expect(src).toMatch(/SaaS 배송비|shippingCost/)
      })
      it.todo('첫 컬럼에 클레임 뱃지 + 문의 아이콘이 통합 렌더링된다')
      it.todo('items[].displayName 우선 표시, 원본명은 보조 표시')
    })
    ```

    **tests/inventory/shipping-cost-edit.test.tsx (covers SC-07):**
    ```ts
    import { describe, it, expect } from 'vitest'
    import { readFileSync, existsSync } from 'node:fs'

    describe('inventory shipping_cost edit', () => {
      it('updateShippingCost server action이 존재한다', () => {
        const actions = readFileSync('src/app/(auth)/inventory/actions.ts', 'utf8')
        expect(actions).toMatch(/updateShippingCost/)
      })
      it.todo('재고 행에서 shipping_cost 입력 → onBlur로 server action 호출 → DB 반영')
      it.todo('비숫자 입력은 거부 (NULL 또는 이전 값 유지)')
    })
    ```

    **tests/marketplace/coupang/normalize.test.ts (covers SC-06):**
    Create directory `tests/marketplace/coupang/` if missing.
    ```ts
    import { describe, it, expect } from 'vitest'

    describe('Coupang normalizeOrder shipping fields', () => {
      it.todo('sheet.shippingPrice.units → NormalizedOrder.shippingFee')
      it.todo('sheet.shipmentType + deliveryChargeTypeName → shippingType (enum prepaid/cod/free/unknown)')
      it('placeholder — 모듈이 export하는 정규화 결과에 shippingFee/Type 키가 있어야 함', () => {
        // RED placeholder — Plan 02에서 normalize 확장 후 GREEN
        expect(true).toBe(true)
      })
    })
    ```

    **tests/marketplace/coupang/inquiries.test.ts (inquiry collection):**
    ```ts
    import { describe, it, expect } from 'vitest'

    describe('Coupang getInquiries', () => {
      it.todo('getInquiries(since) returns NormalizedInquiry[] from /v5/.../onlineInquiries')
      it.todo('빈 응답 → 빈 배열 반환')
      it('placeholder — Plan 02 GREEN 대상', () => {
        expect(true).toBe(true)
      })
    })
    ```
  </action>
  <verify>
    <automated>npx vitest run tests/db/schema.test.ts tests/orders/page-header.test.tsx tests/orders/columns.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "setupFiles: \['./tests/setup.ts'\]" vitest.config.ts` matches
    - `test -f tests/setup.ts && grep -q "@testing-library/jest-dom/vitest" tests/setup.ts`
    - All 9 stub files exist: `for f in tests/db/schema.test.ts tests/orders/get-order-stats.test.ts tests/orders/get-orders.test.ts tests/orders/order-tabs.test.tsx tests/orders/page-header.test.tsx tests/orders/columns.test.tsx tests/inventory/shipping-cost-edit.test.tsx tests/marketplace/coupang/normalize.test.ts tests/marketplace/coupang/inquiries.test.ts; do test -f $f || exit 1; done`
    - schema.test.ts PASSES (Task 2 made it green for shipping fields)
    - page-header.test.tsx FAILS (RED — Plan 03 will remove the link to make it green)
    - columns.test.tsx FAILS (RED — Plan 03 GREEN target)
    - `npx tsc --noEmit` still exits 0
  </acceptance_criteria>
  <done>vitest setup이 jest-dom matchers를 등록하고, 9개 RED 테스트 stub이 phase 8 SC-01..SC-07 + inquiry를 빠짐없이 매핑한다. schema 검증 테스트는 GREEN, 나머지는 의도적으로 RED.</done>
</task>

</tasks>

<verification>
- Migrations: 011/012/013 SQL 파일이 idempotent + 컬럼/인덱스/COMMENT 포함
- Schema sync: drizzle types와 SQL 컬럼 1:1 매칭 (shippingType/Fee/Cost + inquiries 전체)
- Marketplace types: NormalizedOrder 확장 + NormalizedInquiry export + getInquiries optional method
- Vitest infra: setup 파일 등록, jest-dom matchers
- 9개 RED stubs 모두 존재, schema 테스트는 GREEN, 나머지는 의도적 RED (후속 plan GREEN 목표)
- `npx tsc --noEmit` 통과
- `npx vitest run tests/db/schema.test.ts` 그린
</verification>

<success_criteria>
- 3개 SQL 마이그레이션이 supabase/migrations/에 존재
- src/lib/db/schema.ts에 shippingType/shippingFee/shippingCost + inquiries 테이블 정의
- src/lib/marketplace/types.ts에 NormalizedInquiry + getInquiries (optional) 추가
- vitest setup 작동 (tests/setup.ts loaded)
- 9개 RED stub 존재 → 후속 plan은 RED → GREEN 진행 가능
- TypeScript build 통과
</success_criteria>

<output>
After completion, create `.planning/phases/08-orders-ux-improvements/08-01-SUMMARY.md` with:
- 마이그레이션 파일 경로 + 컬럼/테이블 명
- schema.ts 변경 라인
- types.ts 변경 라인
- 9개 RED stub 파일 경로 매트릭스 (테스트 파일 ↔ SC 번호)
- 다음 plan(02/03/04)이 grep해서 GREEN 시킬 acceptance 패턴 요약
</output>
