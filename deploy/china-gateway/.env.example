# The Chinese-facing hostname. Create an A record for this hostname to the Hong Kong server.
APP_DOMAIN=cn.example.com

# Keep this pointed at the existing Vercel production deployment.
UPSTREAM_HOST=funtastic-saas-vercel.vercel.app

# Email address used by Caddy for TLS certificate notifications.
TLS_EMAIL=ops@example.com
