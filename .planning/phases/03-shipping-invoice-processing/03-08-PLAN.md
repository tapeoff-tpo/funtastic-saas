---
phase: 03-shipping-invoice-processing
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(auth)/orders/claims/page.tsx
  - src/app/(auth)/orders/claims/claims-table.tsx
  - src/lib/orders/claims-queries.ts
  - src/lib/orders/claims-actions.ts
  - src/components/layout/sidebar.tsx
autonomous: true
requirements: [ORD-05]
gap_closure: true

must_haves:
  truths:
    - "Admin can view all claims (cancel/return/exchange) in a dedicated page"
    - "Admin can filter claims by type (cancel, return, exchange) and status (requested, processing, completed, rejected)"
    - "Admin can mark a claim as processing or completed"
    - "Admin can add a memo/note to a claim"
    - "Claims page is accessible from sidebar navigation"
  artifacts:
    - path: "src/lib/orders/claims-queries.ts"
      provides: "getClaims query with filtering"
      exports: ["getClaims"]
    - path: "src/lib/orders/claims-actions.ts"
      provides: "updateClaimStatus and updateClaimMemo server actions"
      exports: ["updateClaimStatus", "updateClaimMemo"]
    - path: "src/app/(auth)/orders/claims/page.tsx"
      provides: "Claims management server page"
      min_lines: 40
    - path: "src/app/(auth)/orders/claims/claims-table.tsx"
      provides: "Client-side claims table with actions"
      min_lines: 80
  key_links:
    - from: "src/app/(auth)/orders/claims/page.tsx"
      to: "src/lib/orders/claims-queries.ts"
      via: "getClaims for data loading"
      pattern: "getClaims"
    - from: "src/app/(auth)/orders/claims/claims-table.tsx"
      to: "src/lib/orders/claims-actions.ts"
      via: "updateClaimStatus server action"
      pattern: "updateClaimStatus"
---

<objective>
Create a dedicated claims management page at /orders/claims for processing cancellations, returns, and exchanges.

Purpose: Claims are already collected from marketplaces into the DB, but there is no UI to manage them. Admin needs to see pending claims, update their processing status, and add notes.
Output: /orders/claims page with filtering, status updates, and memo functionality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/lib/db/schema.ts
@src/lib/orders/queries.ts
@src/components/layout/sidebar.tsx
</context>

<interfaces>
<!-- Existing schema the executor needs -->

From src/lib/db/schema.ts (claims table):
```typescript
claims: {
  id: uuid PK,
  orderId: uuid FK -> orders,
  userId: uuid,
  marketplaceId: varchar(50),
  marketplaceClaimId: varchar(200),
  claimType: 'cancel' | 'return' | 'exchange',
  claimStatus: 'requested' | 'processing' | 'completed' | 'rejected',
  reason: text,
  rawData: jsonb,
  requestedAt: timestamp,
  createdAt, updatedAt: timestamp
}
```

From src/lib/db/schema.ts (orders table):
- buyerName, recipientName, marketplaceOrderId -- for display context
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create claims queries and server actions</name>
  <files>src/lib/orders/claims-queries.ts, src/lib/orders/claims-actions.ts</files>
  <action>
1. Create src/lib/orders/claims-queries.ts with `getClaims(userId, filters?)`:
   - Filters: { claimType?: 'cancel'|'return'|'exchange', claimStatus?: string, page?: number, pageSize?: number }
   - Join claims with orders to get buyerName, recipientName, marketplaceOrderId
   - Also join with orderItems (via orderId) to get productName for context
   - WHERE: claims.userId = userId + optional claimType + optional claimStatus
   - ORDER BY: claims.requestedAt DESC
   - Return: { claims: ClaimWithOrder[], total: number }
   - ClaimWithOrder type: claim fields + order.buyerName + order.recipientName + order.marketplaceOrderId + first item productName
   - Default pageSize: 50

2. Create src/lib/orders/claims-actions.ts with:

   a) `updateClaimStatus(claimId: string, status: 'requested'|'processing'|'completed'|'rejected')`:
      - 'use server' directive
      - Auth check via supabase
      - Verify claim belongs to user (join check)
      - Update claims.claimStatus and claims.updatedAt
      - revalidatePath('/orders/claims')

   b) `updateClaimMemo(claimId: string, memo: string)`:
      - 'use server' directive
      - Auth check via supabase
      - Update claims.reason = memo (reuse reason field -- it was originally the marketplace reason, but admin can overwrite/append)
      - revalidatePath('/orders/claims')
  </action>
  <verify>
    <automated>cd /Users/ian/Desktop/funtastic-saas && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>getClaims returns paginated claims with order context, updateClaimStatus changes claim status, updateClaimMemo saves notes</done>
</task>

<task type="auto">
  <name>Task 2: Create claims management UI and sidebar link</name>
  <files>src/app/(auth)/orders/claims/page.tsx, src/app/(auth)/orders/claims/claims-table.tsx, src/components/layout/sidebar.tsx</files>
  <action>
1. Create src/app/(auth)/orders/claims/page.tsx (server component):
   - Parse searchParams for claimType, claimStatus, page
   - Call getClaims(user.id, filters)
   - Render header "클레임 관리" with count
   - Render filter tabs for type (전체/취소/반품/교환) using nuqs or simple links
   - Render filter for status (전체/접수/처리중/완료/반려)
   - Pass data to ClaimsTable client component

2. Create src/app/(auth)/orders/claims/claims-table.tsx ('use client'):
   - Table columns: 마켓, 주문번호, 구매자, 상품명, 클레임유형, 상태, 접수일, 사유/메모, 액션
   - Claim type badges: 취소 (gray), 반품 (amber), 교환 (blue)
   - Status badges: 접수 (yellow), 처리중 (blue), 완료 (green), 반려 (red)
   - Each row actions:
     * Status dropdown/buttons: 처리중, 완료, 반려
     * Inline memo textarea (save on blur)
   - Use native HTML table with Tailwind (consistent with held page, no TanStack Table needed for simple list)
   - Pagination at bottom if total > pageSize

3. Update src/components/layout/sidebar.tsx:
   - Add nav item: { href: '/orders/claims', label: '클레임', icon: AlertTriangle } (import from lucide-react)
   - Place after the existing orders link
  </action>
  <verify>
    <automated>cd /Users/ian/Desktop/funtastic-saas && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>/orders/claims page renders claims with type/status filters, status update actions, memo editing, and sidebar navigation</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- /orders/claims route exists and renders
- Sidebar shows 클레임 navigation item
- Claims can be filtered by type and status
- Admin can update claim status and add memos
</verification>

<success_criteria>
- Admin can navigate to /orders/claims from sidebar
- Claims are listed with order context (buyer, product, marketplace)
- Claims can be filtered by type (cancel/return/exchange) and status
- Admin can change claim status (processing, completed, rejected)
- Admin can add/edit memo per claim
</success_criteria>

<output>
After completion, create `.planning/phases/03-shipping-invoice-processing/03-08-SUMMARY.md`
</output>
