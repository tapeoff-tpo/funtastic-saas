---
phase: 07-cafe24-cj-ns-b2b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/marketplace/adapters/cafe24/adapter.ts
  - src/lib/marketplace/adapters/cafe24/client.ts
  - src/lib/marketplace/adapters/cafe24/types.ts
  - src/lib/marketplace/adapters/cafe24/status-map.ts
  - src/lib/marketplace/adapters/cjonestyle/adapter.ts
  - src/lib/marketplace/adapters/cjonestyle/client.ts
  - src/lib/marketplace/adapters/cjonestyle/types.ts
  - src/lib/marketplace/adapters/cjonestyle/status-map.ts
  - src/lib/marketplace/adapters/kakao-gift/adapter.ts
  - src/lib/marketplace/adapters/kakao-gift/client.ts
  - src/lib/marketplace/adapters/kakao-gift/types.ts
  - src/lib/marketplace/adapters/kakao-gift/status-map.ts
  - src/lib/marketplace/adapters/kakao-store/adapter.ts
  - src/lib/marketplace/adapters/kakao-store/client.ts
  - src/lib/marketplace/adapters/kakao-store/types.ts
  - src/lib/marketplace/adapters/kakao-store/status-map.ts
autonomous: true
requirements:
  - MKT-V2

must_haves:
  truths:
    - "Cafe24 adapter implements testConnection, getOrders, getClaimsOrders, uploadInvoice with OAuth2 auth"
    - "CJ온스타일 adapter implements all MarketplaceAdapter methods with API key auth"
    - "카카오선물하기 adapter implements all MarketplaceAdapter methods"
    - "카카오톡스토어 adapter implements all MarketplaceAdapter methods"
  artifacts:
    - path: "src/lib/marketplace/adapters/cafe24/adapter.ts"
      provides: "Cafe24 MarketplaceAdapter implementation"
      contains: "class Cafe24Adapter"
    - path: "src/lib/marketplace/adapters/cjonestyle/adapter.ts"
      provides: "CJ온스타일 MarketplaceAdapter implementation"
      contains: "class CjOnestyleAdapter"
    - path: "src/lib/marketplace/adapters/kakao-gift/adapter.ts"
      provides: "카카오선물하기 MarketplaceAdapter implementation"
      contains: "class KakaoGiftAdapter"
    - path: "src/lib/marketplace/adapters/kakao-store/adapter.ts"
      provides: "카카오톡스토어 MarketplaceAdapter implementation"
      contains: "class KakaoStoreAdapter"
  key_links:
    - from: "src/lib/marketplace/adapters/cafe24/adapter.ts"
      to: "src/lib/marketplace/types.ts"
      via: "implements MarketplaceAdapter"
      pattern: "MarketplaceAdapter"
---

<objective>
Create 4 Tier 1 marketplace adapters: Cafe24, CJ온스타일, 카카오선물하기, 카카오톡스토어.

Purpose: These are the best-documented APIs in this batch. Cafe24 uses OAuth2 (documented REST API), the others use API key auth. Each gets a full 4-file implementation per D-01.
Output: 4 adapter directories, each with adapter.ts, client.ts, types.ts, status-map.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-cafe24-cj-ns-b2b/07-CONTEXT.md

@src/lib/marketplace/types.ts
@src/lib/marketplace/errors.ts
@src/lib/marketplace/adapters/elevenst/adapter.ts
@src/lib/marketplace/adapters/elevenst/client.ts
@src/lib/marketplace/adapters/elevenst/types.ts
@src/lib/marketplace/adapters/elevenst/status-map.ts

<interfaces>
<!-- MarketplaceAdapter contract from src/lib/marketplace/types.ts -->
```typescript
export interface MarketplaceAdapter {
  readonly config: MarketplaceConfig
  testConnection(credentials: MarketplaceCredentials): Promise<{ success: boolean; error?: string; expiresAt?: Date }>
  authenticate(): Promise<{ success: boolean; expiresAt?: Date }>
  getOrders(since: Date): Promise<NormalizedOrder[]>
  getClaimsOrders(since: Date): Promise<NormalizedClaim[]>
  uploadInvoice(orderId: string, invoice: InvoiceData): Promise<{ success: boolean; error?: string }>
  getProducts(): Promise<NormalizedProduct[]>
  registerProduct(product: NormalizedProduct): Promise<{ success: boolean; marketplaceProductId?: string; error?: string }>
  updateProduct(marketplaceProductId: string, product: Partial<NormalizedProduct>): Promise<{ success: boolean; error?: string }>
}

export interface MarketplaceConfig {
  readonly id: MarketplaceId
  readonly name: string
  readonly authType: AuthType
  readonly rateLimitPerSecond: number
  readonly requiredCredentials: string[]
}

export type AuthType = 'hmac' | 'oauth2' | 'api_key' | 'session'
```

<!-- Error classes from src/lib/marketplace/errors.ts -->
```typescript
export class MarketplaceAuthError extends Error { constructor(marketplaceId, message, isExpired?) }
export class MarketplaceApiError extends Error { constructor(marketplaceId, message, statusCode?, response?) }
export class MarketplaceRateLimitError extends Error { constructor(marketplaceId, retryAfterMs) }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Cafe24 and CJ온스타일 adapters</name>
  <files>
    src/lib/marketplace/adapters/cafe24/adapter.ts
    src/lib/marketplace/adapters/cafe24/client.ts
    src/lib/marketplace/adapters/cafe24/types.ts
    src/lib/marketplace/adapters/cafe24/status-map.ts
    src/lib/marketplace/adapters/cjonestyle/adapter.ts
    src/lib/marketplace/adapters/cjonestyle/client.ts
    src/lib/marketplace/adapters/cjonestyle/types.ts
    src/lib/marketplace/adapters/cjonestyle/status-map.ts
  </files>
  <action>
    Read the elevenst adapter as reference pattern. Create 2 adapters following the exact same 4-file pattern (per D-01).

    **Cafe24** (id: 'cafe24'):
    - `client.ts`: Use ky with OAuth2 Bearer token auth. Base URL: `https://{mall_id}.cafe24api.com/api/v2`. The mall_id comes from credentials. Create client factory function `createCafe24Client(accessToken, mallId)`.
    - `types.ts`: Define Cafe24Order, Cafe24Claim, Cafe24Product, Cafe24OrderResponse etc. Cafe24 uses JSON responses. Order fields: order_id, order_date, buyer_name, buyer_cellphone, shipping_address, items[], order_status.
    - `status-map.ts`: Map Cafe24 statuses: N00 -> 'new', N10 -> 'confirmed', N20 -> 'preparing', N30 -> 'shipped', N40 -> 'delivered'. Export mapCafe24Status, mapCafe24ClaimType, mapCafe24ClaimStatus functions with console.warn fallback for unknown codes.
    - `adapter.ts`: Export class Cafe24Adapter implements MarketplaceAdapter. Config: id='cafe24', name='Cafe24', authType='oauth2', rateLimitPerSecond=40, requiredCredentials=['client_id', 'client_secret', 'mall_id']. Implement all methods: testConnection (GET /admin/store.json), getOrders (GET /admin/orders.json with start_date param), getClaimsOrders (GET /admin/cancellation.json + /admin/return.json + /admin/exchange.json), uploadInvoice (PUT /admin/orders/{orderId}/shipments with tracking_no and shipping_company_code), getProducts (GET /admin/products.json), registerProduct/updateProduct (POST/PUT /admin/products.json). Use mapCarrierCode from '@/lib/shipping/carrier-codes'. Use MarketplaceAuthError on 401, MarketplaceApiError on other errors.

    **CJ온스타일** (id: 'cjonestyle'):
    - `client.ts`: Use ky with API key auth header. Base URL: `https://api.cjonestyle.com/v1` (best-effort per D-03). Create `createCjOnestyleClient(apiKey)`.
    - `types.ts`: Define CjOnestyleOrder, CjOnestyleClaim, CjOnestyleProduct types. JSON-based API.
    - `status-map.ts`: Map statuses: PAID -> 'new', PREPARING -> 'confirmed', READY -> 'preparing', SHIPPED -> 'shipped', DELIVERED -> 'delivered'. Export mapCjOnestyleStatus, mapCjOnestyleClaimType, mapCjOnestyleClaimStatus.
    - `adapter.ts`: Export class CjOnestyleAdapter. Config: id='cjonestyle', name='CJ온스타일', authType='api_key', rateLimitPerSecond=30, requiredCredentials=['api_key', 'seller_code']. Implement all methods with best-effort endpoints: getOrders (/orders), getClaimsOrders (/claims), uploadInvoice (/orders/{id}/invoice), getProducts (/products), testConnection (/seller/info). Use MarketplaceAuthError/MarketplaceApiError appropriately.

    Per D-05: Use ky for HTTP. No XML parsing needed (both are JSON APIs).
  </action>
  <verify>
    <automated>npx tsc --noEmit --strict src/lib/marketplace/adapters/cafe24/adapter.ts src/lib/marketplace/adapters/cjonestyle/adapter.ts 2>&1 | head -20</automated>
  </verify>
  <done>Both Cafe24 and CjOnestyle adapter directories have 4 files each, all exporting their adapter class, all TypeScript-clean</done>
</task>

<task type="auto">
  <name>Task 2: Create 카카오선물하기 and 카카오톡스토어 adapters</name>
  <files>
    src/lib/marketplace/adapters/kakao-gift/adapter.ts
    src/lib/marketplace/adapters/kakao-gift/client.ts
    src/lib/marketplace/adapters/kakao-gift/types.ts
    src/lib/marketplace/adapters/kakao-gift/status-map.ts
    src/lib/marketplace/adapters/kakao-store/adapter.ts
    src/lib/marketplace/adapters/kakao-store/client.ts
    src/lib/marketplace/adapters/kakao-store/types.ts
    src/lib/marketplace/adapters/kakao-store/status-map.ts
  </files>
  <action>
    Create 2 Kakao platform adapters following the 4-file pattern (per D-01).

    **카카오선물하기** (id: 'kakao-gift'):
    - `client.ts`: Use ky with REST API key auth (Authorization: KakaoAK {api_key}). Base URL: `https://gift-api.kakao.com/api/v1` (best-effort per D-03). Create `createKakaoGiftClient(apiKey)`.
    - `types.ts`: Define KakaoGiftOrder, KakaoGiftClaim, KakaoGiftProduct types. Fields: order_no, ordered_at, buyer_name, buyer_phone, receiver_name, receiver_phone, receiver_address, items[], status.
    - `status-map.ts`: Map statuses: ORDERED -> 'new', ACCEPTED -> 'confirmed', PREPARING -> 'preparing', SHIPPING -> 'shipped', DELIVERED -> 'delivered'. Export mapKakaoGiftStatus, mapKakaoGiftClaimType, mapKakaoGiftClaimStatus.
    - `adapter.ts`: Export class KakaoGiftAdapter. Config: id='kakao-gift', name='카카오선물하기', authType='api_key', rateLimitPerSecond=30, requiredCredentials=['api_key', 'store_id']. Implement all methods: testConnection (/store/info), getOrders (/orders with date filter), getClaimsOrders (/claims), uploadInvoice (/orders/{id}/shipping), getProducts (/products), registerProduct/updateProduct.

    **카카오톡스토어** (id: 'kakao-store'):
    - `client.ts`: Use ky with API key auth. Base URL: `https://store-api.kakao.com/api/v1` (best-effort per D-03). Create `createKakaoStoreClient(apiKey)`.
    - `types.ts`: Define KakaoStoreOrder, KakaoStoreClaim, KakaoStoreProduct types. Similar structure to kakao-gift.
    - `status-map.ts`: Map statuses: same pattern as kakao-gift. Export mapKakaoStoreStatus, mapKakaoStoreClaimType, mapKakaoStoreClaimStatus.
    - `adapter.ts`: Export class KakaoStoreAdapter. Config: id='kakao-store', name='카카오톡스토어', authType='api_key', rateLimitPerSecond=30, requiredCredentials=['api_key', 'store_id']. Same method pattern as kakao-gift but with kakao-store specific endpoints.

    Per D-05: Use ky for HTTP. Both are JSON APIs.
  </action>
  <verify>
    <automated>npx tsc --noEmit --strict src/lib/marketplace/adapters/kakao-gift/adapter.ts src/lib/marketplace/adapters/kakao-store/adapter.ts 2>&1 | head -20</automated>
  </verify>
  <done>Both Kakao adapter directories have 4 files each, all TypeScript-clean, implementing MarketplaceAdapter</done>
</task>

</tasks>

<verification>
- All 4 adapter directories exist with 4 files each (16 files total)
- `npx tsc --noEmit` passes for all adapter files
- Each adapter.ts exports a class implementing MarketplaceAdapter
- Each client.ts uses ky (per D-05)
- Each status-map.ts exports mapping functions with fallback behavior
</verification>

<success_criteria>
- 4 Tier 1 adapters created: cafe24, cjonestyle, kakao-gift, kakao-store
- Each follows the exact 4-file pattern from elevenst/ohouse (per D-01)
- Cafe24 uses OAuth2 auth, others use API key auth
- All TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-cafe24-cj-ns-b2b/07-01-SUMMARY.md`
</output>
