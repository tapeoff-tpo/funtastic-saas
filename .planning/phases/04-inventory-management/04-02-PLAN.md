---
phase: 04-inventory-management
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/lib/inventory/actions.ts
  - src/lib/inventory/excel.ts
  - src/app/api/inventory/bulk-upload/route.ts
  - src/app/(auth)/inventory/actions.ts
autonomous: true
requirements: [INV-01, INV-04]

must_haves:
  truths:
    - "setStock accepts warehouseZone and sectorCode parameters and persists them"
    - "Excel upload parses rows with SKU, productName, totalStock, warehouseZone, sectorCode columns"
    - "Bulk upload creates/updates inventory records with picking location data"
    - "Individual stock registration via server action includes picking location fields"
  artifacts:
    - path: "src/lib/inventory/excel.ts"
      provides: "Excel parsing for bulk inventory upload"
      exports: ["parseInventoryExcel"]
      min_lines: 40
    - path: "src/app/api/inventory/bulk-upload/route.ts"
      provides: "POST endpoint for bulk Excel inventory upload"
      exports: ["POST"]
    - path: "src/lib/inventory/actions.ts"
      provides: "Updated setStock with picking location params"
      contains: "warehouseZone"
    - path: "src/app/(auth)/inventory/actions.ts"
      provides: "Updated setStockAction with picking location fields"
      contains: "warehouseZone"
  key_links:
    - from: "src/app/api/inventory/bulk-upload/route.ts"
      to: "src/lib/inventory/excel.ts"
      via: "parseInventoryExcel import"
      pattern: "parseInventoryExcel"
    - from: "src/app/api/inventory/bulk-upload/route.ts"
      to: "src/lib/inventory/actions.ts"
      via: "setStock for each parsed row"
      pattern: "setStock"
---

<objective>
Extend inventory CRUD to support picking location fields and add bulk Excel upload for inventory registration.

Purpose: Sellers need to register inventory individually (with warehouseZone/sectorCode) and in bulk via Excel upload on every incoming shipment. The setStock action needs to accept and persist the new picking location fields. The bulk upload endpoint parses an Excel file and upserts inventory records.

Output: Updated setStock with picking location, Excel parser, bulk upload API route, updated server actions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-inventory-management/04-01-PLAN.md

<interfaces>
<!-- From src/lib/inventory/actions.ts — setStock signature to update -->
export async function setStock(
  userId: string, sku: string, productName: string, totalStock: number
): Promise<ActionResult>

<!-- From src/lib/inventory/types.ts (after 04-01) -->
export interface InventoryRecord {
  id: string; userId: string; sku: string; productName: string;
  warehouseZone: string | null; sectorCode: string | null;
  totalStock: number; reservedStock: number; availableStock: number;
  createdAt: Date; updatedAt: Date;
}

<!-- From src/app/(auth)/inventory/actions.ts — setStockAction to update -->
export async function setStockAction(formData: FormData): Promise<ActionResult>

<!-- ExcelJS is in the project stack (CLAUDE.md) for reading/writing formatted Excel -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend setStock and setStockAction with picking location fields</name>
  <files>src/lib/inventory/actions.ts, src/app/(auth)/inventory/actions.ts</files>
  <action>
1. In `src/lib/inventory/actions.ts`, update `setStock` signature to accept an optional options object:
   ```typescript
   export async function setStock(
     userId: string,
     sku: string,
     productName: string,
     totalStock: number,
     opts?: { warehouseZone?: string; sectorCode?: string },
   ): Promise<ActionResult>
   ```
   - In the UPDATE branch: include `warehouseZone: opts?.warehouseZone ?? existing.warehouseZone` and `sectorCode: opts?.sectorCode ?? existing.sectorCode` in the `.set()` call (preserve existing values if not provided).
   - In the INSERT branch: include `warehouseZone: opts?.warehouseZone ?? null` and `sectorCode: opts?.sectorCode ?? null` in `.values()`.

2. In `src/app/(auth)/inventory/actions.ts`, update `setStockAction` to extract warehouseZone and sectorCode from FormData:
   ```typescript
   const warehouseZone = (formData.get('warehouseZone') as string | null)?.trim() || undefined
   const sectorCode = (formData.get('sectorCode') as string | null)?.trim() || undefined
   ```
   Pass `{ warehouseZone, sectorCode }` as the 5th argument to `setStock`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>setStock accepts and persists warehouseZone/sectorCode, setStockAction extracts them from FormData</done>
</task>

<task type="auto">
  <name>Task 2: Create Excel parser and bulk upload API route</name>
  <files>src/lib/inventory/excel.ts, src/app/api/inventory/bulk-upload/route.ts</files>
  <action>
1. Create `src/lib/inventory/excel.ts`:
   - Import ExcelJS (`import ExcelJS from 'exceljs'`).
   - Export `parseInventoryExcel(buffer: Buffer)` that returns `Array<{ sku: string; productName: string; totalStock: number; warehouseZone?: string; sectorCode?: string }>`.
   - Read the first worksheet. Expected columns (by header text, case-insensitive match):
     - 'SKU' or 'sku' or '품번' -> sku (required)
     - '상품명' or 'productName' or 'product_name' -> productName (required)
     - '수량' or '재고' or 'totalStock' or 'stock' or 'qty' -> totalStock (required, parse as integer)
     - '창고' or 'warehouseZone' or 'warehouse' -> warehouseZone (optional)
     - '위치' or 'sectorCode' or 'sector' or '피킹위치' -> sectorCode (optional)
   - Read header row (row 1) to determine column indices by matching header text.
   - Iterate rows starting from row 2. Skip rows where sku is empty.
   - Return parsed array. Throw error if required columns (SKU, 상품명, 수량) are not found in headers.

2. Create `src/app/api/inventory/bulk-upload/route.ts`:
   - Export async `POST` handler.
   - Authenticate via `createClient()` + `getUser()`. Return 401 if not authenticated.
   - Read request body as FormData, extract file from `file` field.
   - Convert file to Buffer, call `parseInventoryExcel(buffer)`.
   - For each parsed row, call `setStock(userId, row.sku, row.productName, row.totalStock, { warehouseZone: row.warehouseZone, sectorCode: row.sectorCode })`.
   - Collect results: count success/failure. Return JSON `{ total, success, failed, errors: Array<{ sku, error }> }`.
   - Wrap in try/catch. On parse error, return 400 with error message.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>parseInventoryExcel parses Excel with SKU/상품명/수량/창고/위치 columns, POST /api/inventory/bulk-upload accepts file upload and upserts all rows via setStock</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `src/lib/inventory/excel.ts` exports parseInventoryExcel
- `src/app/api/inventory/bulk-upload/route.ts` exports POST handler
- setStock signature includes opts parameter with warehouseZone/sectorCode
</verification>

<success_criteria>
- setStock persists warehouseZone and sectorCode on insert and update
- setStockAction passes picking location fields from FormData
- parseInventoryExcel correctly maps Korean and English column headers
- Bulk upload endpoint authenticates, parses Excel, upserts via setStock, returns success/failure counts
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-inventory-management/04-02-SUMMARY.md`
</output>
