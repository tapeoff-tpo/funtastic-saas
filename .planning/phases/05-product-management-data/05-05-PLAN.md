---
phase: 05-product-management-data
plan: 05
type: execute
wave: 3
depends_on: [05-01, 05-02, 05-03]
files_modified:
  - src/app/(auth)/products/page.tsx
  - src/app/(auth)/products/new/page.tsx
  - src/app/(auth)/products/[id]/page.tsx
  - src/app/(auth)/products/categories/page.tsx
  - src/app/(auth)/products/import/page.tsx
  - src/lib/products/ui-actions.ts
autonomous: false

requirements: [PROD-01, PROD-02, PROD-03, PROD-04, PROD-05, DATA-02]

must_haves:
  truths:
    - "Admin can view all products in a filterable data table"
    - "Admin can create a product with variants and push to marketplaces"
    - "Admin can edit a product and sync changes to marketplaces"
    - "Admin can trigger reverse collection from marketplace"
    - "Admin can manage category mappings per marketplace"
    - "Admin can bulk import/export products via Excel"
  artifacts:
    - path: "src/app/(auth)/products/page.tsx"
      provides: "Product list page with TanStack Table, filters, actions"
      min_lines: 80
    - path: "src/app/(auth)/products/new/page.tsx"
      provides: "Product create form with variant builder"
      min_lines: 60
    - path: "src/app/(auth)/products/[id]/page.tsx"
      provides: "Product edit page with sync status per marketplace"
      min_lines: 60
    - path: "src/app/(auth)/products/categories/page.tsx"
      provides: "Category mapping management UI"
      min_lines: 40
    - path: "src/app/(auth)/products/import/page.tsx"
      provides: "Excel import page with preview and results"
      min_lines: 40
    - path: "src/lib/products/ui-actions.ts"
      provides: "Server actions bridging UI to product business logic"
      exports: ["getProductsAction", "createProductAction", "syncProductAction"]
  key_links:
    - from: "src/app/(auth)/products/page.tsx"
      to: "src/lib/products/ui-actions.ts"
      via: "Server action calls for data and mutations"
      pattern: "getProductsAction|deleteProductAction"
    - from: "src/app/(auth)/products/new/page.tsx"
      to: "src/lib/products/ui-actions.ts"
      via: "createProductAction form submission"
      pattern: "createProductAction"
    - from: "src/app/(auth)/products/[id]/page.tsx"
      to: "src/lib/products/sync.ts"
      via: "syncProductToMarketplace call"
      pattern: "syncProduct"
---

<objective>
Build the complete product management UI -- product list, create/edit forms with variant builder, category mapping page, reverse collection trigger, and Excel import page.

Purpose: This is the user-facing layer that ties together all Phase 5 backend work into a usable product management interface.
Output: Full product management pages under /products with TanStack Table, forms, dialogs, and marketplace sync controls.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-product-management-data/05-01-SUMMARY.md
@.planning/phases/05-product-management-data/05-02-SUMMARY.md
@.planning/phases/05-product-management-data/05-03-SUMMARY.md
@src/app/(auth)/orders/page.tsx
@src/app/(auth)/inventory/page.tsx
@src/lib/products/types.ts
@src/lib/products/queries.ts
@src/lib/products/actions.ts
@src/lib/products/sync.ts
@src/lib/products/reverse-collect.ts
@src/lib/products/categories.ts
@src/lib/products/excel-import.ts
@src/lib/products/excel-export.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Product list page and server actions bridge</name>
  <files>src/app/(auth)/products/page.tsx, src/lib/products/ui-actions.ts</files>
  <action>
Read first: src/app/(auth)/orders/page.tsx (for TanStack Table + nuqs pattern), src/app/(auth)/inventory/page.tsx (for similar data table pattern), src/lib/products/types.ts, src/lib/products/queries.ts

Create src/lib/products/ui-actions.ts with 'use server':
- getProductsAction(filters): wraps getProducts with auth check
- deleteProductAction(productId): wraps deleteProduct with auth check
- syncProductAction(productId, marketplaceId, connectionId): wraps syncProductToMarketplace with auth check
- syncAllAction(productId): wraps syncProductToAllMarketplaces with auth check
- reverseCollectAction(connectionId, marketplaceId): wraps reverseCollectProducts with auth check
- importExcelAction(formData): wraps parseProductExcel + bulkImportProducts with auth check

Create src/app/(auth)/products/page.tsx:
- Server component that fetches initial product data
- TanStack Table with columns: 상품코드, 상품명, 카테고리, 판매가, 상태, 옵션수, 마켓연동, 작업
- nuqs URL state for filters (search, status, category, page, pageSize)
- Follow orders page pattern: parseAsString, parseAsInteger from nuqs
- Row actions: 편집 (link to /products/[id]), 삭제, 마켓동기화
- Toolbar actions:
  - "상품등록" button -> /products/new
  - "역수집" button -> triggers reverseCollectAction (with marketplace selector dialog)
  - "엑셀 가져오기" button -> /products/import
  - "엑셀 내보내기" button -> calls /api/products/export
- Empty state: "등록된 상품이 없습니다. 상품을 등록하거나 마켓플레이스에서 역수집하세요."
- Per existing patterns: native HTML select for filters, Tailwind styling
  </action>
  <verify>
    <automated>cd /Users/ian/Desktop/funtastic-saas/.claude/worktrees/infallible-pike && npx tsc --noEmit src/app/\(auth\)/products/page.tsx src/lib/products/ui-actions.ts 2>&1 | head -20</automated>
  </verify>
  <done>Product list page renders with filterable TanStack Table. Server actions bridge UI to business logic with auth checks. All toolbar actions wired.</done>
</task>

<task type="auto">
  <name>Task 2: Product create/edit forms and category mapping page</name>
  <files>src/app/(auth)/products/new/page.tsx, src/app/(auth)/products/[id]/page.tsx, src/app/(auth)/products/categories/page.tsx, src/app/(auth)/products/import/page.tsx</files>
  <action>
Read first: src/app/(auth)/products/page.tsx (after Task 1), src/lib/products/types.ts, src/lib/products/actions.ts, src/lib/products/sync.ts, src/lib/products/categories.ts, src/lib/products/category-actions.ts

**src/app/(auth)/products/new/page.tsx** - Product create form:
- Form fields: 상품코드(SKU), 상품명, 설명(textarea), 판매가, 원가, 카테고리(select)
- Variant builder section:
  - "옵션 추가" button adds variant row
  - Each variant row: 옵션명, 옵션값(JSON-like: color=빨강, size=L), SKU, 가격조정
  - Remove variant button per row
- Submit calls createProductAction
- Success: redirect to /products/[id]
- Per D-01, D-05: Product with variants, each variant has own SKU

**src/app/(auth)/products/[id]/page.tsx** - Product edit page:
- Load product with variants and marketplace links
- Same form as create, pre-filled with existing data
- Additional section: "마켓플레이스 연동 현황"
  - For each marketplace link: marketplace name, sync status (badge), last synced date, error message
  - "동기화" button per marketplace
  - "전체 동기화" button
- Submit calls updateProductAction
- Per D-04: Edit + push changes to connected marketplaces

**src/app/(auth)/products/categories/page.tsx** - Category mapping:
- Table: 내부 카테고리 | 마켓플레이스 | 마켓 카테고리ID | 마켓 카테고리명 | 작업
- "매핑 추가" button opens form: internal category (select from existing), marketplace (select), marketplace category ID (text input), category name (text)
- Delete mapping button per row
- Per D-03: Manual mapping UI per marketplace

**src/app/(auth)/products/import/page.tsx** - Excel import:
- File upload area (drag-drop or click)
- After upload: preview table showing first 5 rows with column headers
- "가져오기 실행" button processes import
- Results: created count, updated count, error list with row numbers
- Link to download template (empty xlsx with correct headers)
  </action>
  <verify>
    <automated>cd /Users/ian/Desktop/funtastic-saas/.claude/worktrees/infallible-pike && npx tsc --noEmit src/app/\(auth\)/products/new/page.tsx src/app/\(auth\)/products/\[id\]/page.tsx src/app/\(auth\)/products/categories/page.tsx src/app/\(auth\)/products/import/page.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Product create/edit forms work with variant builder. Category mapping page allows manual mapping. Import page handles file upload with preview and results.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify product management UI</name>
  <files>src/app/(auth)/products/page.tsx</files>
  <action>
Human verification of the complete product management system.
What was built: Product list with filters, create/edit with variant builder, reverse collection, category mapping, marketplace sync status, and Excel import/export.
Steps to verify:
1. Visit /products -- verify empty state message shows
2. Click "상품등록" -- fill form with product name, SKU, price, add 2 variants (e.g., 빨강/L, 파랑/M) -- submit
3. Verify product appears in list with correct variant count
4. Click product to edit -- verify form is pre-filled
5. Visit /products/categories -- add a category mapping
6. Click "엑셀 내보내기" -- verify xlsx downloads with product data
7. Visit /products/import -- upload the exported file -- verify preview shows
8. Check "역수집" button opens marketplace selector
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>User has approved the product management UI after manual verification. Type "approved" or describe issues.</done>
</task>

</tasks>

<verification>
- /products page loads with TanStack Table and filters
- Product create form submits and creates product with variants
- Product edit form loads existing data and saves changes
- Category mapping page allows CRUD on mappings
- Excel export downloads valid xlsx
- Excel import page shows file preview before processing
- All pages follow existing Tailwind styling patterns
</verification>

<success_criteria>
Full product management UI is functional. Admin can create products with variants, map categories, trigger reverse collection, sync to marketplaces, and bulk manage via Excel. All 6 requirements (PROD-01 through PROD-05, DATA-02) have user-facing implementations.
</success_criteria>

<output>
After completion, create `.planning/phases/05-product-management-data/05-05-SUMMARY.md`
</output>
